[project]
name = "supp-slotter"
version = "0.0.0"
description = "Supplement slot planner — YAML-first ontology and scheduler."
requires-python = ">=3.14"
dependencies = [
    "pyyaml>=6.0",
    "jsonschema>=4.21",
    "ruamel-yaml>=0.19.1",
    "surrealdb>=2.0.0",
]

[dependency-groups]
dev = [
    "pytest>=8",
    "ruff>=0.7",
    "pyright>=1.1.380",
]

[tool.ruff]
line-length = 120
target-version = "py314"
extend-exclude = [".planning", "schedule.yaml"]

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "F",    # pyflakes
    "I",    # isort
    "B",    # flake8-bugbear
    "UP",   # pyupgrade
]
ignore = ["E501"]  # docstrings can't always fit in 120 chars

[tool.pyright]
include = ["planner", "tests"]
pythonVersion = "3.14"
typeCheckingMode = "strict"
