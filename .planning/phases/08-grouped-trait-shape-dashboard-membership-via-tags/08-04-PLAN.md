---
id: 08-04
wave: 2
depends_on:
  - 08-01
files_modified:
  - planner/cards/traits.py
  - planner/engine/review.py
autonomous: true
requirements:
  - DT-13
must_haves:
  truths:
    - "readable_traits() excludes is: prefix (was class:) and excludes dashboard: from display output"
    - "review-substance output shows all 6 namespace groups (is:, intake:, effect:, risk:, activity:, dashboard:) as section headings"
    - "review-substance output shows dashboard: namespace entries with label/description from traits.yaml"
    - "review-substance shows unknown slugs (slugs not in traits.yaml for that namespace) in an explicit 'unknown' section so typos are visible"
    - "Policy decision documented inline in planner/cards/traits.py: readable_traits() = schedule-narrative display (excludes is: and dashboard:); grouped_trait_defs() + print_trait_details() = review-substance display (includes all 6 namespaces)"
    - "uv run pytest exits 0 after changes"
---

# Plan 08-04: Stage 2c — review-substance Audit and Display Policy (DT-13)

## Context

Stage 2 plan — independent, depends only on Stage 1 (08-01) being committed. This plan delivers the **display/policy improvements** for `review-substance` and `readable_traits()`. The basic `substance.traits` AttributeError fix in `review.py` is NOT in this plan — it was folded into Stage 1 task 08-01-08 (DT-07e) per cross-AI review HIGH-4 (deferring it would leave `review-substance` broken between Stage 1 and Stage 2 commits).

**What Stage 2 (this plan) handles vs Stage 1:**

| Concern | Where it lives |
|---------|----------------|
| `review.py` loads without `AttributeError` on `substance.traits` | Stage 1 task 08-01-08 (DT-07e) — already fixed |
| `readable_traits()` filter logic for grouped model | This plan (08-04-01) |
| `review-substance` output shows all 6 namespace groups with descriptions | This plan (08-04-02) |
| Unknown-slug surfacing as a dedicated section | This plan (08-04-02) |
| Display policy comment: schedule-narrative vs review-audit | This plan (08-04-01) |

## Tasks

<task id="08-04-01">
  <title>DT-13a: Refactor readable_traits() in planner/cards/traits.py for grouped model + document the two-path display policy</title>
  <action>
    In planner/cards/traits.py, update readable_traits() (lines 141–150):

    Current filter logic:
    - Skips "risk:manual_review"
    - Skips any trait_id starting with "class:"

    New filter logic:
    - Keep skip for "risk:manual_review"
    - Change "startswith('class:')" to "startswith('is:')" — is: replaces class: for intrinsic category markers
    - Add skip for "startswith('dashboard:')" — dashboard membership tags are review-classification axes, not scheduling narrative; they should not appear in schedule.yaml plan narrative (review_tags field)
    - Add a docstring on readable_traits() stating the policy verbatim:
      """
      Return display labels for scheduling-narrative use (schedule.yaml review_tags field).
      Excludes:
      - risk:manual_review (operator-only flag, not narrative content)
      - is:* (intrinsic category — review-classification axis, not a scheduling driver)
      - dashboard:* (operator-curated cluster membership — review-classification axis, not a scheduling driver)
      For full grouped display (all 6 namespaces, used by review-substance), use grouped_trait_defs()
      + print_trait_details() instead. The two paths are intentionally distinct:
        readable_traits()       = schedule narrative (scheduling drivers only)
        review-substance output = full audit (all 6 namespaces visible)
      """

    Note: readable_traits() receives a flat set[str] of prefixed trait IDs. After Stage 1, the flat
    set comes from effective_stack_item_traits() (already fixed in Stage 1 task 08-01-07 to
    reconstruct from per-namespace Substance fields). planner/engine/plan.py delegates to
    effective_stack_item_traits() and does NOT reference substance.traits directly — no changes
    to plan.py are needed.
  </action>
  <read_first>
    - planner/cards/traits.py — read readable_traits() implementation (lines 141–150) and all callers
    - planner/engine/plan.py — search for "readable_traits" call sites to understand how trait_ids is built
    - planner/engine/review.py — search for "readable_traits" call sites (confirm review.py uses grouped_trait_defs() path, not readable_traits())
  </read_first>
  <acceptance_criteria>
    - readable_traits() skips trait_ids starting with "is:" (not "class:")
    - readable_traits() skips trait_ids starting with "dashboard:"
    - readable_traits() has a docstring containing the strings "schedule narrative" and "review-classification axis"
    - readable_traits() docstring explicitly references the two-path policy (readable_traits vs grouped_trait_defs + print_trait_details)
    - python3 -c "from planner.cards.traits import readable_traits; r=readable_traits({'is:antioxidant','intake:food_preferred','dashboard:sleep_recovery','risk:manual_review'}, {}); assert 'is:antioxidant' not in r; assert 'dashboard:sleep_recovery' not in r; assert 'risk:manual_review' not in r; print('OK')" exits 0
    - uv run pytest exits 0
  </acceptance_criteria>
</task>

<task id="08-04-02">
  <title>DT-13b: Update review-substance display — all 6 namespace groups visible, unknown slugs surfaced, dashboard: descriptions rendered</title>
  <action>
    In planner/engine/review.py (the cmd_review_substance command) AND planner/cards/traits.py (grouped_trait_defs / print_trait_details helpers):

    Goal: `uv run python -m planner review-substance data/substances/<card>.yaml` must show all 6 namespace groups as section headings, even when a substance has zero entries for a namespace. Unknown slugs must surface clearly so typos are visible.

    1. Audit the display flow:
       a. cmd_review_substance() loads the substance and the trait registry.
       b. It iterates over namespace groups, printing each as a section heading with its slugs.
       c. For each slug, it looks up the corresponding TraitDef in trait_defs and calls print_trait_details() to render label/description/applies_when/effects.

    2. Required changes:
       a. Ensure the six namespaces are printed in a stable order: is, intake, effect, risk, activity, dashboard.
       b. For each namespace, print the heading even if the substance has no entries for it (e.g., "is: (empty)"). This makes empty-namespace state explicit and discoverable.
       c. For dashboard: entries, call print_trait_details() with the dashboard:* TraitDef from traits.yaml. dashboard:* TraitDefs have label and description but no applies_when/effects — print_trait_details() must handle empty effects gracefully (likely already does; verify).
       d. Add an "unknown" section per namespace: if a substance carries a slug under is: that is NOT registered in traits.yaml under is:, list it under an "unknown" subsection with a hint that this slug is not registered. Equivalent for the other 5 namespaces.
       e. Make sure check_substances() (the strict ref-integrity check from Stage 1 DT-08) is NOT invoked during review-substance — review-substance is diagnostic and must tolerate invalid cards (per cross-AI review concern). Use a tolerant lookup path: trait_defs.get(f"{ns}:{slug}") returning None for the "unknown" subsection.

    3. The display policy boundary (documented inline in code, per task 08-04-01):
       - readable_traits() = schedule narrative (excludes is:, dashboard:, risk:manual_review)
       - review-substance display = full audit (all 6 namespaces shown, unknown slugs surfaced)
       These two functions answer different questions; the policy is stated once in readable_traits() docstring.
  </action>
  <read_first>
    - planner/engine/review.py — read full implementation of cmd_review_substance() (verify Stage 1 fix from task 08-01-08 already replaced substance.traits with the 6-field reconstruction)
    - planner/cards/traits.py — read grouped_trait_defs() and print_trait_details() implementations
    - planner/cards/traits.py — read readable_traits() (this task's task-01 sibling)
  </read_first>
  <acceptance_criteria>
    - uv run python -m planner review-substance data/substances/alpha_gpc__sub_tzg5glskrd.yaml shows "is" namespace section (heading visible even if entries empty)
    - The review output for a substance with dashboard: tags shows the "dashboard" namespace section with label and description from traits.yaml
    - print_trait_details() handles dashboard:* TraitDefs (no applies_when/effects) without crashing
    - A substance with an unknown slug under is: (synthetic test — e.g., temporarily add "is: [zzz_unknown_test_slug]" to a copy of a card) appears in an "unknown" subsection or equivalent, NOT silently dropped
    - review-substance does NOT crash on a substance card with unknown slugs (tolerant parse — strict reference integrity is the job of `planner check`, not `review-substance`)
    - Section order is stable: is, intake, effect, risk, activity, dashboard
    - uv run pytest exits 0
  </acceptance_criteria>
</task>

## Verification

```
uv run python -m planner review-substance data/substances/alpha_gpc__sub_tzg5glskrd.yaml
# Must show all 6 namespace section headings: is, intake, effect, risk, activity, dashboard
# Must show label/description for dashboard:* entries

uv run pytest
# Must pass
```

## How this plan addresses the cross-AI review HIGH-4 concern

Per cross-AI review HIGH-4: the basic fix for `substance.traits` AttributeError in `review.py` was folded into Stage 1 (task 08-01-08, DT-07e). This Stage 2 plan now contains ONLY the display/policy improvements that genuinely belong in Stage 2:
- `readable_traits()` filter refactor + two-path policy comment (08-04-01)
- `review-substance` full-audit display with unknown-slug surfacing and stable namespace ordering (08-04-02)

The two display paths (schedule-narrative `readable_traits()` vs full-audit `review-substance`) are explicitly documented as a policy boundary inside `readable_traits()` docstring, eliminating the "muddled display policy" risk that Codex flagged as HIGH for Plan 08-04.
