---
id: 08-04
wave: 2
depends_on:
  - 08-01
files_modified:
  - planner/cards/traits.py
  - planner/engine/review.py
autonomous: true
requirements:
  - DT-13
must_haves:
  truths:
    - "readable_traits() excludes is: prefix (was class:) and excludes dashboard: from display output"
    - "review-substance output shows all 6 namespace groups as section headings"
    - "review-substance output shows dashboard: namespace entries with descriptions from traits.yaml"
    - "uv run pytest exits 0 after changes"
---

# Plan 08-04: Stage 2c — review-substance Audit (DT-13)

## Context

Stage 2 plan — independent, depends only on Stage 1 (08-01) being committed. Updates readable_traits() filter and review-substance display for the grouped model.

## Tasks

<task id="08-04-01">
  <title>DT-13a: Refactor readable_traits() in planner/cards/traits.py for grouped model</title>
  <action>
    In planner/cards/traits.py, update readable_traits() (lines 141–150):

    Current filter logic:
    - Skips "risk:manual_review"
    - Skips any trait_id starting with "class:"

    New filter logic:
    - Keep skip for "risk:manual_review"
    - Change "startswith('class:')" to "startswith('is:')" — is: replaces class: for intrinsic category markers
    - Add skip for "startswith('dashboard:')" — dashboard membership tags are review-axes, not scheduling narrative; they should not appear in schedule.yaml plan narrative (review_tags field)
    - Add code comment explaining the policy: "# is: and dashboard: are review-classification axes, not scheduling drivers — exclude from plan narrative"

    Note: readable_traits() receives a flat set[str] of prefixed trait IDs. After the Stage 1 refactor, the flat set comes from effective_stack_item_traits() (already fixed in 08-01-07 to reconstruct from per-namespace Substance fields). planner/engine/plan.py delegates to effective_stack_item_traits() and does NOT reference substance.traits directly — no changes to plan.py are needed in this task.
  </action>
  <read_first>
    - planner/cards/traits.py — read readable_traits() implementation (lines 141–150) and all callers
    - planner/engine/plan.py — search for "readable_traits" call sites to understand how trait_ids is built
    - planner/engine/review.py — search for "readable_traits" call sites
  </read_first>
  <acceptance_criteria>
    - readable_traits() skips trait_ids starting with "is:" (not "class:")
    - readable_traits() skips trait_ids starting with "dashboard:"
    - Code comment "review-classification axes" or similar is present explaining the exclusion policy
    - python3 -c "from planner.cards.traits import readable_traits; result=readable_traits({'is:antioxidant','intake:food_preferred','dashboard:sleep_recovery','risk:manual_review'}, {}); assert 'is:antioxidant' not in result; assert 'dashboard:sleep_recovery' not in result; print('OK')" exits 0
    - uv run pytest exits 0
  </acceptance_criteria>
</task>

<task id="08-04-02">
  <title>DT-13b: Audit review-substance output for all namespace groups</title>
  <action>
    Review planner/engine/review.py (or wherever review-substance command is implemented):

    1. Verify that the output of "uv run python -m planner review-substance data/substances/<card>.yaml" shows all 6 namespace groups (is:, intake:, effect:, risk:, activity:, dashboard:) as section headings, even when a substance has no entries for a namespace.
    2. Verify that unknown slugs (slugs not in traits.yaml for that namespace) surface in an "unknown" section or with clear indication, so typos are visible.
    3. Verify that print_trait_details() is called for dashboard: namespace entries — the TraitDef for a dashboard: slug should show the label and description from traits.yaml (no effects rendering needed since dashboard: traits have no effects).
    4. If any namespace is silently filtered or hidden: remove that filter so all 6 namespaces appear.

    The existing filter at grouped_trait_defs() in traits.py groups by namespace — verify that "is" and "dashboard" namespaces are included in the output (they were not present before Stage 1, but after DT-03 they exist in traits.yaml and should appear).
  </action>
  <read_first>
    - planner/engine/review.py — read full implementation of the review-substance command
    - planner/cards/traits.py — read grouped_trait_defs() and print_trait_details() implementations
  </read_first>
  <acceptance_criteria>
    - uv run python -m planner review-substance data/substances/alpha_gpc__sub_tzg5glskrd.yaml shows "is" namespace section
    - The review output for a substance with dashboard: tags shows "dashboard" namespace section
    - print_trait_details() is called for dashboard: TraitDef entries (shows description from traits.yaml)
    - A substance with an unknown slug under is: (not in traits.yaml) appears in "unknown" section or equivalent warning
    - uv run pytest exits 0
  </acceptance_criteria>
</task>

## Verification

```
uv run python -m planner review-substance data/substances/alpha_gpc__sub_tzg5glskrd.yaml
# Must show is: namespace section

uv run pytest
# Must pass
```
