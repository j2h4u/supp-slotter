---
id: 08-03
wave: 3
depends_on:
  - 08-01
  - 08-05
files_modified:
  - SKILL.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - DT-12
must_haves:
  truths:
    - "SKILL.md Add Or Enrich A Substance workflow does not contain hardcoded class-marker enumeration"
    - "SKILL.md Add Or Update A Dashboard workflow describes from_traits: not taking:"
    - "SKILL.md Minimal YAML Shapes section shows grouped substance card form and from_traits dashboard form"
    - "SKILL.md contains Which namespace? decision block with explicit guidance on is: vs dashboard: and a What NOT to put in dashboard: subsection"
    - "SKILL.md contains Membership Flow subsection as textual decision tree referencing the canonical OR-across-namespaces resolution rule"
    - "SKILL.md contains Doctor Warning Playbook subsection with WHEN to run doctor and per-class resolution — written against actual DT-14 message wording (Plan 08-05 is committed first)"
    - "SKILL.md Validation Contract section enumerates reference-integrity errors AND advisory doctor lifecycle warning classes — clearly distinguished as hard errors (check) vs advisories (doctor)"
    - "grep -nE '^class:' SKILL.md returns no stale class:* trait enumeration lines AND grep does not show 'Recognised class markers:' enumeration"
    - "grep -nE '^\\\\s*taking:' SKILL.md returns no dashboard YAML examples using taking:"
---

# Plan 08-03: Stage 2b — SKILL.md Agent Entrypoint (DT-12)

## Context

SKILL.md is the primary agent entrypoint (~403 lines). DT-12 requires multiple targeted updates across several sections plus two new subsections (Membership Flow, Doctor Warning Playbook). This plan must not touch docs/private/ files (historical session snapshots — out of scope).

**Wave 3 (depends on both 08-01 and 08-05):** Per cross-AI review consensus, the Doctor Warning Playbook in DT-12f must be written against the actual DT-14 warning message wording. To eliminate drift risk, 08-03 depends on 08-05 (which commits DT-14 warnings first) and reads the implemented `planner/engine/doctor.py` warning text verbatim. This is why 08-03 moves to wave 3 — it sequences after 08-05.

## Tasks

<task id="08-03-01">
  <title>DT-12a: Update File Tree section and Add Or Enrich A Substance workflow</title>
  <action>
    In SKILL.md:

    1. File Tree section (~lines 21–43): Find the data/dashboards/ description line. Add: "membership-via-tags — substances carry dashboard: tags; cluster yaml specifies from_traits: membership rule. Canonical serialization: grouped at rest, grouped in queries."

    2. Add Or Enrich A Substance workflow (~lines 113–129):
       a. Line 116: Update the description of review-substance output. Replace "The registry is grouped by namespace (intake, effect, class, risk, activity); substance cards still reference traits as namespace:name" with "The registry is grouped by namespace (is, intake, effect, risk, activity, dashboard); substance cards use the same grouped namespace keys directly."
       b. Line 117: Replace "traits: []" in the initial card creation instruction with a note that new cards have no namespace keys until populated.
       c. Line 121: Remove the hardcoded class-marker enumeration entirely. The current text reads: "Add only traits that affect current slot timing or single-substance warnings, or class:* markers for intrinsic pharmacological category. Recognised class markers: fat_soluble, mineral, electrolyte, adaptogen, antioxidant, ergogenic, nootropic, omega3." Replace with: "Add only traits that affect current slot timing, single-substance warnings, or intrinsic category classification. See data/traits.yaml for the full namespace registry. Run uv run python -m planner review-substance data/substances/<card>.yaml to inspect a card's current tags grouped by namespace before adding or changing tags."

    3. After the updated step 7 on line 121, add a "Which namespace?" decision block:
       "Which namespace?
       - Use is: when the property is true regardless of stack goals (intrinsic biochemical category). Polyhierarchical; review-classification only — does not influence slot scoring.
       - Use intake: when the substance has a food-state preference (food_required, empty_preferred, etc.). Max 1 entry per substance.
       - Use effect: when the substance has a slot timing effect (energy_like, sleep_disruptive, sleep_support). Drives slot scoring.
       - Use risk: when the substance carries a warning marker. Drives warning emission.
       - Use activity: when the substance has a workout timing marker (pre_workout, post_workout, any_workout). Max 1 entry per substance.
       - Use dashboard: when a curator decided this substance belongs in a named cluster. Polyhierarchical; review-classification only — does not influence slot scoring.
       - Leave unencoded if none apply.

       What NOT to put in dashboard:
       - Do NOT use dashboard: for scheduling-affecting traits. Those go under intake:, effect:, risk:, or activity:.
       - Do NOT use dashboard: as a synonym for is:. is: is for intrinsic biochemical category (open-world); dashboard: is for operator-curated cluster membership (closed-world)."
  </action>
  <read_first>
    - SKILL.md — read lines 21–43 (File Tree) and lines 113–129 (Add Or Enrich A Substance) in full
  </read_first>
  <acceptance_criteria>
    - SKILL.md File Tree data/dashboards/ description mentions "membership-via-tags" and "from_traits"
    - SKILL.md does NOT contain "Recognised class markers: fat_soluble, mineral" or any hardcoded class marker enumeration (verified: grep -n "Recognised class markers" SKILL.md returns no output)
    - SKILL.md contains "See data/traits.yaml for the full namespace registry"
    - SKILL.md contains "Which namespace?" decision block with all 6 namespace branches
    - SKILL.md contains "What NOT to put in dashboard:" with the guard against using dashboard: for scheduling traits
    - grep -nE '^class:' SKILL.md returns no class:* enumeration lines (CSS-like class= or prose mentions acceptable)
  </acceptance_criteria>
</task>

<task id="08-03-02">
  <title>DT-12b: Rewrite Add Or Update A Dashboard workflow with explicit bootstrap and validation command list</title>
  <action>
    In SKILL.md, find the "Add Or Update A Dashboard" section (~lines 139–143). Rewrite it completely:

    New content:
    "### Add Or Update A Dashboard

    Dashboard clusters use grouped from_traits: membership rules — substances carry the tags, dashboards declare the rule.

    Bootstrap sequence for a new operator-curated cluster:
    1. Register the dashboard slug in data/traits.yaml under the dashboard: namespace (add label and description). This must happen BEFORE tagging substance cards — the check command validates that dashboard: slugs in substance cards exist in traits.yaml.
    2. Create data/dashboards/<slug>.yaml with name, description, benefit/risk, and from_traits: { dashboard: [<slug>] }.
    3. For each member substance, open its card and add <slug> to the dashboard: list.
    4. Run uv run python -m planner check to validate reference integrity (hard FK errors).
    5. Run uv run python -m planner plan to regenerate schedule.yaml.
    6. Run uv run python -m planner doctor to check for advisory lifecycle warnings.
    7. Run uv run pytest to confirm tests still pass.

    When to use is: projection vs dashboard: tag:
    - Use from_traits: { is: [<class_slug>] } when membership is defined by an intrinsic biochemical category (e.g. all antioxidants). The cluster grows automatically as new substances acquire that class — intensional / open-world.
    - Use from_traits: { dashboard: [<slug>] } when membership is curated by the operator (e.g. a specific therapeutic cluster). Only explicitly tagged substances are members — extensional / closed-world.
    - Mix both in one from_traits: object when appropriate. Resolution is union (logical OR) across all listed (namespace, slug) pairs — there is NO AND across namespace groups.

    A single cluster may have both benefit and risk sections. Do not split one member set into two files."
  </action>
  <read_first>
    - SKILL.md — read lines 139–143 (Add Or Update A Dashboard section) in full
    - .planning/notes/ontology-dashboard-trait-namespace.md — read "Membership flow" section for accurate bootstrap description
    - .planning/phases/08-grouped-trait-shape-dashboard-membership-via-tags/08-01-PLAN.md — read the canonical from_traits resolution semantics for consistent wording
  </read_first>
  <acceptance_criteria>
    - SKILL.md "Add Or Update A Dashboard" section contains "from_traits:" not "taking:"
    - SKILL.md section contains "Register the dashboard slug in data/traits.yaml" as the first bootstrap step
    - SKILL.md section explains when to use is: projection vs dashboard: tag with intensional/extensional language
    - SKILL.md section enumerates validation commands: check, plan, doctor, pytest
    - SKILL.md section contains the canonical resolution rule with "union (logical OR)"
    - grep -nE '^\s*taking:' SKILL.md returns no output (no stale dashboard taking: YAML keys; from_traits: not matched)
    - grep -n "sort taking alphabetically" SKILL.md returns no output (that rule is gone)
  </acceptance_criteria>
</task>

<task id="08-03-03">
  <title>DT-12c: Update Minimal YAML Shapes section with grouped substance and from_traits dashboard examples</title>
  <action>
    In SKILL.md, find the "Minimal YAML Shapes" section (~lines 145–198). Update:

    1. Substance card example: replace "traits: []" with grouped form showing each namespace as optional:
       ```yaml
       # substance card — namespace keys are optional; omit any that don't apply
       name: Example Substance
       form: optional concrete form
       aliases:
       - EX
       is:
       - antioxidant
       intake:
       - food_preferred
       notes: Short universal substance note.
       ```

    2. Dashboard example: replace "taking: ..." with from_traits form. Show both projection styles:
       ```yaml
       # dashboard — operator-curated cluster (extensional membership)
       name: Example Dashboard
       description: Why this cluster exists.
       benefit:
         description: What useful coverage this cluster represents.
       from_traits:
         dashboard:
         - example_dashboard
       ```
       ```yaml
       # dashboard — class-projection cluster (intensional membership)
       name: Antioxidant Protection
       description: All antioxidant substances.
       benefit:
         description: Antioxidant coverage.
       from_traits:
         is:
         - antioxidant
       ```

    Keep other YAML shapes (product, stacks, relation) unchanged.
  </action>
  <read_first>
    - SKILL.md — read lines 145–198 (Minimal YAML Shapes section) in full
  </read_first>
  <acceptance_criteria>
    - SKILL.md substance card example does not contain "traits: []" or "traits: [namespace:slug]"
    - SKILL.md dashboard example contains "from_traits:" not "taking:"
    - SKILL.md shows both dashboard: (extensional) and is: (intensional) projection examples
    - grep -nE '^\s*traits: \[\]' SKILL.md returns no output in the substance example context
  </acceptance_criteria>
</task>

<task id="08-03-04">
  <title>DT-12d: Update Validation Contract section with reference-integrity errors AND doctor lifecycle warnings — distinguished as hard errors vs advisories</title>
  <action>
    In SKILL.md, find the "Validation Contract" section (~lines 200–207). Add to the existing validation guidance:

    After the existing data-only/schedule-affecting/planner-changed validation paths, add:

    "Reference-integrity errors (hard — from planner check, exit non-zero):
    - Unknown trait '{slug}' under namespace '{namespace}:' in substances/<file>.yaml — the slug is not registered in data/traits.yaml under that namespace. Fix: add the trait definition to traits.yaml under the correct namespace before using it.
    - Unknown trait '{slug}' under namespace '{namespace}:' in from_traits of dashboards/<file>.yaml — the slug is not registered in data/traits.yaml. Fix: register in traits.yaml first, or correct the slug.

    Advisory lifecycle warnings (soft — from planner doctor, exit 0):
    - dashboard.orphan_registration — trait registered in traits.yaml but no substance carries it.
    - dashboard.unused_trait — substance cards carry the tag but no dashboard yaml references it.
    - dashboard.slug_mismatch — dashboard yaml exists without matching trait, or trait exists without yaml.
    - dashboard.empty_cluster — dashboard from_traits resolves to zero member substances.

    Hard errors (check) block all downstream commands. Advisory warnings (doctor) report state for operator attention but do not block."
  </action>
  <read_first>
    - SKILL.md — read lines 200–215 (Validation Contract section) in full
    - planner/engine/doctor.py — read the actual implemented DT-14 warning class keys after Plan 08-05 commits, to ensure the names in SKILL.md match the implementation verbatim
  </read_first>
  <acceptance_criteria>
    - SKILL.md Validation Contract section mentions "Reference-integrity errors (hard"
    - SKILL.md Validation Contract section mentions "Advisory lifecycle warnings (soft"
    - SKILL.md section names all four DT-14 warning class keys: dashboard.orphan_registration, dashboard.unused_trait, dashboard.slug_mismatch, dashboard.empty_cluster (names match the actual keys returned by collect_orphans() after Plan 08-05)
    - SKILL.md section contains example error message with "register it in data/traits.yaml"
  </acceptance_criteria>
</task>

<task id="08-03-05">
  <title>DT-12e: Add Membership Flow subsection (textual decision tree, NOT ASCII art) — references canonical resolution rule</title>
  <action>
    In SKILL.md, add a new "### Membership Flow" subsection after the Validation Contract section. Content must be a textual decision tree (NOT ASCII diagram — agents paraphrase visuals inconsistently across sessions):

    "### Membership Flow

    Canonical from_traits resolution rule: a substance is a member of a dashboard if ANY (namespace, slug) pair in the dashboard's from_traits object also appears in the substance's corresponding per-namespace field. Resolution is union (logical OR) across the entire from_traits object — NO AND semantic across namespace groups.

    To determine which substances are in a dashboard cluster:
    1. Read the cluster's from_traits object to get all (namespace, slug) pairs.
    2. For each substance card in data/substances/, check each namespace field against the cluster's pairs.
    3. A substance is a cluster member if any of its namespace entries matches any (namespace, slug) pair from from_traits.
    4. The full member set is the union of all matching substances.

    To determine which clusters a substance belongs to:
    1. Read the substance's dashboard: list (if present) — each entry is a cluster slug that uses extensional projection.
    2. Also read the substance's is: list — any cluster with from_traits: { is: [<class_slug>] } matching the substance's class is also a cluster for this substance (intensional projection).
    3. Run uv run python -m planner review-substance data/substances/<card>.yaml to see the computed membership for a specific card.

    To add a substance to a cluster:
    1. For an extensional (operator-curated) cluster: add the cluster slug to the substance card's dashboard: list.
    2. For an intensional (class-projection) cluster: ensure the substance's is: list contains the class slug that the cluster projects from."
  </action>
  <read_first>
    - SKILL.md — read lines 200–230 to find the correct insertion point after Validation Contract
    - .planning/notes/ontology-dashboard-trait-namespace.md — read "Membership flow" section for accurate content
    - .planning/phases/08-grouped-trait-shape-dashboard-membership-via-tags/08-01-PLAN.md — verify canonical resolution rule wording matches contracts.py docstring
  </read_first>
  <acceptance_criteria>
    - SKILL.md contains "### Membership Flow" section
    - Section contains the canonical resolution rule with "union (logical OR)" and "NO AND semantic"
    - Membership Flow section describes the resolution algorithm in prose (not ASCII)
    - Section explains both directions: "which substances are in cluster" and "which clusters contain this substance"
    - grep -n "Membership Flow" SKILL.md returns exactly one heading match
  </acceptance_criteria>
</task>

<task id="08-03-06">
  <title>DT-12f: Add Doctor Warning Playbook subsection — written against actual DT-14 implementation (Plan 08-05 commits FIRST per wave dependency)</title>
  <action>
    Pre-condition: Plan 08-05 (DT-14 doctor lifecycle warnings) must be committed. This plan is in wave 3, depending on both 08-01 (Stage 1) AND 08-05 (DT-14). Read planner/engine/doctor.py directly to get the exact warning message wording — copy the wording verbatim into SKILL.md.

    In SKILL.md, add a new "### Doctor Warning Playbook" subsection after the Membership Flow section.

    Content (the warning class descriptions below MUST match the actual implemented message text from planner/engine/doctor.py — read and copy verbatim, not paraphrase):

    "### Doctor Warning Playbook

    WHEN to run uv run python -m planner doctor:
    - After any substance card edit (traits, dashboard: tags, is: tags)
    - After any dashboard yaml edit (from_traits changes, new cluster created)
    - After any data/traits.yaml change (new namespace entry, renamed slug)
    - Once at end of session before commit

    Note: doctor produces ADVISORY warnings (soft — exit 0). For HARD reference-integrity errors that block commits, use planner check.

    Per-warning-class resolution:

    dashboard.orphan_registration: <copy verbatim message format from doctor.py>
    Causes: (A) trait registered for a planned cluster but substance tagging not yet done; (B) cluster abandoned but trait entry not removed.
    Resolution: (A) Tag the relevant substance cards with dashboard: <slug>. (B) Remove the trait entry from traits.yaml and the dashboard yaml file if it exists.

    dashboard.unused_trait: <copy verbatim message format from doctor.py>
    Causes: (A) dashboard yaml deleted while tags remained on substance cards; (B) dashboard yaml not yet created.
    Resolution: (A) Create data/dashboards/<slug>.yaml referencing from_traits: { dashboard: [<slug>] }, OR remove the tag from substance cards and the entry from traits.yaml. (B) Create the dashboard yaml.

    dashboard.slug_mismatch: <copy verbatim message format from doctor.py>
    Precedence: when a slug fires both orphan_registration AND slug_mismatch, only slug_mismatch surfaces — fix the yaml/trait pairing first, then re-run doctor.
    Canonical fix: (A) If yaml exists but trait missing — add the trait to traits.yaml under dashboard:. (B) If trait exists but yaml missing — create the dashboard yaml, or remove the trait entry.

    dashboard.empty_cluster: <copy verbatim message format from doctor.py>
    Causes: all tagged substances were removed; or from_traits slugs do not match any substance's namespace fields under the canonical OR-across-namespaces resolution rule.
    Resolution: Tag substance cards with dashboard: <slug>, OR remove the dashboard yaml if the cluster is abandoned. If the cluster is an intentional placeholder for future use, add a notes: field explaining the intent."
  </action>
  <read_first>
    - SKILL.md — read lines 220–260 to find the correct insertion point after Membership Flow
    - planner/engine/doctor.py — read check_dashboard_lifecycle() implementation AFTER Plan 08-05 is committed; copy the exact warning message wording verbatim into SKILL.md (no paraphrasing)
    - .planning/phases/08-grouped-trait-shape-dashboard-membership-via-tags/08-05-PLAN.md — confirm wave 3 dependency is satisfied (08-05 committed before 08-03 starts)
  </read_first>
  <acceptance_criteria>
    - SKILL.md contains "### Doctor Warning Playbook" section
    - Section contains "WHEN to run" list with four trigger conditions
    - Section contains all four warning class descriptions: orphan_registration, unused_trait, slug_mismatch, empty_cluster
    - Section explicitly notes the precedence rule (slug_mismatch wins over orphan_registration when both fire)
    - Warning message text in SKILL.md matches the actual implementation in planner/engine/doctor.py (cross-check by grep — at least one verbatim message phrase from doctor.py appears in SKILL.md)
    - Each warning class has causes and resolution options
    - grep -n "Doctor Warning Playbook" SKILL.md returns exactly one heading match
  </acceptance_criteria>
</task>

<task id="08-03-07">
  <title>DT-12g: Update .planning/PROJECT.md namespace list</title>
  <action>
    In .planning/PROJECT.md, find the namespace list (around line 17, but verify location by grep). Update to reflect the current namespace set: is:, intake:, effect:, risk:, activity:, dashboard: — replacing any reference to class: namespace.
  </action>
  <read_first>
    - .planning/PROJECT.md — read lines 10–25 to find the namespace list (or grep for "class:" / "namespace" to locate)
  </read_first>
  <acceptance_criteria>
    - .planning/PROJECT.md namespace list contains "is:" and "dashboard:" and does NOT contain "class:"
    - grep -nE '^class:' .planning/PROJECT.md returns no namespace-list lines
  </acceptance_criteria>
</task>

## Verification

```
grep -nE '^class:' SKILL.md                # must return no stale class:* trait enumeration
grep -nE '^\s*taking:' SKILL.md            # must return no dashboard workflow lines with taking: YAML key
grep -nE '^\s*traits: \[\]' SKILL.md       # must return no substance card example lines
grep -n "Membership Flow" SKILL.md         # exactly one heading
grep -n "Doctor Warning Playbook" SKILL.md # exactly one heading
grep -n "union (logical OR)" SKILL.md      # at least one match (canonical resolution rule)
grep -n "Recognised class markers" SKILL.md # no output (hardcoded enumeration removed)
```

## How this plan addresses cross-AI review concerns

- **DT-12f / DT-14 sequencing (consensus concern):** This plan is in wave 3 with explicit `depends_on: [08-01, 08-05]`. Plan 08-05 (DT-14 doctor warnings) commits first; this plan reads `planner/engine/doctor.py` for the actual implemented warning message text and copies it verbatim into SKILL.md. No drift between planned message wording and SKILL.md description.
- **"What NOT to put in dashboard:" (OpenCode suggestion):** Added as a subsection of the Which namespace? decision block. Guards against agents adding scheduling traits to the dashboard: namespace.
- **Canonical from_traits resolution rule:** Stated verbatim in Membership Flow section, matching planner/contracts.py docstring (Stage 1) and docs/ (Plan 08-02). One source of truth.
- **Validation command list (Codex suggestion):** Added to "Add Or Update A Dashboard" workflow — explicit list of check, plan, doctor, pytest.
- **Hard errors vs advisory warnings boundary:** Validation Contract section distinguishes check (hard, blocks) vs doctor (advisory, soft). Doctor Warning Playbook reiterates this.
- **Grep hygiene:** Acceptance criteria use anchored regex `^\s*taking:` not bare `taking:` (avoids false positives on `from_traits:`).
