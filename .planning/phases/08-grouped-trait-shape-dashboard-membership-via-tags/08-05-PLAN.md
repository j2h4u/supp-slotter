---
id: 08-05
wave: 2
depends_on:
  - 08-01
files_modified:
  - planner/engine/doctor.py
  - tests/test_phase_03.py
autonomous: true
requirements:
  - DT-14
must_haves:
  truths:
    - "uv run python -m planner doctor emits dashboard.orphan_registration warning when a dashboard: slug exists in traits.yaml but no substance carries it"
    - "uv run python -m planner doctor emits dashboard.unused_trait warning when substance cards carry a dashboard: tag but no dashboard yaml references it via from_traits"
    - "uv run python -m planner doctor emits dashboard.slug_mismatch warning when a dashboards/*.yaml exists without a matching dashboard: trait in traits.yaml or vice versa"
    - "uv run python -m planner doctor emits dashboard.empty_cluster warning when a dashboard's from_traits resolves to zero member substances using the canonical OR-across-namespaces resolution rule"
    - "Each warning message contains actionable resolution guidance text"
    - "uv run python -m planner doctor exits 0 on the post-Stage-1 clean repo (all four warning classes report zero entries)"
    - "uv run pytest exits 0 after changes — existing doctor tests in tests/test_phase_03.py updated if section keys/ordering changed"
    - "check-vs-doctor boundary documented in doctor.py: planner check fails on invalid references (hard errors); planner doctor warns on valid-but-suspicious lifecycle states (advisory)"
    - "Empty-cluster resolution reuses the same from_traits resolver helpers from cards/dashboards.py — no parallel re-implementation"
---

# Plan 08-05: Stage 2d — Doctor Lifecycle Warnings (DT-14)

## Context

Stage 2 plan — independent, depends only on Stage 1 (08-01). Adds four new lifecycle warning classes to doctor.py. DT-14 is required (not optional): the primary consumer is agentic sessions with no persistent memory. Warnings must include actionable resolution text.

**check-vs-doctor boundary (per cross-AI review concern):**
- `planner check` = hard reference-integrity errors on data that cannot be loaded or that violates the file-based FK constraint (slug in substance card not registered in traits.yaml; slug in dashboard from_traits not registered in traits.yaml). Exit non-zero.
- `planner doctor` = advisory warnings on valid-but-suspicious lifecycle states (orphan trait registrations, unused tags, dashboard/trait slug pairing inconsistency, empty clusters). Exit 0 unless explicitly configured otherwise.

The four DT-14 warning classes are advisory by design — they fire on states that pass `check` but indicate operator hygiene drift.

**from_traits resolution semantics (canonical — reused from Stage 1):**
The empty-cluster check (08-05-01 below) MUST use the same helper functions added in Stage 1 task 08-01-06 (`_from_traits_pairs()` and `_substance_carries()` in `planner/cards/dashboards.py`). No parallel re-implementation. The canonical rule is: union (logical OR) across the entire from_traits object — a substance is a member if ANY (namespace, slug) pair matches. NO AND across namespaces.

## Tasks

<task id="08-05-01">
  <title>DT-14: Implement four dashboard lifecycle warning classes in planner/engine/doctor.py — reusing Stage 1 from_traits resolver helpers</title>
  <action>
    In planner/engine/doctor.py, add four new lifecycle checks to collect_orphans(). These checks scan for mismatches between: traits.yaml dashboard: namespace entries, substance card dashboard: tags, and dashboard yaml from_traits references.

    Add a new helper function check_dashboard_lifecycle(trait_defs, substances, dashboard_files) -> dict[str, list[str]] that performs the four checks and returns warning messages keyed by warning class. Call it from collect_orphans() and merge its results into the returned sections dict.

    At the top of check_dashboard_lifecycle(), add a docstring stating the check-vs-doctor boundary:
    """
    Advisory lifecycle warnings for dashboard tag/yaml/trait state. These complement
    (do not replace) the hard reference-integrity errors in planner check:
      - planner check fails on slugs not registered in traits.yaml (hard FK constraint)
      - planner doctor warns on valid-but-suspicious lifecycle states (this function)
    """

    The four warning classes and their detection logic:

    **dashboard.orphan_registration** — trait registered but no substance carries it:
    - Collect all dashboard: slug names defined in trait_defs (namespace == "dashboard")
    - Collect all dashboard: slugs actually carried by substance cards (union of substance.dashboard tuples)
    - Registered but not carried = orphan_registration candidates
    - Message format: "Orphan registration: dashboard:{slug} defined in data/traits.yaml but no substance card has it under its dashboard: group. Likely cause: trait registered for a planned cluster but substance tagging not yet done. Resolution: tag relevant substance cards under dashboard:, OR remove the trait entry from data/traits.yaml if the cluster is abandoned."

    **dashboard.unused_trait** — substances carry it but no dashboard yaml references it:
    - Collect all dashboard: slugs carried by substance cards (union of substance.dashboard tuples)
    - Collect all dashboard: slugs referenced in dashboard yaml from_traits (dashboard namespace only — use the same helper _from_traits_pairs from Stage 1's cards/dashboards.py for consistency)
    - Carried but not referenced = unused_trait candidates
    - Message format: "Unused trait: dashboard:{slug} is carried by {N} substance card(s) but no dashboard yaml references it via from_traits. Likely cause: dashboard yaml deleted while tags remained, OR yaml not yet created. Resolution: create data/dashboards/{slug}.yaml referencing it via from_traits: {{ dashboard: [{slug}] }}, OR remove the tag from substance cards and the entry from data/traits.yaml."

    **dashboard.slug_mismatch** — yaml file exists without matching trait, or trait exists without yaml:
    - Collect dashboard yaml stems: set of stems from DASHBOARDS_DIR.glob("*.yaml")
    - Collect dashboard trait slugs: set of short_name values where namespace == "dashboard" in trait_defs
    - yaml_stem not in trait_slugs → "yaml without trait" mismatch
    - trait_slug not in yaml_stems → "trait without yaml" mismatch
    - Precedence rule (per cross-AI review): if a slug fires both orphan_registration AND slug_mismatch (trait without yaml AND no substance carries it), emit ONLY slug_mismatch — the yaml-creation step is the prerequisite. orphan_registration fires only when the yaml exists.
    - Message format (yaml without trait): "Slug mismatch: data/dashboards/{slug}.yaml exists but dashboard:{slug} is not registered in data/traits.yaml. Fix: add dashboard:{slug} entry to data/traits.yaml (with label and description)."
    - Message format (trait without yaml): "Slug mismatch: dashboard:{slug} is registered in data/traits.yaml but data/dashboards/{slug}.yaml does not exist. Fix: create data/dashboards/{slug}.yaml referencing from_traits: {{ dashboard: [{slug}] }}, or remove the trait entry from data/traits.yaml."

    **dashboard.empty_cluster** — from_traits resolves to zero member substances (using canonical OR-across-namespaces resolution):
    - For each dashboard yaml, resolve membership using the SAME helper functions added in Stage 1 task 08-01-06 (_from_traits_pairs and _substance_carries from cards/dashboards.py) — do NOT re-implement the resolution logic. Import the helpers.
    - Clusters resolving to 0 members (no substance matches any from_traits pair) = empty_cluster candidates
    - Message format: "Empty cluster: data/dashboards/{slug}.yaml from_traits resolves to zero member substances (using union resolution: OR across all listed (namespace, slug) pairs). Resolution: tag substances under dashboard: {slug}, OR remove the dashboard yaml if abandoned. (If this is an intentional placeholder, add a notes: field explaining the intent.)"

    Add these four sections to collect_orphans() return dict with keys:
    "dashboard.orphan_registration", "dashboard.unused_trait", "dashboard.slug_mismatch", "dashboard.empty_cluster"

    Verify existing collect_orphans() callers — cmd_doctor() in particular — handle the new section keys correctly (most likely a generic iteration over the returned dict — no changes needed; verify by reading cmd_doctor()).
  </action>
  <read_first>
    - planner/engine/doctor.py — read full collect_orphans() and cmd_doctor() implementations
    - planner/contracts.py — verify Substance.dashboard field name after Stage 1
    - planner/cards/traits.py — read load_traits() and TraitDef.namespace/short_name fields
    - planner/io.py — read DASHBOARDS_DIR constant
    - planner/cards/dashboards.py — read _from_traits_pairs and _substance_carries helpers from Stage 1; import these rather than re-implementing
    - tests/test_phase_03.py — read existing doctor tests to identify any that assert on collect_orphans() return dict keys/shape
  </read_first>
  <acceptance_criteria>
    - doctor.py has check_dashboard_lifecycle() function with check-vs-doctor boundary docstring
    - check_dashboard_lifecycle() imports _from_traits_pairs and _substance_carries from planner.cards.dashboards (verified by grep)
    - collect_orphans() returns dict includes keys "dashboard.orphan_registration", "dashboard.unused_trait", "dashboard.slug_mismatch", "dashboard.empty_cluster"
    - Synthetic test 1: adding a dashboard:test_orphan_xyz slug to traits.yaml with no substance carrying it → uv run python -m planner doctor output contains "Orphan registration" and "test_orphan_xyz"
    - Synthetic test 2: temporarily tagging a substance with dashboard: [test_unused_xyz] but no dashboard yaml → doctor output contains "Unused trait" and "test_unused_xyz"
    - Synthetic test 3: creating a dashboard yaml file test_yaml_only.yaml without a traits.yaml entry → doctor output contains "Slug mismatch" and "yaml exists but dashboard:"
    - Synthetic test 4: a dashboard whose from_traits: resolves to 0 substances using OR resolution → doctor output contains "Empty cluster" and "union resolution"
    - Each warning message contains "Resolution:" with actionable options
    - Precedence rule: a slug that is registered in traits.yaml without yaml AND not carried by any substance fires ONLY slug_mismatch (NOT also orphan_registration) — verified by synthetic test
    - uv run python -m planner doctor exits 0 on the unmodified post-Stage-1 repo state (all four sections report zero entries)
    - uv run pytest exits 0
  </acceptance_criteria>
</task>

<task id="08-05-02">
  <title>Update existing doctor tests in tests/test_phase_03.py to accept the new section keys (per cross-AI review consensus on test fixture scope)</title>
  <action>
    Per cross-AI review concern: existing doctor tests in tests/test_phase_03.py may assert on collect_orphans() return-dict keys, ordering, or counts. The new section keys (dashboard.orphan_registration, dashboard.unused_trait, dashboard.slug_mismatch, dashboard.empty_cluster) may break those assertions if they check exact key sets or section count.

    Audit and update:
    1. Read all tests in tests/test_phase_03.py that call doctor functions or assert on doctor output.
    2. For any test that asserts on exact section keys or section count: update to accommodate the four new keys. Acceptable patterns:
       - Use `expected_keys <= actual_keys` (subset) rather than equality
       - Explicitly add the new keys to expected sets where appropriate
       - Assert sections by name lookup rather than by index
    3. For any test that runs cmd_doctor() against the real data/ directory: ensure the test expects exit 0 (all four new sections are empty on the post-Stage-1 clean repo).
    4. Add at least one new test that exercises one of the warning classes with a temp-fixture dashboard yaml or trait entry (use tmp_path + a copied data/ snapshot, the same pattern as existing temp-fixture tests in the file).

    Also audit tests/test_phase_01.py, tests/test_phase_02.py for any doctor assertions that may need updating.
  </action>
  <read_first>
    - tests/test_phase_03.py — grep for "doctor" and "collect_orphans" to find all affected test functions
    - tests/test_phase_01.py — same audit
    - tests/test_phase_02.py — same audit
    - planner/engine/doctor.py — verify the post-08-05-01 collect_orphans() return shape
  </read_first>
  <acceptance_criteria>
    - All tests/test_phase_*.py tests that assert on doctor output sections updated to accommodate the four new keys (no exact-equality assertions on the section-key set that would fail when new keys are added)
    - At least one new test exercises a DT-14 warning class with a temp fixture
    - uv run pytest exits 0
  </acceptance_criteria>
</task>

## Verification

On the post-Stage-1 clean repo:
```
uv run python -m planner doctor
# Must exit 0
# Four sections all empty:
#   dashboard.orphan_registration: 0
#   dashboard.unused_trait: 0
#   dashboard.slug_mismatch: 0
#   dashboard.empty_cluster: 0

uv run pytest
# Must pass
```

To test warnings manually (temp fixtures only — do not commit):
1. Add a dashboard: slug to traits.yaml without tagging any substance → expect orphan_registration
2. Tag a substance with a new dashboard: slug without creating yaml → expect unused_trait + slug_mismatch (with precedence: only slug_mismatch surfaces if no substance carries it; both if substances do)
3. Create a dashboard yaml without a traits.yaml entry → expect slug_mismatch
4. Create a dashboard yaml whose from_traits slug matches zero substances → expect empty_cluster

## How this plan addresses cross-AI review concerns

- **check-vs-doctor boundary:** Documented in doctor.py via the check_dashboard_lifecycle() docstring. `check` = hard FK errors; `doctor` = advisory lifecycle warnings. Explicit precedence rule defined for orphan_registration vs slug_mismatch overlap.
- **from_traits semantics consistency:** Empty-cluster check imports the Stage 1 resolver helpers (_from_traits_pairs, _substance_carries) — no parallel re-implementation, single source of truth across loader, doctor, and tests.
- **Existing doctor test impact:** Task 08-05-02 explicitly audits and updates tests/test_phase_*.py for the new section keys.
