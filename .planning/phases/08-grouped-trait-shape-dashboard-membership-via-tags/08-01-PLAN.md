---
id: 08-01
wave: 1
depends_on: []
files_modified:
  - schema/substance.schema.json
  - schema/dashboard.schema.json
  - data/traits.yaml
  - planner/io.py
  - planner/contracts.py
  - planner/cards/substance.py
  - planner/cards/dashboards.py
  - planner/engine/_scheduling.py
  - planner/engine/doctor.py
  - planner/engine/check.py
  - data/substances/*.yaml
  - data/dashboards/*.yaml
  - tests/test_phase_01.py
  - tests/test_scheduling_units.py
  - tests/test_schemas.py
  - schedule.yaml
autonomous: true
requirements:
  - DT-01
  - DT-02
  - DT-03
  - DT-04
  - DT-05
  - DT-06
  - DT-07
  - DT-08
  - DT-09
  - DT-10
must_haves:
  truths:
    - "uv run python -m planner check exits 0 after full Stage 1 migration"
    - "uv run pytest exits 0 — all tests pass on migrated codebase"
    - "uv run python -m planner plan produces schedule.yaml with 13 dashboard entries in benefits/risks (vasodilation_no_pathway absent)"
    - "schema_errors() returns [] for a substance card using grouped keys (is:, intake:, effect:, risk:, activity:, dashboard:)"
    - "schema_errors() returns at least one error for a substance card using flat traits: [class:antioxidant] form"
    - "All 200 substance YAML files pass schema validation after migration"
    - "All 13 dashboard YAML files pass schema validation after migration"
    - "check_substances() emits prescriptive error for substance with unknown slug under any namespace"
    - "check_dashboards() emits prescriptive error for dashboard with from_traits slug not in traits.yaml"
---

# Plan 08-01: Stage 1 — Core Schema + Data + Contracts (Atomic Commit)

## Context

Stage 1 covers DT-01 through DT-10. All 10 tasks are correctness changes that must land in a **single atomic git commit** — intermediate states break `planner check`. Do NOT commit partial Stage 1 work. Run tests only after all tasks are complete.

**Mandatory implementation order (load-bearing):**
1. DT-01: substance schema
2. DT-05: dashboard schema
3. DT-03: traits.yaml rename + REGISTERED_NAMESPACES update
4. DT-07: Python contracts + loaders (must precede YAML migration)
5. DT-02: migrate all substance YAML cards
6. DT-04: add dashboard: tags to substance cards
7. DT-06: rewrite all dashboard YAML files, delete vasodilation_no_pathway
8. DT-08: add reference-integrity checks
9. DT-10: update and add tests
10. DT-09: regenerate schedule.yaml, verify output

## Tasks

<task id="08-01-01">
  <title>DT-01: Overhaul substance.schema.json — replace traits array with grouped namespace keys</title>
  <action>
    In schema/substance.schema.json:
    1. Remove "traits" from the "required" array — no namespace key is required (a substance card may omit any namespace).
    2. Remove the "traits" property definition from "properties".
    3. Add 6 new properties: "is", "intake", "effect", "risk", "activity", "dashboard" — each defined as:
       type: array, uniqueItems: true, items: {type: string, pattern: "^[a-z][a-z0-9_]*$"}
    4. For "intake" and "activity": add maxItems: 1.
    5. "is", "effect", "risk", "dashboard" have no maxItems (polyhierarchical).
    6. Keep "additionalProperties": false at the substance object level — this closes the key set.
    7. Keep "required": ["id", "name"] — only id and name remain required.
  </action>
  <read_first>
    - schema/substance.schema.json — read full current structure before editing
  </read_first>
  <acceptance_criteria>
    - schema/substance.schema.json "required" array contains exactly ["id", "name"]
    - schema/substance.schema.json "properties" has no "traits" key
    - schema/substance.schema.json "properties" has keys: "is", "intake", "effect", "risk", "activity", "dashboard"
    - "intake" and "activity" properties each have maxItems: 1
    - "additionalProperties": false remains present at top level
    - python3 -c "import json; s=json.load(open('schema/substance.schema.json')); assert 'traits' not in s['properties']; assert 'is' in s['properties']; assert s['properties']['intake']['maxItems']==1; print('OK')" exits 0
  </acceptance_criteria>
</task>

<task id="08-01-02">
  <title>DT-05: Overhaul dashboard.schema.json — replace taking[] with from_traits grouped object</title>
  <action>
    In schema/dashboard.schema.json:
    1. Remove "taking" from the "required" array.
    2. Remove the "taking" property definition from "properties".
    3. Add "from_traits" to "required".
    4. Add "from_traits" property: type object, additionalProperties: false, with optional properties "is", "intake", "effect", "risk", "activity", "dashboard" — each an array of strings with pattern "^[a-z][a-z0-9_]*$".
    5. Keep existing "anyOf": [{required: [benefit]}, {required: [risk]}] unchanged.
    6. Keep "additionalProperties": false at the dashboard object level.
    7. Keep "name", "description" in "required".
  </action>
  <read_first>
    - schema/dashboard.schema.json — read full current structure before editing
  </read_first>
  <acceptance_criteria>
    - schema/dashboard.schema.json "required" array contains "from_traits", "name", "description" and does NOT contain "taking"
    - schema/dashboard.schema.json "properties" has "from_traits" key and no "taking" key
    - "from_traits" has additionalProperties: false
    - python3 -c "import json; s=json.load(open('schema/dashboard.schema.json')); assert 'taking' not in s['properties']; assert 'from_traits' in s['properties']; assert 'from_traits' in s['required']; print('OK')" exits 0
  </acceptance_criteria>
</task>

<task id="08-01-03">
  <title>DT-03: Rename class→is in traits.yaml and update REGISTERED_NAMESPACES in planner/io.py</title>
  <action>
    In data/traits.yaml:
    1. Rename the top-level key "class:" to "is:" — all 8 entries (adaptogen, antioxidant, electrolyte, ergogenic, fat_soluble, mineral, nootropic, omega3) move under "is:" with their content unchanged.
    2. Add new top-level key "dashboard:" with one entry per operator-curated cluster (9 entries — all dashboards except the 4 being evaluated as pure-class, but per research ALL 13 surviving dashboards are operator-curated). Add these 13 entries under "dashboard:", each with label and description:
       - connective_tissue_support: label "Connective Tissue Support", description "Operator-curated cluster for collagen-synthesis and connective tissue review."
       - bleeding_load: label "Bleeding Load", description "Operator-curated cluster for anticoagulant and platelet-modulating substance review."
       - cholinergic_load: label "Cholinergic Load", description "Operator-curated cluster for cholinergic substance review."
       - cortisol_reduction: label "Cortisol Reduction", description "Operator-curated cluster for HPA-axis modulating and cortisol-lowering substance review."
       - electrolyte_hydration_support: label "Electrolyte / Hydration Support", description "Operator-curated cluster for electrolyte and hydration substance review."
       - mitochondrial_health: label "Mitochondrial Health", description "Operator-curated cluster for mitochondrial and energy-metabolism substance review."
       - neurocognitive_support: label "Neurocognitive Support", description "Operator-curated cluster for cognitive-support substance review."
       - serotonergic_load: label "Serotonergic Load", description "Operator-curated cluster for serotonergic substance review."
       - skin_barrier_support: label "Skin Barrier Support", description "Operator-curated cluster for skin-barrier and collagen substance review."
       - sleep_recovery: label "Sleep Recovery", description "Operator-curated cluster for sleep-support substance review."
       - vascular_health: label "Vascular Health", description "Operator-curated cluster for vascular-tone and blood-flow substance review."
       - antioxidant_protection: label "Antioxidant Protection", description "Operator-curated cluster for antioxidant and anti-inflammatory substance review."
       - workout_performance: label "Workout Performance", description "Operator-curated cluster for ergogenic and workout-performance substance review."

    In planner/io.py:
    1. In REGISTERED_NAMESPACES set (lines 29–36): replace "class" with "is", add "dashboard", remove "mechanism".
       Final set: {"intake", "effect", "is", "risk", "activity", "dashboard"}
  </action>
  <read_first>
    - data/traits.yaml — read full current structure to understand all 8 class: entries
    - planner/io.py — read lines 29–36 for REGISTERED_NAMESPACES current value
  </read_first>
  <acceptance_criteria>
    - data/traits.yaml has top-level "is:" key with 8 entries, no "class:" key
    - data/traits.yaml has top-level "dashboard:" key with 13 entries
    - python3 -c "import yaml; d=yaml.safe_load(open('data/traits.yaml').read()); assert 'is' in d; assert 'class' not in d; assert 'dashboard' in d; assert len(d['dashboard'])==13; print('OK')" exits 0
    - planner/io.py REGISTERED_NAMESPACES contains "is" and "dashboard", does NOT contain "class" or "mechanism"
    - grep -n "REGISTERED_NAMESPACES" planner/io.py shows {"intake", "effect", "is", "risk", "activity", "dashboard"}
  </acceptance_criteria>
</task>

<task id="08-01-04">
  <title>DT-07a: Update Substance and Dashboard dataclasses in planner/contracts.py</title>
  <action>
    In planner/contracts.py:
    1. Replace the Substance dataclass field "traits: tuple[str, ...]" with 6 per-namespace fields, all optional with default ():
       - is_: tuple[str, ...] = ()    (note underscore suffix — "is" is a Python keyword)
       - intake: tuple[str, ...] = ()
       - effect: tuple[str, ...] = ()
       - risk: tuple[str, ...] = ()
       - activity: tuple[str, ...] = ()
       - dashboard: tuple[str, ...] = ()
       Keep all other Substance fields (id, name, form, aliases, notes, concerns, prefer_with) unchanged.
    2. Remove the DashboardMember dataclass entirely (lines 71–76).
    3. Replace Dashboard.taking field: remove "taking: tuple[DashboardMember, ...]" and add "from_traits: dict[str, tuple[str, ...]]" with no default (required field, but empty dict {} is valid).
       New Dashboard dataclass: name, description, from_traits, benefit, risk, started.
  </action>
  <read_first>
    - planner/contracts.py — read full file to understand all dataclasses and their field order
  </read_first>
  <acceptance_criteria>
    - Substance dataclass has fields: id, name, is_, intake, effect, risk, activity, dashboard (plus form, aliases, notes, concerns, prefer_with)
    - Substance dataclass has NO "traits" field
    - DashboardMember class is absent from contracts.py
    - Dashboard dataclass has "from_traits: dict[str, tuple[str, ...]]" field, no "taking" field
    - python3 -c "from planner.contracts import Substance, Dashboard; s=Substance(id='x',name='y'); assert hasattr(s,'is_'); assert not hasattr(s,'traits'); print('OK')" exits 0
  </acceptance_criteria>
</task>

<task id="08-01-05">
  <title>DT-07b: Update substance loader in planner/cards/substance.py</title>
  <action>
    In planner/cards/substance.py:
    1. In load_substance(): replace "traits=tuple(data.get('traits') or ())" with 6 separate fields:
       is_=tuple(data.get("is") or ()),
       intake=tuple(data.get("intake") or ()),
       effect=tuple(data.get("effect") or ()),
       risk=tuple(data.get("risk") or ()),
       activity=tuple(data.get("activity") or ()),
       dashboard=tuple(data.get("dashboard") or ()),
    2. In check_substances() (around line 235–239): replace the flat traits validation loop with namespace-aware validation. For each namespace ("is", "intake", "effect", "risk", "activity", "dashboard"), read substance.get(namespace) or []; for each bare slug in that list, check if f"{namespace}:{slug}" is in trait_ids. If not found, append error: f"{sf}: Unknown trait '{slug}' under namespace '{namespace}:' — register it in data/traits.yaml under '{namespace}:' first (with label and description)."
    3. In the Substance(id=...) constructor call in check_substances() (line ~209–213): update to use is_=() instead of traits=().
    4. Update any other place in substance.py that references substance.traits to use the appropriate namespace field.
  </action>
  <read_first>
    - planner/cards/substance.py — read full file, focus on load_substance() and check_substances()
    - planner/contracts.py — verify new Substance field names after task 08-01-04
  </read_first>
  <acceptance_criteria>
    - load_substance() reads data.get("is"), data.get("intake"), etc. — no data.get("traits") remains
    - check_substances() validates each namespace group against trait_ids using f"{namespace}:{slug}" lookup
    - Error message format: "Unknown trait '{slug}' under namespace '{namespace}:' — register it in data/traits.yaml under '{namespace}:' first (with label and description)."
    - python3 -c "from planner.cards.substance import load_substance; print('import OK')" exits 0
  </acceptance_criteria>
</task>

<task id="08-01-06">
  <title>DT-07c: Update dashboard loader and resolver in planner/cards/dashboards.py</title>
  <action>
    In planner/cards/dashboards.py:
    1. Remove imports of DashboardMember from planner.contracts.
    2. Remove _build_dashboard_member() function entirely.
    3. In load_dashboard(): replace taking-member construction with from_traits reading:
       from_traits_raw = data.get("from_traits") or {}
       from_traits = {ns: tuple(slugs) for ns, slugs in from_traits_raw.items() if isinstance(slugs, list)}
       Pass from_traits=from_traits to Dashboard constructor.
    4. Replace collect_dashboard_substance_refs(): change to return empty set — after refactor, dashboards don't reference substances by ID. Function signature stays the same for minimal caller disruption. Body: return set()
    5. Rewrite build_dashboard_review() membership resolution: instead of iterating dashboard.taking members, scan all substances for each dashboard:
       For each dashboard file: load dashboard, get its from_traits dict.
       For each substance in substances dict: check if any (namespace, slug) pair from dashboard.from_traits matches substance's corresponding namespace field. A substance is a member if substance.<namespace_field> contains slug for any (namespace, slug) pair.
       Note: namespace "is" in from_traits maps to substance field "is_" (underscore suffix).
       Classify each member substance as covered (in active_substances), inactive (in inactive_substances), or missing (neither).
    6. Rewrite check_dashboards(): replace taking[] substance ref validation with from_traits slug ref validation. For each namespace key in dashboard's from_traits, for each bare slug, check f"{namespace}:{slug}" is in trait_ids (passed as parameter or derived from substances). Error format: "Unknown trait '{slug}' under namespace '{namespace}:' in from_traits — register it in data/traits.yaml under '{namespace}:' first."
       Note: check_dashboards() currently takes (dashboard_files, substance_ids, substances) — add trait_ids parameter or derive from substance_ids. The simplest approach: add a trait_ids: set[str] parameter and update the call site in check.py.
  </action>
  <read_first>
    - planner/cards/dashboards.py — read full file
    - planner/contracts.py — verify Dashboard.from_traits field shape after task 08-01-04
    - planner/engine/check.py — read check_dashboards() call site to update signature
    - planner/engine/doctor.py — read collect_dashboard_substance_refs() usage at line ~91
  </read_first>
  <acceptance_criteria>
    - _build_dashboard_member() function absent from dashboards.py
    - DashboardMember not imported in dashboards.py
    - load_dashboard() constructs Dashboard with from_traits= parameter, no taking= parameter
    - collect_dashboard_substance_refs() returns set() (empty set)
    - build_dashboard_review() resolves membership by scanning substance namespace fields against dashboard from_traits — not by iterating taking members
    - check_dashboards() validates from_traits slugs against trait_ids, not substance_ids
    - python3 -c "from planner.cards.dashboards import load_dashboard, build_dashboard_review; print('import OK')" exits 0
  </acceptance_criteria>
</task>

<task id="08-01-07">
  <title>DT-07d: Update effective_stack_item_traits() in planner/engine/_scheduling.py and trait refs in doctor.py</title>
  <action>
    In planner/engine/_scheduling.py, in effective_stack_item_traits() (line 44–56):
    Replace "for trait_id in substance.traits" with iteration over the 5 scheduling-relevant namespaces ONLY (exclude dashboard — it has no slot effects):
    For each component substance, construct a flat set of prefixed trait IDs:
      scheduling_traits = (
          {f"is:{s}" for s in substance.is_} |
          {f"intake:{s}" for s in substance.intake} |
          {f"effect:{s}" for s in substance.effect} |
          {f"risk:{s}" for s in substance.risk} |
          {f"activity:{s}" for s in substance.activity}
      )
    Then iterate over scheduling_traits (not substance.traits) for effective/primary/sources tracking.
    The dashboard namespace is intentionally excluded — dashboard: slugs are curation markers with no scheduling effects.

    In planner/engine/doctor.py:
    Replace "for trait_id in substance.traits" (line ~55) with iteration over all 6 namespace fields to reconstruct trait_refs:
      for field, ns in [('is_','is'),('intake','intake'),('effect','effect'),('risk','risk'),('activity','activity'),('dashboard','dashboard')]:
          for slug in getattr(substance, field):
              trait_refs.add(f"{ns}:{slug}")
  </action>
  <read_first>
    - planner/engine/_scheduling.py — read effective_stack_item_traits() full implementation
    - planner/engine/doctor.py — read collect_orphans() around line 50–57 for traits loop
    - planner/contracts.py — verify Substance field names after task 08-01-04
  </read_first>
  <acceptance_criteria>
    - effective_stack_item_traits() iterates over {f"is:{s}" for s in substance.is_} union of 5 namespaces (not substance.traits)
    - "dashboard" namespace is NOT included in the scheduling trait set
    - doctor.py trait_refs reconstruction covers all 6 namespace fields (including dashboard)
    - python3 -c "from planner.engine._scheduling import effective_stack_item_traits; print('import OK')" exits 0
    - python3 -c "from planner.engine.doctor import collect_orphans; print('import OK')" exits 0
  </acceptance_criteria>
</task>

<task id="08-01-08">
  <title>DT-02: Migrate all substance YAML cards from flat traits: to grouped keys</title>
  <action>
    Migrate all 200 data/substances/*.yaml files from flat "traits: [namespace:slug, ...]" to grouped form.
    For each substance card:
    1. Read the "traits:" list (e.g. ["class:antioxidant", "intake:food_preferred", "risk:manual_review"]).
    2. Group by namespace prefix: class: → is: (bare slug), intake: → intake: (bare slug), effect: → effect:, risk: → risk:, activity: → activity:.
    3. Write each group as a top-level key with list of bare slugs (no prefix in slug).
    4. Remove the "traits:" key entirely.
    5. Substances with empty traits: [] get no namespace keys added (all groups are optional).
    Migration rules:
    - "class:X" → under top-level "is:" as bare "X"
    - "intake:X" → under top-level "intake:" as bare "X"
    - "effect:X" → under top-level "effect:" as bare "X"
    - "risk:X" → under top-level "risk:" as bare "X"
    - "activity:X" → under top-level "activity:" as bare "X"
    Keep all other fields (id, name, form, aliases, notes, concerns, prefer_with) unchanged.
    Write a migration script or do this with a careful programmatic approach — 200 files is too many for manual editing.
    Preserve YAML formatting style (use ruamel.yaml or yaml.dump with sort_keys=False to avoid reordering fields).
  </action>
  <read_first>
    - data/substances/alpha_gpc__sub_tzg5glskrd.yaml — example with class: and risk:
    - data/substances/ashwagandha__sub_810b76cde1.yaml — example with class: + intake: + risk:
    - schema/substance.schema.json — verify grouped schema is in place (task 08-01-01 must be done)
  </read_first>
  <acceptance_criteria>
    - No data/substances/*.yaml file contains a "traits:" key
    - At least one substance file contains a top-level "is:" key with bare slug list
    - python3 -c "import subprocess, sys; r=subprocess.run(['uv','run','python','-m','planner','check'],capture_output=True,text=True); print(r.stdout,r.stderr); sys.exit(r.returncode)" (substance schema errors = 0)
    - grep -rn "^traits:" data/substances/ returns no output
  </acceptance_criteria>
</task>

<task id="08-01-09">
  <title>DT-04: Add dashboard: tags to substance cards based on current taking[] migration map</title>
  <action>
    For each of the 13 surviving dashboards, add the dashboard slug to the "dashboard:" group on each member substance card.
    Migration map (from dashboard taking[] lists — read each dashboard file to get the substance IDs):
    For each data/dashboards/*.yaml (except vasodilation_no_pathway.yaml which is being deleted):
    1. Read the "taking:" list to get substance IDs.
    2. For each substance ID, open the corresponding substance card (already migrated in DT-02).
    3. Add the dashboard slug (filename without .yaml) to the substance's "dashboard:" list.
    4. If the substance already has a "dashboard:" key, append; otherwise create it.

    Substance cards referenced by multiple dashboards get multiple entries in their "dashboard:" list.
    Example: if connective_tissue_support.yaml has substance sub_q2learrbro, then
    data/substances/*__sub_q2learrbro.yaml gets "dashboard:\n- connective_tissue_support" added.

    Do NOT add dashboard: tags for vasodilation_no_pathway — that dashboard is being deleted.
  </action>
  <read_first>
    - data/dashboards/connective_tissue_support.yaml — example taking[] list
    - data/dashboards/vascular_health.yaml — example taking[] list
    - data/dashboards/bleeding_load.yaml — example taking[] list (16 members, largest)
    - data/substances/ — any member substance card to understand current structure after DT-02
  </read_first>
  <acceptance_criteria>
    - Substance cards that were in connective_tissue_support's taking[] now have "connective_tissue_support" in their "dashboard:" list
    - Substance cards that were in vascular_health's taking[] have "vascular_health" in their "dashboard:" list
    - No substance card has "vasodilation_no_pathway" in its dashboard: list
    - grep -rn "vasodilation_no_pathway" data/substances/ returns no output
    - python3 -c "import yaml; s=yaml.safe_load(open('data/substances/alpha_lipoic_acid__sub_iu7b8h87g2.yaml').read()); print(s.get('dashboard', []))" (verify a known member has its tag)
  </acceptance_criteria>
</task>

<task id="08-01-10">
  <title>DT-06: Rewrite all dashboard YAML files to use from_traits, delete vasodilation_no_pathway</title>
  <action>
    For each of the 13 surviving data/dashboards/*.yaml files:
    1. Replace the "taking:" block with "from_traits:" using grouped object syntax.
    2. Since all 13 are operator-curated (per RESEARCH.md — none are pure class projections), each gets:
       from_traits:
         dashboard:
         - <slug_matching_filename>
    3. Keep all other fields (name, description, benefit, risk, started) unchanged.
    4. Remove the entire "taking:" block.

    Delete data/dashboards/vasodilation_no_pathway.yaml entirely.

    The 13 surviving dashboard slugs and their from_traits entries:
    - antioxidant_protection → dashboard: [antioxidant_protection]
    - bleeding_load → dashboard: [bleeding_load]
    - cholinergic_load → dashboard: [cholinergic_load]
    - connective_tissue_support → dashboard: [connective_tissue_support]
    - cortisol_reduction → dashboard: [cortisol_reduction]
    - electrolyte_hydration_support → dashboard: [electrolyte_hydration_support]
    - mitochondrial_health → dashboard: [mitochondrial_health]
    - neurocognitive_support → dashboard: [neurocognitive_support]
    - serotonergic_load → dashboard: [serotonergic_load]
    - skin_barrier_support → dashboard: [skin_barrier_support]
    - sleep_recovery → dashboard: [sleep_recovery]
    - vascular_health → dashboard: [vascular_health]
    - workout_performance → dashboard: [workout_performance]
  </action>
  <read_first>
    - data/dashboards/connective_tissue_support.yaml — current structure to preserve non-taking fields
    - data/dashboards/vasodilation_no_pathway.yaml — confirm it has 5 members all in vascular_health before deleting
    - schema/dashboard.schema.json — verify from_traits schema is in place (task 08-01-02 must be done)
  </read_first>
  <acceptance_criteria>
    - data/dashboards/vasodilation_no_pathway.yaml does not exist
    - ls data/dashboards/*.yaml | wc -l outputs 13
    - Each surviving dashboard YAML has "from_traits:" key and no "taking:" key
    - python3 -c "import yaml; d=yaml.safe_load(open('data/dashboards/connective_tissue_support.yaml').read()); assert 'from_traits' in d; assert 'taking' not in d; assert d['from_traits']['dashboard']==['connective_tissue_support']; print('OK')" exits 0
    - grep -rn "^taking:" data/dashboards/ returns no output
  </acceptance_criteria>
</task>

<task id="08-01-11">
  <title>DT-08: Add reference-integrity checks to check_substances() and check_dashboards()</title>
  <action>
    The substance-side ref-integrity check is already implemented in task 08-01-05 (check_substances() validates namespace slugs against trait_ids).
    The dashboard-side ref-integrity check is already implemented in task 08-01-06 (check_dashboards() validates from_traits slugs against trait_ids).

    Verify the error message format in both:
    - Substance side: "Unknown trait '{slug}' under namespace '{namespace}:' in {sf} — register it in data/traits.yaml under '{namespace}:' first (with label and description)."
    - Dashboard side: "Unknown trait '{slug}' under namespace '{namespace}:' in from_traits of {gf} — register it in data/traits.yaml under '{namespace}:' first."

    Also verify that planner/engine/check.py passes trait_ids to check_dashboards() correctly. Read check.py and update the check_dashboards() call site if the signature changed in 08-01-06.
    Current call: check_dashboards(dashboard_files, substance_ids, substances)
    New call needs trait_ids: check_dashboards(dashboard_files, substance_ids, substances, trait_ids=trait_ids)
    (or restructure as needed to pass the trait set).
  </action>
  <read_first>
    - planner/engine/check.py — read cmd_check() to find check_dashboards() call site
    - planner/cards/substance.py — verify error message format from task 08-01-05
    - planner/cards/dashboards.py — verify error message format from task 08-01-06
  </read_first>
  <acceptance_criteria>
    - check_dashboards() receives trait_ids and validates from_traits slugs
    - Introducing an unknown slug in a substance card (e.g. "is: [nonexistent_slug]") causes "uv run python -m planner check" to print an error containing "nonexistent_slug" and exit non-zero
    - Introducing an unknown slug in a dashboard from_traits causes "uv run python -m planner check" to print an error and exit non-zero
    - Error messages include "register it in data/traits.yaml" guidance
  </acceptance_criteria>
</task>

<task id="08-01-12">
  <title>DT-10: Update existing tests and add new tests for Stage 1 correctness</title>
  <action>
    Update tests/test_phase_01.py:
    1. test_training_substances_have_expected_activity_traits() (line ~156–162):
       Change "assert activity_trait in card['traits']" to "assert bare_slug in card.get('activity', [])"
       where bare_slug is the slug without the "activity:" prefix (e.g. "pre_workout" not "activity:pre_workout").
    2. test_vascular_dashboard_taking_members_are_seven_known_substances() (line ~165–185):
       Rewrite to verify: (a) vascular_health.yaml has from_traits.dashboard == ["vascular_health"];
       (b) exactly 7 substance cards have "vascular_health" in their dashboard: list.

    Update tests/test_scheduling_units.py:
    1. make_substance() factory (line ~65–66): change "traits=()" to use new field names. The factory should create Substance(id=sub_id, name=name) with default namespace fields — no traits parameter.

    Update tests/test_schemas.py (or add to test_phase_01.py or new test file):
    Add new test functions:
    1. test_substance_schema_accepts_grouped_form: create a dict with is=["antioxidant"], intake=["food_preferred"] and assert schema_errors() returns [].
    2. test_substance_schema_rejects_flat_traits_form: create a dict with traits=["class:antioxidant"] and assert schema_errors() returns non-empty list.
    3. test_substance_schema_enforces_intake_maxitems: create dict with intake=["empty_preferred", "food_required"] and assert schema_errors() returns non-empty list.
    4. test_from_traits_dashboard_schema_accepts_grouped_form: create valid dashboard dict with from_traits={dashboard: ["connective_tissue_support"]} and assert schema_errors() returns [].
    5. test_check_substances_rejects_unknown_namespace_slug: call check_substances() with a substance file containing is: [unknown_slug] and assert errors list is non-empty and contains "unknown_slug".
    6. test_check_dashboards_rejects_unknown_from_traits_slug: call check_dashboards() with a dashboard file containing from_traits: {dashboard: [unknown_slug]} and assert errors list is non-empty.

    Also update any other test fixtures that construct Substance(traits=...) or read card["traits"] from YAML.
  </action>
  <read_first>
    - tests/test_phase_01.py — read EXPECTED_ACTIVITY_TRAITS dict and both affected test functions
    - tests/test_scheduling_units.py — read make_substance() and all Substance() constructor calls
    - tests/test_schemas.py — read existing schema test patterns to match style
    - tests/test_phase_03.py — check for any traits references that need updating
  </read_first>
  <acceptance_criteria>
    - test_training_substances_have_expected_activity_traits passes with grouped substance YAML
    - test_vascular_dashboard_taking_members_are_seven_known_substances passes with from_traits YAML
    - No test constructs Substance(traits=...)
    - New schema tests: grouped form accepted, flat form rejected, maxItems:1 enforced
    - uv run pytest exits 0 — all tests pass
  </acceptance_criteria>
</task>

<task id="08-01-13">
  <title>DT-09: Regenerate schedule.yaml and verify dashboard coverage output</title>
  <action>
    Run: uv run python -m planner plan
    This regenerates schedule.yaml using the new from_traits-based membership resolution.
    Verify the output:
    1. "benefits" section contains exactly 13 dashboard entries (not 14 — vasodilation_no_pathway is gone).
    2. "Vasodilation / NO Pathway" is absent from benefits and risks sections.
    3. "Vascular Health" entry appears with the same 7 substance names as before (sub_fmuptat7pw, sub_699a985e61, sub_3918fe347e, sub_877c24aad4, sub_396c221c31, sub_a3ec9f9c52, sub_7628e4f478 mapped to their substance names).
    4. Workout Performance cluster shows same members as before.
    5. No cluster shows an empty covered+inactive+missing set (which would indicate resolution breakage).
    If any cluster's membership differs from the pre-refactor baseline (captured in tests), investigate whether the substance got its dashboard: tag in DT-04 and whether the from_traits resolution in build_dashboard_review() is correct.
  </action>
  <read_first>
    - schedule.yaml — read current (pre-refactor) benefits section to capture baseline cluster memberships before running plan
    - planner/cards/dashboards.py — verify build_dashboard_review() from_traits resolution is implemented correctly
  </read_first>
  <acceptance_criteria>
    - uv run python -m planner plan exits 0
    - schedule.yaml "benefits" list length == 13 (verify with python3 -c "import yaml; s=yaml.safe_load(open('schedule.yaml').read()); print(len(s['benefits']))")
    - "Vasodilation / NO Pathway" not in any section of schedule.yaml
    - "Vascular Health" entry present in benefits with covered/inactive/missing matching 7 substance names
    - uv run pytest exits 0 after schedule regeneration
  </acceptance_criteria>
</task>

<task id="08-01-14">
  <title>Stage 1 atomic commit — commit all changes together</title>
  <action>
    After all tasks 08-01-01 through 08-01-13 are complete and uv run pytest passes:
    Stage all changes and create a single atomic commit covering the full Stage 1 migration:
    git add schema/substance.schema.json schema/dashboard.schema.json data/traits.yaml planner/io.py planner/contracts.py planner/cards/substance.py planner/cards/dashboards.py planner/engine/_scheduling.py planner/engine/doctor.py planner/engine/check.py data/substances/ data/dashboards/ tests/ schedule.yaml
    git commit -m "feat(ontology): grouped trait shape + dashboard membership via tags (Stage 1)"

    The commit message body should note: schema overhaul, 200 substance cards migrated, 13 dashboards converted to from_traits, vasodilation_no_pathway deleted, reference-integrity checks added, tests updated.
  </action>
  <read_first>
    - Run uv run pytest one final time to confirm all tests pass before committing
    - Run uv run python -m planner check to confirm 0 errors
  </read_first>
  <acceptance_criteria>
    - git log --oneline -1 shows the Stage 1 commit
    - uv run python -m planner check exits 0 on committed state
    - uv run pytest exits 0 on committed state
    - git diff HEAD shows no unstaged changes to Stage 1 files
  </acceptance_criteria>
</task>

## Verification

Run after all tasks:
```
uv run python -m planner check
uv run pytest
uv run python -m planner plan
```

All three must exit 0. schedule.yaml must have 13 dashboard entries, not 14.
