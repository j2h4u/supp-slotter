---
phase: 02-substance-product-yaml-model-split
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - planner.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "D-07 D-08 D-09 D-10: planner.py loads separate Substance, Product, and InventoryItem registries."
    - "D-11 D-12 D-28: product components are aggregated into one schedulable inventory item; component conflicts become warnings, not split assignments."
    - "D-03 D-04 D-05: planner.py remains a deterministic local YAML CLI and adds no MCP, web import, approval, draft, confidence, or database workflow."
    - "D-21 D-23 D-25: validation and warnings stay practical, deterministic, and explainable."
    - "Review 02-02 HIGH: acceptance criteria verify loader/refresh behavior and CLI output rather than exact implementation text where equivalent correct code should pass."
    - "Review 02-02 MEDIUM: cmd_check target-path mode, missing inventory entry severity, and the new mechanism namespace are explicit."
  artifacts:
    - path: "planner.py"
      provides: "Substance/product/inventory loader and validator"
      contains: "SUBSTANCES_DIR, PRODUCTS_DIR, load_substance, check_substances, check_product_formulas"
---

<objective>
Update planner.py validation and data loading for the split model. The check command must validate substances, product formulas, inventory product refs, goal substance refs, and trait overrides before scheduling uses the new model.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add split-model path constants and loaders</name>
  <read_first>
    - planner.py
    - data/substances/nattokinase.yaml
    - data/products/nattokinase.yaml
    - data/inventory.yaml
  </read_first>
  <files>planner.py</files>
  <action>
In `planner.py`, add `SUBSTANCES_DIR = DATA_DIR / "substances"` while keeping `PRODUCTS_DIR = DATA_DIR / "products"` for concrete product formulas. Add `load_substance(sf: Path)` mirroring the current `load_product(pf: Path)` behavior and keep `load_product` for product formulas.

Do not add compatibility readers that scan old product-as-substance cards. The new source of universal traits is always `data/substances/*.yaml`.

Add `mechanism` to `REGISTERED_NAMESPACES` so Phase 2 mechanism traits validate through the existing namespace gate.
  </action>
  <acceptance_criteria>
    - `grep -q 'SUBSTANCES_DIR = DATA_DIR / "substances"' planner.py`
    - `grep -q 'def load_substance' planner.py`
    - `grep -q 'def load_product' planner.py`
    - `uv run planner.py check data/traits.yaml` exits 0 after `data/traits.yaml` contains `mechanism:vasodilator`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace product-card validation with substance and formula validation</name>
  <read_first>
    - planner.py
    - schema/substance.schema.json
    - schema/product.schema.json
    - data/substances/*.yaml
    - data/products/*.yaml
  </read_first>
  <files>planner.py</files>
  <action>
Replace the old `check_products(product_files, trait_ids)` behavior with two validation passes:

1. `check_substances(substance_files, trait_ids) -> tuple[list[str], list[str], dict[str, Path]]`
   - schema-validates each substance against `substance`
   - enforces `id == filename stem`
   - enforces unique ids
   - validates every substance `traits[]` id against `trait_ids`
   - validates `prefer_with` refs against substance ids if retained on substances
   - preserves unmatched concern INFO reporting

2. `check_product_formulas(product_files, substance_ids) -> tuple[list[str], list[str], dict[str, Path]]`
   - schema-validates each product against `product`
   - enforces `id == filename stem`
   - enforces unique product ids
   - validates every `components[].substance` ref against `substance_ids`
   - emits a deterministic error containing `components[` and `unknown substance` for missing refs

Keep `schema_errors` and `report` as the shared error path.
  </action>
  <acceptance_criteria>
    - `grep -q 'def check_substances' planner.py`
    - `grep -q 'def check_product_formulas' planner.py`
    - `grep -q 'schema_errors(substance, "substance"' planner.py`
    - `grep -q 'schema_errors(product, "product"' planner.py`
    - `grep -q 'components' planner.py`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Validate inventory product refs, overrides, and goal substance refs</name>
  <read_first>
    - planner.py
    - schema/inventory.schema.json
    - schema/goal.schema.json
    - data/inventory.yaml
    - data/goals/vascular_health.yaml
  </read_first>
  <files>planner.py</files>
  <action>
Update `check_inventory_alignment` so inventory rows are keyed by inventory item id and each entry's `product` field must reference an existing product formula id. It should still report product formulas with no inventory entry as refresh candidates, but the primary validation is now inventory `product` -> product card.

Validation severity is explicit: an inventory entry whose `product` points to a missing formula is an error and makes `uv run planner.py check` exit non-zero. A product formula with no inventory entry is a non-fatal refresh candidate/warning because it may be a shelf product not currently in protocol.

Keep `check_inventory_overrides` validating `traits_override.add/remove` against `trait_ids`.

Update `check_goals` so `members[].substance` refs validate against `substance_ids`, not product ids. Error text should say `has no matching substance card (expected at data/substances/<id>.yaml)`.

Update `cmd_check` full-scan flow to:

1. validate slots and traits
2. build `substance_ids`
3. build `product_ids`
4. validate inventory schema/alignment/overrides
5. validate goals against `substance_ids`

Update `cmd_check(target)` single-file mode directly:

- `data/substances/<id>.yaml` validates as a Substance and checks its trait ids against `trait_ids`.
- `data/products/<id>.yaml` validates as a Product formula and checks every component substance ref against the current substance registry.
- `data/inventory.yaml` validates inventory schema, product refs, and trait overrides against current products and traits.
- `data/goals/<id>.yaml` validates goal schema and member substance refs against current substances.
- Unknown target paths should keep the existing unsupported-target behavior if one exists; otherwise return a clear non-zero error naming the unsupported target.
  </action>
  <acceptance_criteria>
    - `grep -q 'has no matching substance card' planner.py`
    - `grep -q 'data/substances' planner.py`
    - `grep -q 'product.*has no matching product' planner.py || grep -q 'has no matching product card' planner.py`
    - `uv run planner.py check` exits 0 after plan 02 is complete
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Keep refresh deterministic for product-backed inventory</name>
  <read_first>
    - planner.py
    - data/products/*.yaml
    - data/inventory.yaml
  </read_first>
  <files>planner.py</files>
  <action>
Update `cmd_refresh` so it discovers product formula ids from `data/products/*.yaml` and adds missing inventory entries as:

```python
supplements[new_id] = {"product": new_id, "stack": "inactive"}
```

The CLI help/docstring should say refresh appends missing product formulas to inventory as `{product: <id>, stack: inactive}`.

Addresses review concern 02-02: do not rely on exact local variable names or exact source text for acceptance. The behavior contract is that a product formula absent from inventory is appended with product equal to its formula id and stack equal to inactive, and the user-visible refresh/help text describes that shape.
  </action>
  <acceptance_criteria>
    - Create a temporary product formula `data/products/__refresh_probe__.yaml` that references an existing substance, run `uv run planner.py refresh`, and verify `data/inventory.yaml` contains an entry `__refresh_probe__` with `product: __refresh_probe__` and `stack: inactive`; remove the probe product and inventory entry before finishing the task.
    - `uv run planner.py refresh --help 2>&1 || uv run planner.py --help 2>&1` shows user-facing text that mentions missing product formulas are appended to inventory with `product` and `stack: inactive`.
    - `uv run planner.py check` exits 0 after the temporary probe cleanup.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `uv run planner.py check`
- Negative check: temporarily change one product component substance to `bogus_substance_xyz`; `uv run planner.py check` must fail and mention `unknown substance` or `data/substances/bogus_substance_xyz.yaml`.
- Negative check: temporarily change one goal member substance to `bogus_substance_xyz`; `uv run planner.py check` must fail and mention `has no matching substance card`.
- Single-file checks: `uv run planner.py check data/substances/nattokinase.yaml`, `uv run planner.py check data/products/nattokinase.yaml`, `uv run planner.py check data/inventory.yaml`, and `uv run planner.py check data/goals/vascular_health.yaml` each exit 0 after the split model is complete.
</verification>

<success_criteria>
- Check validates the split model end to end.
- Product formulas cannot reference missing substances.
- Inventory items cannot reference missing products.
- Goal members validate against substance cards.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML files -> validator | Local YAML must be schema and ref checked before planning |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Mitigation |
|-----------|----------|-----------|----------|------------|
| T-02-02-01 | Tampering | product component refs | high | `cmd_check` rejects missing substance refs before planning |
| T-02-02-02 | Tampering | inventory product refs | high | `cmd_check` rejects missing product refs before planning |
| T-02-02-03 | Safety | goal ref ambiguity | medium | goals validate against substances only after the split |
</threat_model>
