---
phase: 01-training-stacks-goals-ontology
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data/products/l_citrulline_malate.yaml
  - data/products/creatine.yaml
  - data/products/electrolyte_caps.yaml
  - data/products/l_carnitine_l_tartrate.yaml
  - data/goals/vascular_health.yaml
  - data/goals/mitochondrial_health.yaml
autonomous: true
requirements: [TRAIN-03, GOAL-01, GOAL-02]

must_haves:
  truths:
    - "4 substance cards declare an activity:* trait per CONTEXT.md substance-assignment table"
    - "data/goals/ directory exists"
    - "data/goals/vascular_health.yaml has 4 members all with status:taking and substance refs to existing cards"
    - "data/goals/mitochondrial_health.yaml has 1 member taking (acetyl_l_carnitine) + 2 candidates name-only (CoQ10, ALA)"
  artifacts:
    - path: "data/products/l_citrulline_malate.yaml"
      provides: "Pre-workout NO/blood-flow substance"
      contains: "activity:pre_workout"
    - path: "data/products/creatine.yaml"
      provides: "Timing-agnostic ergogenic"
      contains: "activity:any_workout"
    - path: "data/products/electrolyte_caps.yaml"
      provides: "Around-session electrolyte support"
      contains: "activity:any_workout"
    - path: "data/products/l_carnitine_l_tartrate.yaml"
      provides: "Peripheral carnitine for training (no BBB crossing)"
      contains: "activity:any_workout"
    - path: "data/goals/vascular_health.yaml"
      provides: "Vascular goal cluster (4 taking)"
      contains: "id: vascular_health, members: 4 entries"
    - path: "data/goals/mitochondrial_health.yaml"
      provides: "Mitochondrial goal cluster (Bruce Ames triad)"
      contains: "id: mitochondrial_health, members: 3 entries"
  key_links:
    - from: "goal members[].substance"
      to: "data/products/{ref}.yaml"
      via: "referential integrity validator (lands plan 03)"
      pattern: "substance: [a-z_]+"
    - from: "substance trait activity:*"
      to: "data/traits.yaml activity namespace"
      via: "check_products trait-id resolution"
      pattern: "activity:(pre|post|any)_workout"
---

<objective>
Add activity-namespace traits to the 4 training-stack substance cards, and create the data/goals/ directory with the two seed goal cards (vascular_health, mitochondrial_health). After this plan, all training substances declare their activity affinity, and the goal-master canonical model has its first two clusters on disk.

Purpose: TRAIN-03 (activity traits applied to substances), GOAL-01 (goals dir exists), GOAL-02 (2 seed goal cards).
Output: 4 modified product cards + 2 new goal cards in a new data/goals/ directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-training-stacks-goals-ontology/CONTEXT.md
@schema/goal.schema.json
@data/products/l_citrulline_malate.yaml
@data/products/creatine.yaml
@data/products/electrolyte_caps.yaml
@data/products/l_carnitine_l_tartrate.yaml

<interfaces>
<!-- Goal-card schema constraints. Schema is already on disk; do not modify. -->

goal.schema.json (top-level):
  required: [id, name, description, status]
  status enum: active | paused | retired
  members: array of objects, each with:
    required: [status]
    status enum: taking | candidate | declined
    oneOf: [{required:[substance]}, {required:[name]}]   # exactly one of these two
    optional: role, note, reason
  id pattern: ^[a-z][a-z0-9_]*$

Substance card structure (existing pattern):
  id: <stem>
  name: "Display Name"
  traits: ["ns:trait_id", ...]
  prefer_with: ["other_id"]    # optional
  notes: "..."                  # optional
  unmatched_concerns: [...]     # optional
</interfaces>

<substance_assignments>
<!-- Activity trait per substance, from CONTEXT.md. Use verbatim. -->

l_citrulline_malate     -> activity:pre_workout    (RCT-backed pre-exercise NO/blood flow)
creatine                -> activity:any_workout    (timing-agnostic per RCT)
electrolyte_caps        -> activity:any_workout    (operator's call: around session)
l_carnitine_l_tartrate  -> activity:any_workout    (peripheral, pre-or-post)
</substance_assignments>

<goal_seed_data>
<!-- Final content for both goal cards, derived from CONTEXT.md decisions. -->

vascular_health:
  members (all status:taking):
    - l_citrulline_malate    role: NO precursor / blood flow
    - nattokinase            role: fibrinolytic / clot prevention
    - tadalafil              role: PDE5 inhibition / vascular tone
    - vitamin_b5             role: lipid metabolism support

mitochondrial_health (Bruce Ames triad pattern):
  members:
    - acetyl_l_carnitine     status: taking      role: mitochondrial fuel transport (BBB-crossing carnitine)
    - name: "Coenzyme Q10"   status: candidate   role: electron transport chain support
    - name: "Alpha-Lipoic Acid" status: candidate role: mitochondrial antioxidant / pyruvate dehydrogenase cofactor
</goal_seed_data>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add activity:* traits to 4 substance cards</name>
  <files>
    data/products/l_citrulline_malate.yaml,
    data/products/creatine.yaml,
    data/products/electrolyte_caps.yaml,
    data/products/l_carnitine_l_tartrate.yaml
  </files>
  <action>
For each of the 4 product cards, add the appropriate activity trait to the existing `traits:` list. Preserve all existing traits, prefer_with, notes, unmatched_concerns. Append the activity trait at the end of the traits list (keeps diff minimal).

Mapping:
- l_citrulline_malate.yaml — append `"activity:pre_workout"` to traits
- creatine.yaml — append `"activity:any_workout"` to traits
- electrolyte_caps.yaml — append `"activity:any_workout"` to traits
- l_carnitine_l_tartrate.yaml — append `"activity:any_workout"` to traits

Do NOT remove or modify existing traits. Do NOT add `goals:` field — goal-master canonical (CONTEXT.md): substance cards do NOT carry goals; goal cards reference substances.

Note on electrolyte_caps: it carries `intake:prefers_food` while training slots have `food: false`. CONTEXT.md constraint #4 documents this as accepted operator-tolerated scoring mismatch. No action needed.
  </action>
  <verify>
    <automated>python3 -c "import yaml; want={'l_citrulline_malate':'activity:pre_workout','creatine':'activity:any_workout','electrolyte_caps':'activity:any_workout','l_carnitine_l_tartrate':'activity:any_workout'};
[__import__('sys').exit(f'{k}: missing {v}') for k,v in want.items() if v not in yaml.safe_load(open(f'data/products/{k}.yaml'))['traits']]; print('OK')"</automated>
  </verify>
  <done>All 4 cards declare their activity:* trait; existing traits/prefer_with/notes preserved verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Create data/goals/ directory + vascular_health.yaml</name>
  <files>data/goals/vascular_health.yaml</files>
  <action>
Create the directory `data/goals/` (mkdir -p) and write `data/goals/vascular_health.yaml` with exactly this structure (oneOf substance|name, status enum taking|candidate|declined, role string):

```yaml
id: vascular_health
name: "Vascular Health"
description: "Поддержка тонуса сосудов и кровотока: NO-вазодилатация, фибринолитическая активность, PDE5-модуляция, метаболическая поддержка липидного обмена. Синергия nattokinase + L-citrulline + tadalafil — основной кардиоваскулярный кластер operator'а."
status: active

members:
  - substance: l_citrulline_malate
    status: taking
    role: "NO precursor — повышает arginine, вазодилатация, улучшение кровотока"
  - substance: nattokinase
    status: taking
    role: "Fibrinolytic — растворение фибриновых сгустков, профилактика тромбозов"
  - substance: tadalafil
    status: taking
    role: "PDE5 inhibition — низкодозная пролонгированная вазодилатация (off-label vascular tone)"
  - substance: vitamin_b5
    status: taking
    role: "Pantothenic acid — кофактор CoA, поддержка липидного метаболизма (cholesterol/HDL)"
```

All 4 substance refs MUST exist as cards in data/products/ (they do: l_citrulline_malate, nattokinase, tadalafil, vitamin_b5). The referential validator in plan 03 will enforce this; pre-validate locally with the verify command below.
  </action>
  <verify>
    <automated>python3 -c "import yaml,json,jsonschema,pathlib; d=yaml.safe_load(open('data/goals/vascular_health.yaml')); s=json.loads(open('schema/goal.schema.json').read()); jsonschema.Draft202012Validator(s).validate(d); refs=[m['substance'] for m in d['members'] if 'substance' in m]; missing=[r for r in refs if not pathlib.Path(f'data/products/{r}.yaml').exists()]; assert not missing, f'unresolved: {missing}'; assert len(d['members'])==4 and all(m['status']=='taking' for m in d['members']); print('OK')"</automated>
  </verify>
  <done>data/goals/ exists; vascular_health.yaml validates against goal.schema.json; all 4 members status:taking with valid substance refs.</done>
</task>

<task type="auto">
  <name>Task 3: Create mitochondrial_health.yaml (Bruce Ames triad)</name>
  <files>data/goals/mitochondrial_health.yaml</files>
  <action>
Write `data/goals/mitochondrial_health.yaml`:

```yaml
id: mitochondrial_health
name: "Mitochondrial Health"
description: "Поддержка митохондриальной функции: транспорт жирных кислот в матрикс, ETC support, антиоксидантная защита от ROS. Bruce Ames triad pattern (carnitine + CoQ10 + ALA) — комплекс для митохондриального здоровья и снижения окислительного стресса с возрастом."
status: active

members:
  - substance: acetyl_l_carnitine
    status: taking
    role: "Carnitine carrier — транспорт длинноцепочечных жирных кислот в митохондриальный матрикс; BBB-crossing форма (нейропротекция)"
  - name: "Coenzyme Q10"
    status: candidate
    role: "ETC carrier (Complex I/II → III) — рассматривается как добавление к стеку"
  - name: "Alpha-Lipoic Acid"
    status: candidate
    role: "Митохондриальный антиоксидант + кофактор pyruvate dehydrogenase / α-ketoglutarate DH; рассматривается как добавление к стеку"
```

Note the mix: the first member uses `substance:` (ref to existing card), the other two use `name:` (free-text candidate, no card yet). The schema's `oneOf` constraint enforces exactly one of those two keys per member.
  </action>
  <verify>
    <automated>python3 -c "import yaml,json,jsonschema,pathlib; d=yaml.safe_load(open('data/goals/mitochondrial_health.yaml')); s=json.loads(open('schema/goal.schema.json').read()); jsonschema.Draft202012Validator(s).validate(d); m=d['members']; assert len(m)==3; takers=[x for x in m if x['status']=='taking']; cands=[x for x in m if x['status']=='candidate']; assert len(takers)==1 and takers[0].get('substance')=='acetyl_l_carnitine'; assert len(cands)==2 and all('name' in c and 'substance' not in c for c in cands); assert pathlib.Path('data/products/acetyl_l_carnitine.yaml').exists(); print('OK')"</automated>
  </verify>
  <done>mitochondrial_health.yaml validates against goal.schema.json; 1 taking (acetyl_l_carnitine, valid ref) + 2 candidate name-only entries; oneOf constraint satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → goal cards / product cards | Local YAML, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | goal members[].substance ref drifts from card filename | mitigate | Per-task automated existence check (pathlib) before commit; planner.cmd_check referential validator (plan 03) catches at run time |
| T-01-04 | Tampering | mismatched activity trait on substance | mitigate | Verify command checks expected activity trait per substance |
</threat_model>

<verification>
After all 3 tasks: every per-task automated assertion passes. Note that `planner.py check` will not cleanly validate goal cards yet (validator lands plan 03), so we use direct jsonschema validation in the verify commands to gain equivalent coverage early.
</verification>

<success_criteria>
- 4 product cards each carry the correct activity:* trait
- data/goals/vascular_health.yaml: 4 members, all taking, all valid refs
- data/goals/mitochondrial_health.yaml: 1 taking (valid ref) + 2 candidate name-only members
- Both goal cards validate against schema/goal.schema.json
- No `goals:` field added to any substance card (goal-master canonical preserved)
</success_criteria>

<output>
After completion, create `.planning/phases/01-training-stacks-goals-ontology/01-02-SUMMARY.md` documenting: which substance cards got which activity trait, and the member counts/statuses of each goal card.
</output>
