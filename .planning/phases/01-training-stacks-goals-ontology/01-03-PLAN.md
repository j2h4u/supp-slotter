---
phase: 01-training-stacks-goals-ontology
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - planner.py
autonomous: true
requirements: [TRAIN-02, TRAIN-03, GOAL-03]

must_haves:
  truths:
    - "REGISTERED_NAMESPACES includes 'activity'"
    - "cmd_refresh writes new entries with stack:inactive (not active:false)"
    - "cmd_plan partitions substances by stack — training substances cannot land in daily slots and vice versa"
    - "cmd_check loads every data/goals/*.yaml, validates against schema/goal.schema.json, and errors on members[].substance refs that have no matching product card"
  artifacts:
    - path: "planner.py"
      provides: "Stack-aware planner + goal-card validator"
      contains: "REGISTERED_NAMESPACES, activity, stack:, check_goals"
  key_links:
    - from: "cmd_plan substance loop"
      to: "slots.stack ↔ inventory.stack match"
      via: "candidate-slot filter"
      pattern: "slot.*stack.*!=.*stack"
    - from: "cmd_check goal pass"
      to: "card_ids registry built earlier in cmd_check"
      via: "members[].substance lookup"
      pattern: "members.*substance"
---

<objective>
Update planner.py to support the stack partition and goal-card validation. Four discrete change points, all in one file: register the activity namespace, swap refresh's default flag from active:false to stack:inactive, add stack-partition filtering to the candidate-slot enumeration in cmd_plan, and add goal-card validation (schema + referential integrity) to cmd_check.

Purpose: TRAIN-02 (stack-aware planner enforcement), TRAIN-03 (activity namespace registration), GOAL-03 (referential integrity validator).
Output: 1 modified file (planner.py).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-training-stacks-goals-ontology/CONTEXT.md
@planner.py
@schema/goal.schema.json
@schema/inventory.schema.json
@schema/slots.schema.json

<interfaces>
<!-- Existing helpers in planner.py the executor will reuse — do not reimplement. -->

load_yaml(path) -> object
load_schema(name) -> dict                       # name without ".schema.json" suffix
schema_errors(data, schema_name, file_path) -> list[str]
report(errors, info) -> int                     # prints + returns exit code
load_product(pf) -> tuple[dict|None, str|None]

# In cmd_check, by the time goal validation runs, this dict is in scope:
card_ids: dict[str, Path]                       # product-id -> product file path
# Built by check_products(); the executor must thread it into the new goal pass.
</interfaces>

<change_map>
<!-- Four discrete change points. Keep them surgical — minimal diff. -->

1. REGISTERED_NAMESPACES (module constant, ~line 34):
     {"intake","effect","class","family","risk"} -> add "activity"

2. cmd_refresh, new-entry default (~line 312):
     supplements[new_id] = {"active": False}  ->  supplements[new_id] = {"stack": "inactive"}
     Update the print message to mention "stack: inactive" instead of "active: false".

3. cmd_plan, substance-eligibility + candidate-slot loop (~lines 422–462):
     a) Filter by stack at the inventory loop: skip entries with stack=="inactive" (replace `if not entry.get("active"): continue`).
     b) Capture each substance's stack alongside traits (so the candidate loop can use it). Simplest: a parallel dict `sub_stacks: dict[str, str]`.
     c) In the candidate-slot enumeration, skip slots whose `stack` field doesn't match the substance's stack (BEFORE running compute_slot_score — saves work and prevents wrong-stack reasons in explanations).

4. cmd_check, new goal pass (insert before final report() call, after inventory checks):
     a) Glob `data/goals/*.yaml` (DATA_DIR / "goals" — handle dir not existing: empty list, no error).
     b) For each file: load + JSON-schema validate against "goal" schema.
     c) For each members[i]: if member has `substance:` key, verify substance is in card_ids dict (built by check_products earlier). Error message format: f"{goal_path}: members[{i}].substance '{ref}' has no matching product card (expected at data/products/{ref}.yaml)".
</change_map>

<negative_test_hint>
The plan-04 smoke test will confirm the goal validator works by temporarily inserting a bogus member ref and running check. The validator MUST fire on missing refs — that's the GOAL-03 acceptance.
</negative_test_hint>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register activity namespace + flip refresh default to stack:inactive</name>
  <files>planner.py</files>
  <action>
Two surgical edits:

(a) Update `REGISTERED_NAMESPACES` (currently `{"intake", "effect", "class", "family", "risk"}`) to include `"activity"`. Keep the constant near the top of the file alongside VALID_LEVELS.

(b) In `cmd_refresh`, change the new-entry default:
- `supplements[new_id] = {"active": False}` -> `supplements[new_id] = {"stack": "inactive"}`
- Update the trailing print line to read: `f"added {len(discovered)} new entries (stack: inactive): {', '.join(discovered)}"`
- Update the docstring/help line for the `refresh` subparser (search for `"append missing product cards to inventory.yaml as {active: false}"`) to read `"append missing product cards to inventory.yaml as {stack: inactive}"`. Also update the module docstring (line ~12) referring to `{active: false}`.

Do NOT touch any other logic in cmd_refresh.
  </action>
  <verify>
    <automated>python3 -c "import re; src=open('planner.py').read(); assert '\"activity\"' in src and 'REGISTERED_NAMESPACES' in src, 'namespace not registered'; assert re.search(r'supplements\\[new_id\\]\\s*=\\s*\\{\\s*\"stack\"\\s*:\\s*\"inactive\"', src), 'refresh default not switched'; assert 'active: false' not in src.lower() or 'stack: inactive' in src, 'stale active:false reference'; print('OK')"</automated>
  </verify>
  <done>REGISTERED_NAMESPACES = {"intake","effect","class","family","risk","activity"}; cmd_refresh writes `{"stack": "inactive"}`; help/docstring strings updated.</done>
</task>

<task type="auto">
  <name>Task 2: Add stack-partition filter to cmd_plan</name>
  <files>planner.py</files>
  <action>
Modify `cmd_plan` in two places:

(a) Substance-eligibility loop (currently `for sid, entry in inventory_data.get("supplements") ...`):
- Replace `if not entry.get("active"): continue` with:
  ```python
  stack = entry.get("stack")
  if stack == "inactive":
      continue
  ```
- Track the stack alongside traits/cards. Add a parallel dict at the same level as `active` and `cards`:
  ```python
  sub_stacks: dict[str, str] = {}
  ```
  and inside the loop after the card-load success branch: `sub_stacks[sid] = stack`.

(b) Candidate-slot enumeration (currently `for slot_name, slot in slots.items(): score, blocked, reasons = compute_slot_score(...)`):
- Insert a stack filter BEFORE compute_slot_score:
  ```python
  if slot.get("stack") != sub_stacks[sid]:
      continue
  ```
- Result: training substances see only training slots as candidates; daily substances see only daily slots.

Leave greedy + local search logic untouched. Leave separate_from logic untouched. Leave prefer_with logic untouched.

Note on minor scoring quirk (already accepted per CONTEXT.md constraint #3): the global balance penalty `BALANCE_WEIGHT * sum(load^2)` now sums over 6 slots instead of 4, so absolute scores will shift. Operator already accepts the post-migration topology will differ from the pre-Phase-1 baseline of 32.
  </action>
  <verify>
    <automated>python3 -c "import re; src=open('planner.py').read(); assert 'sub_stacks' in src, 'sub_stacks dict not added'; assert re.search(r'stack\\s*==\\s*[\"\\\']inactive', src), 'inactive skip not in cmd_plan'; assert re.search(r'slot\\.get\\([\"\\\']stack[\"\\\']\\)\\s*!=\\s*sub_stacks', src), 'stack-partition filter missing'; assert 'entry.get(\"active\")' not in src, 'stale active filter still present'; print('OK')"</automated>
  </verify>
  <done>cmd_plan skips inactive substances; candidate slots are stack-partitioned; no residual reference to `entry.get("active")`.</done>
</task>

<task type="auto">
  <name>Task 3: Add goal-card validator to cmd_check (referential integrity)</name>
  <files>planner.py</files>
  <action>
Add a new helper `check_goals(goal_files, card_ids) -> list[str]` and wire it into `cmd_check`. The validator MUST run after `check_products` (so card_ids is populated) and only on full-scan mode (target is None) so single-file product checks stay fast.

Helper signature and behavior:
```python
def check_goals(goal_files: list[Path], card_ids: dict[str, Path]) -> list[str]:
    """Validate goal cards against schema + referential integrity of members[].substance refs."""
    errors: list[str] = []
    for gf in goal_files:
        try:
            goal = load_yaml(gf)
        except yaml.YAMLError as e:
            errors.append(f"{gf}: yaml parse error: {e}")
            continue
        if goal is None:
            errors.append(f"{gf}: empty file")
            continue
        if not isinstance(goal, dict):
            errors.append(f"{gf}: top-level must be a mapping")
            continue
        errors.extend(schema_errors(goal, "goal", gf))
        for i, member in enumerate(goal.get("members") or []):
            if not isinstance(member, dict):
                continue  # schema_errors already flagged
            ref = member.get("substance")
            if ref is None:
                continue  # name-only member
            if ref not in card_ids:
                errors.append(
                    f"{gf}: members[{i}].substance '{ref}' has no matching product card "
                    f"(expected at data/products/{ref}.yaml)"
                )
    return errors
```

Add a module-level constant `GOALS_DIR = DATA_DIR / "goals"` near the other path constants (line ~30).

Wire into `cmd_check`, inside the `if target is None:` block (full-scan only), AFTER inventory checks and BEFORE `return report(...)`:
```python
goal_files = sorted(GOALS_DIR.glob("*.yaml")) if GOALS_DIR.exists() else []
errors.extend(check_goals(goal_files, card_ids))
```

Behavior contract:
- Missing data/goals/ directory: NOT an error; treated as zero goal cards.
- Invalid YAML or schema violation in any goal card: error.
- members[].substance ref pointing at a non-existent card: error.
- members[].name (free-text candidate without substance ref): ignored — no validation needed.

Do not modify any existing helper or any other cmd_check code path.
  </action>
  <verify>
    <automated>python3 -c "import re,subprocess; src=open('planner.py').read(); assert 'def check_goals(' in src, 'check_goals helper not defined'; assert 'GOALS_DIR' in src and 'goals' in src, 'GOALS_DIR constant missing'; assert re.search(r'check_goals\\(.*card_ids\\)', src), 'check_goals not wired into cmd_check'; assert 'schema_errors(goal, \"goal\"' in src, 'goal schema not validated'; print('OK')"</automated>
  </verify>
  <done>check_goals helper exists, validates schema + member refs; wired into cmd_check full-scan path; missing data/goals/ directory is silently tolerated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML on disk → planner.py | Local files only; no network input; no privileged operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | Stale `active:` key in legacy inventory entries silently bypassed by new stack filter | mitigate | inventory.schema.json `additionalProperties: false` rejects unknown keys → check fails fast on schema validation before cmd_plan runs |
| T-01-06 | Information disclosure | check error messages echo file paths | accept | Local CLI on operator's own machine; paths are not sensitive |
| T-01-07 | Denial of service | malformed goal YAML crashes cmd_check | mitigate | check_goals catches yaml.YAMLError, top-level-non-mapping, and empty-file cases explicitly |
</threat_model>

<verification>
Per-task assertions cover structural changes. The behavioral integration test — `uv run planner.py check && uv run planner.py plan` against the migrated data from plans 01 and 02 — runs in plan 04. If plan 04 fails, the gap-closure path returns to this plan with specific repro.
</verification>

<success_criteria>
- 4 surgical change points landed, all in planner.py
- Module-level: REGISTERED_NAMESPACES includes "activity"; GOALS_DIR constant added
- cmd_refresh: stack:inactive default; updated help/docstring
- cmd_plan: stack partition enforced via sub_stacks dict + candidate-slot filter
- cmd_check: check_goals helper validates schema + members[].substance refs against card_ids
- No regression to existing helpers (load_yaml, schema_errors, check_traits, check_products, check_inventory_alignment, check_inventory_overrides) — they remain untouched
</success_criteria>

<output>
After completion, create `.planning/phases/01-training-stacks-goals-ontology/01-03-SUMMARY.md` documenting: the 4 change points landed, line-number anchors (approximate), and any deviations from the change_map (there should be none).
</output>
