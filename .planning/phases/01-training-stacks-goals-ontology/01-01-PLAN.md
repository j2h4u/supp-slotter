---
phase: 01-training-stacks-goals-ontology
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/slots.yaml
  - data/traits.yaml
  - data/inventory.yaml
autonomous: true
requirements: [TRAIN-01, TRAIN-02, TRAIN-03]

must_haves:
  truths:
    - "data/slots.yaml has 6 slots; the original 4 carry stack:daily; pre_workout/post_workout carry stack:training"
    - "data/traits.yaml declares 3 activity:* traits with the asymmetric levels from CONTEXT.md"
    - "data/inventory.yaml has zero `active:` keys; every entry has a `stack` value matching CONTEXT.md substance assignments"
  artifacts:
    - path: "data/slots.yaml"
      provides: "Slot table with stack partition (4 daily + 2 training)"
      contains: "stack: daily, stack: training, activity: pre_workout, activity: post_workout"
    - path: "data/traits.yaml"
      provides: "activity namespace traits (pre_workout/post_workout/any_workout)"
      contains: "activity:pre_workout, activity:post_workout, activity:any_workout"
    - path: "data/inventory.yaml"
      provides: "23 inventory entries migrated to stack partition"
      contains: "stack: training, stack: daily, stack: inactive"
  key_links:
    - from: "inventory entry stack"
      to: "slot stack"
      via: "planner.py:cmd_plan stack filter (lands in plan 03)"
      pattern: "stack:\\s*(daily|training|inactive)"
    - from: "activity traits in traits.yaml"
      to: "activity field on training slots"
      via: "effects[].match.activity"
      pattern: "match: \\{activity:"
---

<objective>
Migrate the three foundational data files to the stack-partitioned, activity-aware model. After this plan, the data is internally consistent with the already-updated schemas — but `planner.py check` will not yet pass because the planner code has not been updated (lands in plan 03). Smoke test in plan 04 is the gate.

Purpose: TRAIN-01 (training slots), TRAIN-02 (stack partition replaces active flag), TRAIN-03 (activity namespace).
Output: 3 modified data files; pre-Phase-1 active/inactive flags fully replaced by stack: daily|training|inactive.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-training-stacks-goals-ontology/CONTEXT.md
@schema/slots.schema.json
@schema/inventory.schema.json
@data/slots.yaml
@data/traits.yaml
@data/inventory.yaml

<interfaces>
<!-- Schema constraints the executor MUST honor. Schemas are already on disk; do not modify them. -->

slots.schema.json (slots.*):
  required: [label, order, stack]
  optional: [time, food, activity]
  stack enum: daily | training
  time enum: morning | day | evening
  activity enum: pre_workout | post_workout | any_workout

inventory.schema.json (supplements.*):
  required: [stack]            # NOTE: `active` field is NOT in the schema — it will fail additionalProperties:false
  optional: [dose, brand, notes, traits_override]
  stack enum: daily | training | inactive

traits.yaml structure (per existing entries):
  "<namespace>:<name>":
    label: str
    description: str
    applies_when: str
    effects:
      - match: {<slot_field>: <value>}
        level: prefer_strong | prefer | avoid | avoid_strong   # OR
        block: true
    separate_from: ["<trait>"]   # optional
</interfaces>

<assignments>
<!-- Substance -> stack mapping from CONTEXT.md. Use this verbatim. -->

training (4): l_citrulline_malate, creatine, electrolyte_caps, l_carnitine_l_tartrate
daily (11):   vitamin_d3, vitamin_b5, coenzyme_b_complex, magnesium_glycinate,
              trace_minerals, potassium_citrate, lions_mane_b6_complex,
              acetyl_l_carnitine, astaxanthin, nattokinase, tadalafil
inactive (8): lions_mane, picamilon, se_methyl_l_selenocysteine,
              dihydroquercetin_complex, copper, n_acetyl_cysteine,
              krill_oil, glycine

Total: 23 (matches current inventory).

Promotion: l_carnitine_l_tartrate moves from active:false -> stack:training (per success criterion 10).
</assignments>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update slots.yaml — add stack to 4 existing slots, append 2 training slots</name>
  <files>data/slots.yaml</files>
  <action>
Add `stack: daily` to morning_empty, morning_food, day_food, evening_empty (preserve order, label, time, food). Append two new slots:

```yaml
  pre_workout:
    label: "Pre-workout"
    order: 5
    stack: training
    food: false
    activity: pre_workout

  post_workout:
    label: "Post-workout"
    order: 6
    stack: training
    food: false
    activity: post_workout
```

Rationale: `food: false` is included so existing intake:prefers_empty_stomach substances score correctly in training slots (per CONTEXT.md). Training slots intentionally omit `time:` — workouts float across the day.
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; d=yaml.safe_load(open('data/slots.yaml')); s=d['slots']; assert len(s)==6, f'expected 6 slots got {len(s)}'; assert all('stack' in v for v in s.values()), 'every slot must have stack'; assert s['pre_workout']['stack']=='training' and s['pre_workout']['activity']=='pre_workout'; assert s['post_workout']['stack']=='training' and s['post_workout']['activity']=='post_workout'; assert all(s[n]['stack']=='daily' for n in ('morning_empty','morning_food','day_food','evening_empty')); print('OK')"</automated>
  </verify>
  <done>data/slots.yaml has 6 slots, all carry `stack`, training pair carries `activity:pre_workout` / `activity:post_workout`. JSON-schema validation against schema/slots.schema.json passes (verified by the planner in plan 04).</done>
</task>

<task type="auto">
  <name>Task 2: Add activity namespace to traits.yaml</name>
  <files>data/traits.yaml</files>
  <action>
Append a new section to data/traits.yaml under a clearly-titled comment header (mirror existing namespace headers — `# === activity namespace — приём вокруг тренировки ===`). Add three traits with asymmetric levels EXACTLY as locked in CONTEXT.md:

```yaml
  "activity:pre_workout":
    label: "Pre-workout"
    description: "Принимается строго перед тренировкой — RCT-подтверждённый эффект завязан на pre-exercise timing (пиковая концентрация к началу нагрузки)."
    applies_when: "L-Citrulline Malate (NO/blood flow, 40–60 min до). НЕ применять к веществам, для которых timing неважен (creatine) или которые равно работают до/после."
    effects:
      - match: {activity: pre_workout}
        level: prefer_strong

  "activity:post_workout":
    label: "Post-workout"
    description: "Принимается строго после тренировки — recovery / replenishment контекст."
    applies_when: "Recovery-focused substances where post-session timing matters specifically. НЕ применять к timing-agnostic substances."
    effects:
      - match: {activity: post_workout}
        level: prefer_strong

  "activity:any_workout":
    label: "Any workout slot"
    description: "Привязан к тренировке, но timing (до/после) не критичен — RCT не показывает преимущества одного варианта над другим. Graceful degradation: специфичные pre/post traits перебивают этот по силе."
    applies_when: "Creatine (timing-agnostic per RCT), electrolyte caps (around session), L-Carnitine L-Tartrate (peripheral, pre-or-post). НЕ применять к веществам со строгим timing — для них есть pre_workout / post_workout."
    effects:
      - match: {activity: pre_workout}
        level: prefer
      - match: {activity: post_workout}
        level: prefer
```

Naming convention: workout stem (pre_workout / post_workout / any_workout) — locked per CONTEXT.md.

Do NOT modify any existing trait entry. Do NOT touch existing namespace section headers.
  </action>
  <verify>
    <automated>python3 -c "import yaml; t=yaml.safe_load(open('data/traits.yaml'))['traits']; required={'activity:pre_workout','activity:post_workout','activity:any_workout'}; missing=required-set(t); assert not missing, f'missing: {missing}'; assert t['activity:pre_workout']['effects'][0]['level']=='prefer_strong'; assert t['activity:post_workout']['effects'][0]['level']=='prefer_strong'; awo=t['activity:any_workout']['effects']; assert len(awo)==2 and all(e['level']=='prefer' for e in awo); print('OK')"</automated>
  </verify>
  <done>traits.yaml contains exactly 3 new activity:* traits with the asymmetric level pattern. Existing 16 traits untouched. (Full check pass deferred to plan 04 — REGISTERED_NAMESPACES does not yet include `activity` until plan 03 lands.)</done>
</task>

<task type="auto">
  <name>Task 3: Migrate inventory.yaml — replace active flag with stack partition</name>
  <files>data/inventory.yaml</files>
  <action>
For all 23 supplement entries, remove `active: true|false` and insert `stack: <value>` as the first key after the supplement id. Stack values per CONTEXT.md substance-assignment table:

- training (4): l_citrulline_malate, creatine, electrolyte_caps, l_carnitine_l_tartrate
- daily (11): vitamin_d3, vitamin_b5, coenzyme_b_complex, magnesium_glycinate, trace_minerals, potassium_citrate, lions_mane_b6_complex, acetyl_l_carnitine, astaxanthin, nattokinase, tadalafil
- inactive (8): lions_mane, picamilon, se_methyl_l_selenocysteine, dihydroquercetin_complex, copper, n_acetyl_cysteine, krill_oil, glycine

Note: l_carnitine_l_tartrate is being PROMOTED from inactive -> training (success criterion 10).

Preserve all existing dose, brand, notes fields verbatim. Preserve section comment headers. Re-flow section comment for the inactive group to drop the "physically owned, not currently taking" phrasing if it now reads awkwardly given l_carnitine_l_tartrate moved out — light editorial touch only, no semantic changes elsewhere.

Do NOT add traits_override on any entry (none required for this phase).
  </action>
  <verify>
    <automated>python3 -c "import yaml; inv=yaml.safe_load(open('data/inventory.yaml'))['supplements']; assert len(inv)==23, f'count {len(inv)}'; assert not any('active' in e for e in inv.values()), 'active key still present'; assert all('stack' in e for e in inv.values()), 'missing stack'; from collections import Counter; c=Counter(e['stack'] for e in inv.values()); assert c=={'daily':11,'training':4,'inactive':8}, f'stack histogram: {c}'; assert inv['l_carnitine_l_tartrate']['stack']=='training', 'l_carnitine not promoted'; print('OK')"</automated>
  </verify>
  <done>inventory.yaml has 23 entries; histogram = {daily:11, training:4, inactive:8}; no `active` keys remain; l_carnitine_l_tartrate is on the training stack. (Full schema check deferred to plan 04 smoke test.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → YAML files | Local filesystem only; no network, no auth, no PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | data/inventory.yaml during migration | mitigate | JSON-schema validation in plan 04 catches malformed entries; per-task automated YAML parse + structural assertions catch typos pre-commit |
| T-01-02 | Information disclosure | inventory contents | accept | Personal supplement list, no medical PHI, repo-local only |
</threat_model>

<verification>
After all 3 tasks: each file is valid YAML and the per-task assertions pass. Full `planner.py check` will FAIL until plan 03 lands (REGISTERED_NAMESPACES missing `activity` and inventory schema-validation requires the schema to allow `stack` — already done). This is expected; the smoke test in plan 04 is the integration gate.
</verification>

<success_criteria>
- All 3 files modified, no `active:` keys in inventory.yaml
- Stack histogram exactly {daily:11, training:4, inactive:8}
- 6 slots in slots.yaml with stack assignments
- 19 traits in traits.yaml (16 pre-existing + 3 activity:*)
- Schema mismatches against schema/slots.schema.json or schema/inventory.schema.json => stop and fix before commit
</success_criteria>

<output>
After completion, create `.planning/phases/01-training-stacks-goals-ontology/01-01-SUMMARY.md` documenting: which files were modified, the final stack histogram, and confirmation that l_carnitine_l_tartrate was promoted to training.
</output>
