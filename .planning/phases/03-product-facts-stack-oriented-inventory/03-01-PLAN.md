---
phase: 03-product-facts-stack-oriented-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - schema/product.schema.json
  - data/products/*.yaml
  - data/inventory.yaml
  - tests/test_phase_03.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Product facts such as brand and label-backed component amounts are copied into product cards before inventory shape migration can strip source fields."
    - "Inventory may still carry legacy brand/dose after this plan as a temporary source-of-truth bridge for Plan 03-02, but product cards already contain migrated known facts."
    - "Product cards record missing/ambiguous label facts as `unmatched_concerns` instead of guessed data."
    - "Every existing inventory `brand`, `dose`, and `notes` value is routed explicitly before Plan 03-02 strips inventory-owned fields."
    - "Planner behavior is unchanged by copying product metadata."
  artifacts:
    - path: "data/products/*.yaml"
      provides: "Concrete product label facts copied before inventory strip"
      contains: "brand:"
    - path: "tests/test_phase_03.py"
      provides: "Product fact ownership regressions"
      contains: "brand"
  key_links:
    - from: "tests/test_phase_03.py"
      to: "data/products/*.yaml"
      via: "product fact ownership assertions"
      pattern: "brand"
---

<objective>
Copy product facts out of inventory and into concrete product cards before inventory is rewritten.

Purpose: preserve existing brand/dose/label facts before the stack-oriented inventory migration strips non-inventory fields.
Output: product cards with known brand/label facts plus tests that prove facts are not lost or guessed.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-product-facts-stack-oriented-inventory/03-CONTEXT.md
@.planning/phases/02-substance-product-yaml-model-split/02-VERIFICATION.md
@schema/product.schema.json
@schema/inventory.schema.json
@data/products/*.yaml
@data/inventory.yaml
@tests/test_phase_02.py

<interfaces>
Current schema contracts:

- `schema/product.schema.json` already supports product `brand`, `notes`,
  `unmatched_concerns`, and component `label`, `amount`, `notes`.
- `schema/inventory.schema.json` still allows legacy per-item `brand` and `dose`
  at the start of this plan. Do not remove those fields until Plan 03-02.
</interfaces>

<review_feedback_to_address>
This plan incorporates `03-REVIEWS.md`.

Concrete routing policy before Plan 03-02 removes `brand` and `dose` from inventory:

| Inventory field | Source value | Target |
|-----------------|--------------|--------|
| `brand` | every known non-`unknown` value | copy to the referenced product card as `brand` |
| `brand` | `l_citrulline_malate: unknown`, `krill_oil: unknown` | do not write `brand: unknown`; record no product brand |
| `dose` | `vitamin_d3: 10000 IU` | product component `amount: 10000 IU` if treated as label fact; otherwise product `unmatched_concerns` with the exact source string |
| `dose` | `vitamin_b5: 15 mg` | product component `amount: 15 mg` if treated as label fact; otherwise product `unmatched_concerns` with the exact source string |
| `dose` | `magnesium_glycinate: 200 mg × 2/day (= 400 mg elemental Mg)` | inventory `notes` as operator usage; do not convert to product component amount |
| `dose` | `electrolyte_caps: 1 g/cap` | product `notes` or component amount only if label-backed; preserve exact source string |
| `dose` | `potassium_citrate: 99 mg elemental K` | product component `amount: 99 mg elemental K` if label-backed; otherwise product `unmatched_concerns` |
| `dose` | `lions_mane_b6_complex: 150 mg Lion's Mane + 1 mg B6` | already represented as component amounts; preserve exact values on product components |
| `dose` | `acetyl_l_carnitine: 1000 mg` | product component `amount: 1000 mg` if label-backed; otherwise product `unmatched_concerns` |
| `dose` | `astaxanthin: 6 mg` | product component `amount: 6 mg` if label-backed; otherwise product `unmatched_concerns` |
| `dose` | `nattokinase: 13000 FU` | product component `amount: 13000 FU` for nattokinase if label-backed; leave B6/B12 amounts unresolved unless label-backed |
| `dose` | `l_citrulline_malate: 5 g` | product component `amount: 5 g` if label-backed; otherwise product `unmatched_concerns` |
| `dose` | `tadalafil: 1.25 mg` | inventory `notes` as operator usage/off-label low-dose; do not convert to product component amount |
| `dose` | `creatine: 5 g` | product component `amount: 5 g` if label-backed; otherwise product `unmatched_concerns` |
| `dose` | `picamilon: 50 mg` | product component `amount: 50 mg` if label-backed; otherwise product `unmatched_concerns` |
| `dose` | `se_methyl_l_selenocysteine: 200 mcg` | product component `amount: 200 mcg` if label-backed; otherwise product `unmatched_concerns` |
| `dose` | `copper: 2 mg (Albion bisglycinate)` | product component `amount: 2 mg` plus `label`/`notes` for Albion bisglycinate if label-backed; otherwise product `unmatched_concerns` |
| `dose` | `l_carnitine_l_tartrate: 530 mg` | product component `amount: 530 mg` if label-backed; otherwise product `unmatched_concerns` |
| `dose` | `n_acetyl_cysteine: 600 mg` | product component `amount: 600 mg` if label-backed; otherwise product `unmatched_concerns` |
| `dose` | `krill_oil: 1 g` | product component `amount: 1 g` if label-backed; otherwise product `unmatched_concerns` |
| `dose` | `glycine: 1 g` | product component `amount: 1 g` if label-backed; otherwise product `unmatched_concerns` |
| `notes` | `coenzyme_b_complex: 240 caps; active/coenzymated B-vitamin forms; dose per cap not labelled granularly` | product `notes`; keep inventory `traits_override` only in inventory |
| `notes` | `trace_minerals: Multi-trace-mineral complex; per-cap weights not enumerated` | product `notes` or `unmatched_concerns`; keep narrow therapeutic window override in inventory |
| `notes` | `tadalafil: Off-label low-dose...` | inventory `notes`; this is operator usage/risk context |
| `notes` | `lions_mane: Standalone Lion's Mane...` | inventory `notes`; this is shelf/operator state |
| `notes` | `dihydroquercetin_complex: Dihydroquercetin...` | product `notes`; product composition fact |

Tests must prove the migration is complete, not spot-checked: the set of current known inventory brands must exactly match corresponding product-card `brand` values after copy.
</review_feedback_to_address>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add product fact preservation regressions</name>
  <files>tests/test_phase_03.py</files>
  <behavior>
    - Tests assert known product brands from current inventory are present on referenced product cards.
    - Tests assert product cards never use fake `brand: unknown`.
    - Tests assert unresolved per-component label amounts are concerns/notes instead of invented values.
    - Tests assert every current `dose` and `notes` source string is either represented on the product card or intentionally preserved as inventory notes for operator usage.
  </behavior>
  <action>
Create `tests/test_phase_03.py` using the existing YAML helper style.

Add tests that load current `data/inventory.yaml` and `data/products/*.yaml` and assert:
- every inventory item with a non-`unknown` brand has that exact brand on its referenced product card, using a completeness check over all current non-unknown inventory brands;
- no product card has `brand: unknown`;
- products with unresolved component amounts keep an explicit `notes` or `unmatched_concerns` entry rather than made-up `amount` values.
- the exact source strings for current `dose` and `notes` values are accounted for according to the routing table in this plan.

These tests should fail before product facts are copied to product cards.
  </action>
  <verify>
    <automated>uv run pytest tests/test_phase_03.py -q</automated>
  </verify>
  <done>Product fact preservation tests exist and fail before data migration.</done>
</task>

<task type="auto">
  <name>Task 2: Copy known product facts into product cards</name>
  <files>data/products/*.yaml, data/inventory.yaml</files>
  <action>
For every current inventory item whose brand is known and not `unknown`, copy that exact brand to the referenced product card.

Do not remove brand/dose from inventory in this plan. Plan 03-02 performs the destructive inventory strip after this preservation step is complete.

Route every current `dose` and `notes` source string according to the table in `<review_feedback_to_address>`.

Move label-backed component amounts into `components[].amount` only when the existing files make the label fact clear. If the value may be operator usage or the product serving label is not clearly backed by the current files, preserve the exact source string in product `unmatched_concerns` or product `notes`, except for explicitly operator-owned usage (`magnesium_glycinate`, `tadalafil`) which remains inventory `notes` until a real regimen model exists. Do not create `regimen.yaml`.

Examples:
- `lions_mane_b6_complex` already has label-backed component amounts and should keep them.
- Products with inventory `dose` but no clear product serving label should keep the ambiguity in product `notes` or `unmatched_concerns`, not guessed component amounts.

Do not write `brand: unknown` into product cards.
  </action>
  <verify>
    <automated>uv run planner.py check</automated>
    <automated>uv run pytest tests/test_phase_03.py -q</automated>
  </verify>
  <done>Known product facts are preserved on product cards before inventory migration.</done>
</task>

<task type="auto">
  <name>Task 3: Lock product schema support for copied facts</name>
  <files>schema/product.schema.json, tests/test_phase_03.py</files>
  <action>
Ensure `schema/product.schema.json` supports the fields used in product cards:
- product `brand`;
- component `label`;
- component `amount`;
- component `notes`;
- product `notes`;
- product `unmatched_concerns`.

Do not add broad vendor/marketplace/package modeling. If no schema change is needed, leave the schema unchanged and let the tests document the current contract.
  </action>
  <verify>
    <automated>uv run planner.py check</automated>
    <automated>uv run pytest tests/test_phase_03.py -q</automated>
  </verify>
  <done>Product schema supports copied facts without adding YAGNI metadata.</done>
</task>

</tasks>

<verification>
- `uv run planner.py check`
- `uv run pytest tests/test_phase_03.py -q`
</verification>

<success_criteria>
- Known product brands and label-backed facts are present in product cards before inventory is stripped.
- No product card has fake `brand: unknown`.
- Ambiguous label amounts are documented as notes/concerns, not guessed.
- Inventory source facts remain available for Plan 03-02 until the stack migration strips them.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inventory facts -> product facts | Migration must preserve existing source facts before later inventory stripping. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | T | product fact copy | mitigate | Tests assert all known inventory brands are copied to referenced product cards before stripping inventory. |
| T-03-01-02 | R | fake label facts | mitigate | Tests reject `brand: unknown` and require ambiguous label facts to remain notes/concerns. |
| T-03-01-03 | S | product metadata | mitigate | Planner check verifies product metadata does not alter scheduling validation. |
</threat_model>
