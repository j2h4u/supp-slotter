---
phase: 03-product-facts-stack-oriented-inventory
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - data/substances/*.yaml
  - data/products/coenzyme_b_complex.yaml
  - data/products/lions_mane_b6_complex.yaml
  - data/products/nattokinase.yaml
  - tests/test_phase_03.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Concrete B6 forms exist as separate substance cards: `b6_pyridoxal_5_phosphate` and `b6_pyridoxine_hcl`."
    - "Coenzyme B-Complex references `b6_pyridoxal_5_phosphate`."
    - "Lion's Mane + B6 Complex references `b6_pyridoxine_hcl`."
    - "No new `class:b_vitamin` or `family:vitamin_b6` trait is added merely for taxonomy."
    - "Any product with unresolved B6 form records the uncertainty instead of pretending the form is known."
  artifacts:
    - path: "data/substances/b6_pyridoxal_5_phosphate.yaml"
      provides: "P-5-P B6 substance"
      contains: "pyridoxal"
    - path: "data/substances/b6_pyridoxine_hcl.yaml"
      provides: "Pyridoxine HCl B6 substance"
      contains: "hydrochloride"
    - path: "tests/test_phase_03.py"
      provides: "B6 form regression tests"
      contains: "b6_pyridoxal_5_phosphate"
  key_links:
    - from: "data/products/coenzyme_b_complex.yaml"
      to: "data/substances/b6_pyridoxal_5_phosphate.yaml"
      via: "component substance ref"
      pattern: "b6_pyridoxal_5_phosphate"
    - from: "data/products/lions_mane_b6_complex.yaml"
      to: "data/substances/b6_pyridoxine_hcl.yaml"
      via: "component substance ref"
      pattern: "b6_pyridoxine_hcl"
---

<objective>
Split generic Vitamin B6 references into concrete product-label forms without adding unused taxonomy.

Purpose: avoid treating P-5-P and pyridoxine HCl as the same substance while keeping the trait model YAGNI.
Output: concrete B6 substance cards, product component refs updated to known forms, and regression tests.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-product-facts-stack-oriented-inventory/03-CONTEXT.md
@.planning/phases/03-product-facts-stack-oriented-inventory/03-02-SUMMARY.md
@data/substances/vitamin_b6.yaml
@data/products/coenzyme_b_complex.yaml
@data/products/lions_mane_b6_complex.yaml
@data/products/nattokinase.yaml
@tests/test_phase_03.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add B6 form regressions</name>
  <files>tests/test_phase_03.py</files>
  <behavior>
    - Tests assert P-5-P and pyridoxine HCl are distinct substance ids.
    - Tests assert products reference concrete B6 forms where the form is known.
    - Tests assert no broad B-vitamin class/family trait was added just for grouping.
  </behavior>
  <action>
Add tests that:
- require `data/substances/b6_pyridoxal_5_phosphate.yaml`;
- require `data/substances/b6_pyridoxine_hcl.yaml`;
- assert both cards have `traits: []` or no scheduling taxonomy traits unless a real planner rule was introduced;
- assert `coenzyme_b_complex` references `b6_pyridoxal_5_phosphate`;
- assert `lions_mane_b6_complex` references `b6_pyridoxine_hcl`;
- assert `class:b_vitamin` and `family:vitamin_b6` are not introduced for these new cards.
  </action>
  <verify>
    <automated>uv run pytest tests/test_phase_03.py -q</automated>
  </verify>
  <done>B6 form tests exist and fail before data migration.</done>
</task>

<task type="auto">
  <name>Task 2: Create concrete B6 form substance cards</name>
  <files>data/substances/b6_pyridoxal_5_phosphate.yaml, data/substances/b6_pyridoxine_hcl.yaml, data/substances/vitamin_b6.yaml</files>
  <action>
Create:

```yaml
id: b6_pyridoxal_5_phosphate
name: Vitamin B6 (pyridoxal-5-phosphate / P-5-P)
traits: []
```

and:

```yaml
id: b6_pyridoxine_hcl
name: Vitamin B6 (pyridoxine hydrochloride)
traits: []
```

Do not add `class:b_vitamin`, `family:vitamin_b6`, or another grouping trait unless this same plan also adds a concrete planner/validator/warning rule that uses it. The expected implementation is no new B6 grouping trait.

Remove or stop using the old generic `vitamin_b6` card only when all product references are resolved to concrete forms. If any current product's B6 form is not label-backed, keep the uncertainty explicit on that product rather than inventing a form.
  </action>
  <verify>
    <automated>uv run planner.py check</automated>
    <automated>uv run pytest tests/test_phase_03.py -q</automated>
  </verify>
  <done>Concrete B6 substance cards validate and do not introduce unused taxonomy.</done>
</task>

<task type="auto">
  <name>Task 3: Update product component refs to known B6 forms</name>
  <files>data/products/coenzyme_b_complex.yaml, data/products/lions_mane_b6_complex.yaml, data/products/nattokinase.yaml, tests/test_phase_03.py</files>
  <action>
Update known product component refs:
- `coenzyme_b_complex` uses `b6_pyridoxal_5_phosphate`.
- `lions_mane_b6_complex` uses `b6_pyridoxine_hcl`.

Preserve component label text so the product card remains readable, for example:

```yaml
- substance: b6_pyridoxal_5_phosphate
  label: Vitamin B6 (as pyridoxal-5-phosphate / P-5-P)
```

For `nattokinase`, do not guess the B6 form. If the current files do not contain label-backed form evidence, add a clear `unmatched_concerns` entry and leave the product in the smallest valid state that does not assert a false form. If a label-backed form is present in existing files, update it to the matching concrete B6 form.
  </action>
  <verify>
    <automated>uv run planner.py check</automated>
    <automated>uv run planner.py plan</automated>
    <automated>uv run pytest tests/test_phase_03.py -q</automated>
    <automated>uv run pytest tests/test_phase_02.py -q</automated>
  </verify>
  <done>Known B6 form refs are concrete, unresolved forms are explicit, and planner validation still passes.</done>
</task>

</tasks>

<verification>
- `uv run planner.py check`
- `uv run planner.py plan`
- `uv run pytest tests/test_phase_03.py -q`
- `uv run pytest tests/test_phase_02.py -q`
</verification>

<success_criteria>
- Concrete B6 form substance cards exist.
- Known product labels reference concrete B6 forms.
- No unused B-vitamin grouping taxonomy is added.
- Unresolved label form facts are documented as uncertainty, not guessed.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| product label facts -> substance refs | Product labels must map to concrete substances without inventing form facts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | T | B6 form mapping | mitigate | Tests assert known products reference the correct concrete B6 form ids. |
| T-03-03-02 | S | unresolved label form | mitigate | Unresolved product labels are recorded as concerns instead of false substance refs. |
| T-03-03-03 | R | unused taxonomy | mitigate | Tests assert no `class:b_vitamin` or `family:vitamin_b6` traits are introduced without a real rule. |
</threat_model>
