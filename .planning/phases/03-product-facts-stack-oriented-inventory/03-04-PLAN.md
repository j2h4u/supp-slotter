---
phase: 03-product-facts-stack-oriented-inventory
plan: 04
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - tests/test_phase_01.py
  - tests/test_phase_02.py
  - tests/test_phase_03.py
  - schedule.yaml
autonomous: true
requirements: []

must_haves:
  truths:
    - "Full validation passes after product fact preservation, stack-oriented inventory, and B6 form split."
    - "Schedule generation still schedules inventory item ids as inseparable products."
    - "Regression tests cover no product facts in inventory, product facts in product cards, concrete B6 form refs, and previous Phase 1/2 topology."
    - "No `regimen.yaml` or broad unused B-vitamin taxonomy was introduced."
  artifacts:
    - path: "tests/test_phase_03.py"
      provides: "Phase 3 regression suite"
      contains: "stack"
    - path: "schedule.yaml"
      provides: "Regenerated schedule after Phase 3 data model cleanup"
      contains: "quality"
  key_links:
    - from: "tests/test_phase_03.py"
      to: "data/inventory.yaml"
      via: "stack-oriented inventory assertions"
      pattern: "stacks"
    - from: "tests/test_phase_03.py"
      to: "data/products/*.yaml"
      via: "product fact ownership assertions"
      pattern: "brand"
---

<objective>
Run final Phase 3 regression and smoke verification.

Purpose: prove the data ownership cleanup did not break the verified Phase 2 planner behavior.
Output: updated regression coverage, regenerated schedule, and final validation evidence.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-product-facts-stack-oriented-inventory/03-CONTEXT.md
@.planning/phases/03-product-facts-stack-oriented-inventory/03-01-SUMMARY.md
@.planning/phases/03-product-facts-stack-oriented-inventory/03-02-SUMMARY.md
@.planning/phases/03-product-facts-stack-oriented-inventory/03-03-SUMMARY.md
@tests/test_phase_01.py
@tests/test_phase_02.py
@tests/test_phase_03.py
@planner.py
@schedule.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen final invariant tests</name>
  <files>tests/test_phase_01.py, tests/test_phase_02.py, tests/test_phase_03.py</files>
  <action>
Review Phase 1/2 tests after the new inventory shape and B6 split. Keep existing behavioral assertions but update helpers to the new stack-oriented inventory shape.

Ensure Phase 3 tests assert:
- no `regimen.yaml` exists;
- inventory has top-level stacks;
- inventory entries do not contain product facts;
- product cards carry known brands;
- concrete B6 forms are used where known;
- no unused `class:b_vitamin` or `family:vitamin_b6` trait is introduced.
  </action>
  <verify>
    <automated>uv run pytest tests/test_phase_01.py tests/test_phase_02.py tests/test_phase_03.py -q</automated>
  </verify>
  <done>Regression suite captures Phase 3 invariants and previous topology guarantees.</done>
</task>

<task type="auto">
  <name>Task 2: Run final check and regenerate schedule</name>
  <files>schedule.yaml</files>
  <action>
Run the final planner commands:

```bash
uv run planner.py check
uv run planner.py plan
```

Commit the regenerated `schedule.yaml` only if it changes. The schedule should still contain inventory item ids and product/component explanations, not standalone split components.
  </action>
  <verify>
    <automated>uv run planner.py check</automated>
    <automated>uv run planner.py plan</automated>
  </verify>
  <done>Planner check passes and schedule is regenerated from Phase 3 data.</done>
</task>

<task type="auto">
  <name>Task 3: Full suite and artifact consistency</name>
  <files>tests/test_phase_03.py, schedule.yaml</files>
  <action>
Run the full regression suite and inspect generated schedule for:
- stack partition preserved;
- inactive items absent;
- products remain inseparable;
- warnings still include product/item context.

If any existing tests were accidentally weakened during migration, strengthen them before completing the phase.
  </action>
  <verify>
    <automated>uv run pytest</automated>
    <automated>uv run planner.py check</automated>
  </verify>
  <done>Full suite and planner check pass after all Phase 3 changes.</done>
</task>

</tasks>

<verification>
- `uv run planner.py check`
- `uv run planner.py plan`
- `uv run pytest`
</verification>

<success_criteria>
- Full planner validation passes.
- Full pytest suite passes.
- Schedule output remains product/inventory-item based.
- Phase 3 KISS constraints are preserved: no regimen file, no unused B6 taxonomy.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data cleanup -> verified planner behavior | Ownership cleanup must not regress scheduler semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | R | regression coverage | mitigate | Full pytest and final invariant tests prove previous behavior remains covered. |
| T-03-04-02 | T | generated schedule | mitigate | `uv run planner.py plan` regenerates deterministic output after validation. |
| T-03-04-03 | S | product/component semantics | mitigate | Tests inspect schedule product/component explanations and inactive/stack topology. |
</threat_model>
