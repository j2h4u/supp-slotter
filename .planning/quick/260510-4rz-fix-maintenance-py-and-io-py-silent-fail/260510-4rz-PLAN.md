---
phase: quick-260510-4rz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - planner/io.py
  - planner/maintenance.py
  - planner/cards/relations.py
  - tests/test_maintenance.py
autonomous: true
requirements: [C1, EH1, EH2, EH7, EH9, EH10]
must_haves:
  truths:
    - "load_yaml errors carry the file path in their message instead of bare yaml/OS tracebacks"
    - "load_schema errors name the schema file that failed to load or parse"
    - "Stacks YAML write failures during auto-maintenance produce a clear error pointing at stacks.yaml and exit non-zero, not a raw traceback"
    - "Skipped cards in rewrite_substance_refs print a stderr warning naming the path and the CardLoadError reason"
    - "load_global_relations prints a stderr warning when relations.yaml top-level is not a mapping (instead of silently returning [])"
    - "auto_maintenance_needed distinguishes 'no work needed' (False) from 'card load failed' (None), and run_auto_maintenance_unlocked aborts cleanly on the error signal"
  artifacts:
    - path: "planner/io.py"
      provides: "load_yaml + load_schema with descriptive error wrapping"
      contains: "CardLoadError"
    - path: "planner/maintenance.py"
      provides: "guarded stacks.yaml write, vocal CardLoadError skips, error-vs-no-work return signal"
      contains: "auto_maintenance_needed"
    - path: "planner/cards/relations.py"
      provides: "vocal load_global_relations on non-mapping data"
      contains: "warning"
    - path: "tests/test_maintenance.py"
      provides: "Regression coverage for the new failure modes"
  key_links:
    - from: "planner/io.py:load_yaml"
      to: "planner.contracts.CardLoadError"
      via: "except (OSError, yaml.YAMLError) → raise CardLoadError"
      pattern: "CardLoadError"
    - from: "planner/maintenance.py:run_auto_maintenance_unlocked"
      to: "planner/maintenance.py:auto_maintenance_needed"
      via: "None return value triggers non-zero exit"
      pattern: "auto_maintenance_needed"
---

<objective>
Close 6 silent-failure / misleading-sentinel findings flagged by the code-quality review pass on the current `planner/` package: C1 (unguarded stacks.yaml write), EH1/EH2 (raw tracebacks from `load_yaml` / `load_schema`), EH7 (silent CardLoadError continues), EH9 (silent `[]` on bad relations.yaml), EH10 (False conflates "no work needed" with "load error").

Purpose: every error path either exits non-zero with an operator-readable message naming the offending file, or emits a stderr warning explaining what was skipped. No raw `FileNotFoundError`/`yaml.YAMLError`/`OSError`/`JSONDecodeError` should reach the user from these call sites.

Output: tightened error handling in `planner/io.py`, `planner/maintenance.py`, `planner/cards/relations.py`, plus regression tests. EH8 (lock TOCTOU) is intentionally deferred per scope constraints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@planner/io.py
@planner/maintenance.py
@planner/cards/relations.py
@planner/engine/check.py
@planner/contracts.py

<interfaces>
<!-- Existing contracts the executor must use directly -->

From `planner/contracts.py` (already imported by io.py via late-binding):
```python
class CardLoadError(Exception):
    def __init__(self, path: Path, message: str) -> None:
        self.path = path
        self.message = message
        super().__init__(message)
```
- Pattern in current code: `raise CardLoadError(path, f"{path}: <reason>")` — message ALWAYS prefixed with the path string so `display_message(e.message)` (called by check.py / __main__) renders relative paths.

From `planner/io.py`:
```python
def load_yaml(path: Path) -> object: ...
def load_yaml_mapping(path: Path) -> dict[str, Any]: ...   # already wraps load_yaml + isinstance dict guard, raises CardLoadError
def load_schema(name: str) -> dict[str, Any]: ...
def display_message(message: str) -> str: ...              # strips ROOT prefix for friendly paths
```

From `planner/maintenance.py`:
```python
def auto_maintenance_needed(data_dir: Path = DATA_DIR) -> bool: ...   # SIGNATURE CHANGES to bool | None in Task 4
def run_auto_maintenance(data_dir: Path = DATA_DIR, *, suppress_output: bool = False) -> int: ...
def run_auto_maintenance_unlocked(data_dir: Path = DATA_DIR, *, suppress_output: bool = False) -> int: ...
def rewrite_substance_refs(data_dir: Path, substance_renames: dict[str, str]) -> None: ...
```

Existing stderr-warning pattern used in this codebase (match the wording shape):
```python
print(f"warning: could not write {path}: {e}", file=sys.stderr)
```
And the more recent `_normalize_card_dir` pattern:
```python
print(f"ERROR: {display_message(e.message)}", file=sys.stderr)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wrap load_yaml and load_schema with CardLoadError / RuntimeError on I/O and parse failures (EH1, EH2)</name>
  <files>planner/io.py, tests/test_maintenance.py</files>
  <behavior>
    - `load_yaml(missing_path)` → raises `CardLoadError` whose `.message` starts with the offending path (not bare `FileNotFoundError`).
    - `load_yaml(path_with_malformed_yaml)` → raises `CardLoadError` whose `.message` names the path AND mentions YAML parse context (not bare `yaml.YAMLError`).
    - `load_yaml(valid_yaml)` returns the parsed object unchanged (any type) — back-compat for non-mapping callers like `load_yaml(slots_path)` in check.py.
    - `load_schema("nonexistent")` → raises `RuntimeError` whose message names `<SCHEMA_DIR>/nonexistent.schema.json` (not bare `FileNotFoundError`).
    - `load_schema("malformed_json")` → raises `RuntimeError` whose message names the schema file AND mentions JSON parse context (not bare `JSONDecodeError`).
    - Existing happy-path tests in tests/ continue to pass unchanged.
  </behavior>
  <action>
    1. In `planner/io.py`, modify `load_yaml(path: Path) -> object`:
       - Move the `from planner.contracts import CardLoadError` import to module-level (top of file). It is already used by `load_yaml_mapping`; promoting it avoids re-importing inside `load_yaml`. There is no import cycle: `planner/contracts.py` is a leaf module that does not import from `planner.io`.
       - Wrap `path.read_text()` in `try/except OSError as e: raise CardLoadError(path, f"{path}: {e}") from e`. This catches `FileNotFoundError`, `PermissionError`, etc.
       - Wrap `yaml.safe_load(...)` in `try/except yaml.YAMLError as e: raise CardLoadError(path, f"{path}: invalid YAML: {e}") from e`.
       - Keep the return type `object` and return the parsed value unchanged on the happy path. Do NOT add a mapping guard here — that is `load_yaml_mapping`'s job, and check.py / validate_schemas rely on `load_yaml` returning lists/scalars too.
    2. In `planner/io.py`, modify `load_schema(name: str) -> dict[str, Any]`:
       - Compute `schema_path = SCHEMA_DIR / f"{name}.schema.json"` once.
       - Wrap `schema_path.read_text()` in `try/except OSError as e: raise RuntimeError(f"could not read schema {schema_path}: {e}") from e`.
       - Wrap `json.loads(...)` in `try/except json.JSONDecodeError as e: raise RuntimeError(f"could not parse schema {schema_path}: {e}") from e`.
       - Keep the cast and return shape unchanged on the happy path.
    3. Update `planner/io.py`'s `load_yaml_mapping` to drop its now-redundant local `from planner.contracts import CardLoadError` since the import is module-level.
    4. Add tests to `tests/test_maintenance.py` (or wherever suite already exercises `load_yaml`; if no existing module fits, add to `tests/test_maintenance.py` since this PLAN already touches it):
       - `test_load_yaml_missing_file_raises_card_load_error`: write nothing, call `load_yaml(tmp_path / "absent.yaml")`, assert `CardLoadError` raised and `str(absent.yaml path)` appears in `e.message`.
       - `test_load_yaml_malformed_yaml_raises_card_load_error`: write `":\n  - bad: ["` (any clearly invalid YAML), assert `CardLoadError` raised and `"invalid YAML"` substring is in `e.message`.
       - `test_load_schema_missing_raises_runtime_error_naming_schema`: monkeypatch `planner.io.SCHEMA_DIR` to `tmp_path`, call `load_schema("nope")`, assert `RuntimeError` whose `str(exc)` contains `nope.schema.json`.
       - `test_load_schema_malformed_json_raises_runtime_error`: write `tmp_path / "bad.schema.json"` with `"{not json"`, monkeypatch `SCHEMA_DIR` to `tmp_path`, assert `RuntimeError` whose message names the schema file.
    5. Run the test suite: `cd /home/j2h4u/repos/j2h4u/supp-slotter && python -m pytest tests/ -x -q`. The four new tests must pass; existing tests must not regress.
    6. Commit: `fix(io): convert load_yaml and load_schema raw errors into descriptive CardLoadError/RuntimeError`
  </action>
  <verify>
    <automated>cd /home/j2h4u/repos/j2h4u/supp-slotter && python -m pytest tests/ -x -q -k "load_yaml or load_schema or maintenance" && python -m pyright planner/ --outputjson | python -m json.tool | grep -E '"error"|"warning"' | wc -l</automated>
  </verify>
  <done>
    All four new tests pass; full pytest suite remains green; pyright error+warning count is 0; commit landed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard stacks.yaml write in run_auto_maintenance_unlocked (C1)</name>
  <files>planner/maintenance.py, tests/test_maintenance.py</files>
  <behavior>
    - When `stacks_path.write_text(...)` raises `OSError` after substance/product renames have committed, `run_auto_maintenance_unlocked` prints a single stderr line naming `stacks_path` and the underlying error, and returns `1` (not a raw traceback).
    - Happy path (stacks.yaml writable) is unchanged: function still returns 0 and the rewritten YAML lands on disk.
    - The error message is explicit that substance/product renames already happened, so the operator knows the on-disk state needs reconciliation. (E.g. `"auto-maintenance: stacks.yaml write failed AFTER substance/product renames committed; reconcile manually: {e}"`.)
  </behavior>
  <action>
    1. In `planner/maintenance.py`, locate `run_auto_maintenance_unlocked` (currently lines ~373–428). Find the block:
       ```python
       if stacks_path.exists() and product_renames:
           stacks_data = load_yaml(stacks_path)
           if isinstance(stacks_data, dict):
               rewrite_stack_product_refs(cast(dict[str, Any], stacks_data), product_renames)
               stacks_path.write_text(
                   yaml.safe_dump(...)
               )
       ```
    2. Wrap the `stacks_path.write_text(...)` call in `try/except OSError as e:` and on failure:
       - `print(f"auto-maintenance: failed to write {stacks_path} after product renames committed; reconcile stacks.yaml manually: {e}", file=sys.stderr)`
       - `return 1`
       Use `display_message(str(stacks_path))` for the path token to keep the output relative-rooted, matching `_normalize_card_dir`.
    3. Note: `load_yaml(stacks_path)` itself can now raise `CardLoadError` (Task 1). That's the right behavior — a corrupted stacks.yaml at this point should bubble up rather than silently skip rewriting. Do NOT swallow it. (If a test reveals `cmd_check` callers can't tolerate this, escalate before suppressing.)
    4. Add a regression test in `tests/test_maintenance.py`:
       - `test_run_auto_maintenance_unlocked_returns_1_when_stacks_write_fails`: build a minimal data tree under `tmp_path` (substances/, products/, stacks.yaml) where one product needs renaming so the stacks-write branch is reached; monkeypatch `Path.write_text` (or use chmod 0o444 on stacks.yaml) so the write raises `OSError`; assert return value is `1` and the stderr message contains `stacks.yaml`.
       - Use `capfd` / `capsys` to capture stderr.
       - The test should also assert that the substance/product files DID get renamed (verifying the C1 root concern: writes are not atomic, the OS-level inconsistency is now at least announced).
    5. Run tests: `cd /home/j2h4u/repos/j2h4u/supp-slotter && python -m pytest tests/ -x -q`.
    6. Commit: `fix(maintenance): catch OSError on stacks.yaml write and exit non-zero with a reconciliation hint`
  </action>
  <verify>
    <automated>cd /home/j2h4u/repos/j2h4u/supp-slotter && python -m pytest tests/ -x -q -k "stacks_write or maintenance" && python -m pyright planner/ --outputjson | python -m json.tool | grep -E '"error"|"warning"' | wc -l</automated>
  </verify>
  <done>
    Stacks write failure path produces operator-readable stderr + return 1; regression test passes; pyright clean; commit landed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make rewrite_substance_refs and load_global_relations vocal on skip (EH7, EH9)</name>
  <files>planner/maintenance.py, planner/cards/relations.py, tests/test_maintenance.py</files>
  <behavior>
    - The three `except CardLoadError: continue` sites in `rewrite_substance_refs` (products dir, dashboards dir, substances dir) each emit a stderr line of shape `f"warning: skipping {path}: {e.message}"` before continuing.
    - `load_global_relations` emits a stderr warning when `relations.yaml` parses but is not a top-level mapping (e.g. user wrote a list by accident). The warning names the path and the actual top-level type. Return value remains `[]` so callers don't crash, but the user is informed.
    - Existing happy paths (clean cards, mapping-typed relations.yaml) emit no extra stderr noise.
  </behavior>
  <action>
    1. In `planner/maintenance.py:rewrite_substance_refs`, locate the three sites at lines ~220–222 (products), ~250–252 (dashboards), ~280–282 (substances). For each:
       - Replace `except CardLoadError: continue` with:
         ```python
         except CardLoadError as e:
             print(f"warning: skipping {path}: {display_message(e.message)}", file=sys.stderr)
             continue
         ```
       - `display_message` is already imported at the top of `maintenance.py`.
    2. In `planner/cards/relations.py:load_global_relations` (lines 15–19), modify the non-mapping branch:
       ```python
       data = load_yaml(RELATIONS_PATH)
       if not isinstance(data, dict):
           print(
               f"warning: {RELATIONS_PATH}: expected mapping, got {type(data).__name__}; "
               "ignoring relation-based warnings",
               file=sys.stderr,
           )
           return []
       ```
       Add `import sys` to relations.py if not already present (check before adding — current file has no `sys` import, so add `import sys` near the top alongside other stdlib imports).
       Use the bare `RELATIONS_PATH` (already imported); do not add `display_message` to relations.py just for this — relations.py historically uses raw path strings in `check_global_relations` error output, keep consistent.
    3. Add tests to `tests/test_maintenance.py`:
       - `test_rewrite_substance_refs_warns_on_corrupted_product`: in `tmp_path`, create one valid substance file (with rename mapping) and one corrupted product YAML (e.g. `"id: [bad nested"`). Call `rewrite_substance_refs(tmp_path, {"old_id": "new_id"})`. Assert stderr captured contains `"warning: skipping"` and the product filename.
       - `test_load_global_relations_warns_on_non_mapping`: write `tmp_path / "relations.yaml"` containing `"- a list at top level\n"`. Monkeypatch `planner.cards.relations.RELATIONS_PATH` to that path. Assert return value `== []` AND stderr contains `"expected mapping, got list"`.
       - Optional micro-test `test_load_global_relations_quiet_on_mapping`: ensure no stderr is emitted on a valid mapping file.
    4. Run tests: `cd /home/j2h4u/repos/j2h4u/supp-slotter && python -m pytest tests/ -x -q`.
    5. Commit: `fix(maintenance,relations): print stderr warning before silent CardLoadError skip and on non-mapping relations.yaml`
  </action>
  <verify>
    <automated>cd /home/j2h4u/repos/j2h4u/supp-slotter && python -m pytest tests/ -x -q -k "rewrite_substance_refs or load_global_relations" && python -m pyright planner/ --outputjson | python -m json.tool | grep -E '"error"|"warning"' | wc -l</automated>
  </verify>
  <done>
    Three CardLoadError skips produce stderr lines naming path+reason; non-mapping relations.yaml warns once; existing pytest stays green; pyright 0/0/0; commit landed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Disambiguate auto_maintenance_needed: None for error, False for "no work" (EH10)</name>
  <files>planner/maintenance.py, tests/test_maintenance.py</files>
  <behavior>
    - `auto_maintenance_needed` returns:
      - `True` — at least one card needs renaming or id assignment (existing behavior).
      - `False` — all cards conform; no work needed (existing behavior).
      - `None` — a `CardLoadError` was raised while reading a card; this is an error, not evidence that maintenance is unnecessary.
    - `run_auto_maintenance` (the locking caller) treats `None` as a hard error: prints a stderr line naming the failure mode and returns `1` WITHOUT acquiring the maintenance lock and WITHOUT calling `run_auto_maintenance_unlocked`.
    - Existing call site `run_auto_maintenance(suppress_output=True)` from `cmd_check` (engine/check.py:32) propagates the non-zero return up and `cmd_check` already prints the standard `"check: skipped (maintenance lock held)"` fallback message — verify whether that fallback is still appropriate for the new error case, and if not, leave a follow-up note (do NOT modify check.py in this task; flag it for the SUMMARY if the wording is misleading).
    - Type hint signature changes from `bool` to `bool | None`. Pyright must remain clean.
  </behavior>
  <action>
    1. Open `planner/maintenance.py:auto_maintenance_needed` (lines ~322–357). Change signature:
       ```python
       def auto_maintenance_needed(data_dir: Path = DATA_DIR) -> bool | None:
       ```
       Update the docstring to explicitly document the three-valued return: `True` = work needed, `False` = no work, `None` = card load failed (caller should abort).
    2. Modify the two `except CardLoadError: return False` sites (substance loop ~line 336, product loop ~line 348):
       ```python
       except CardLoadError as e:
           print(
               f"auto-maintenance: could not read {path}: {display_message(e.message)}",
               file=sys.stderr,
           )
           return None
       ```
    3. Open `run_auto_maintenance` (~line 359). It currently does:
       ```python
       if auto_maintenance_needed(data_dir):
           if not acquire_maintenance_lock(...):
               return 1
           lock_acquired = True
       ```
       Replace with explicit three-valued handling:
       ```python
       needs = auto_maintenance_needed(data_dir)
       if needs is None:
           return 1
       if needs:
           if not acquire_maintenance_lock(data_dir.parent / MAINTENANCE_LOCK_DIR.name):
               return 1
           lock_acquired = True
       ```
    4. Audit other callers of `auto_maintenance_needed`:
       - `cd /home/j2h4u/repos/j2h4u/supp-slotter && rg -n "auto_maintenance_needed" --type py`
       - Update each caller that uses it in a boolean context to handle the `None` case explicitly. If only the call site inside `run_auto_maintenance` exists, that's fine — no other changes needed.
    5. Audit pyright surface: any remaining `if auto_maintenance_needed(...):` style truthy check on the new tri-state return is acceptable for `if`/`if not` (None is falsy), but **identity comparison** is required wherever you need to distinguish None from False.
    6. Add tests in `tests/test_maintenance.py`:
       - `test_auto_maintenance_needed_returns_none_on_card_load_error`: in `tmp_path`, write `substances/broken.yaml` with malformed YAML so `load_card_mapping` raises `CardLoadError`. Call `auto_maintenance_needed(tmp_path)` and assert the return value is `None` (use `assert result is None`, not `not result`).
       - `test_run_auto_maintenance_returns_1_without_acquiring_lock_on_load_error`: same setup; call `run_auto_maintenance(tmp_path, suppress_output=True)`, assert return is `1`, assert the lock directory was NOT created (`assert not (tmp_path.parent / MAINTENANCE_LOCK_DIR.name).exists()`).
       - `test_auto_maintenance_needed_still_returns_false_when_clean`: existing happy-path coverage — if it doesn't already exist, add a minimal version with one well-formed substance + product whose filenames already match canonical form.
    7. Run tests: `cd /home/j2h4u/repos/j2h4u/supp-slotter && python -m pytest tests/ -x -q`.
    8. Verify pyright stays at 0 errors / 0 warnings.
    9. Commit: `fix(maintenance): return None from auto_maintenance_needed on CardLoadError; abort lock acquisition on error`
  </action>
  <verify>
    <automated>cd /home/j2h4u/repos/j2h4u/supp-slotter && python -m pytest tests/ -x -q -k "auto_maintenance_needed or run_auto_maintenance" && python -m pyright planner/ --outputjson | python -m json.tool | grep -E '"error"|"warning"' | wc -l</automated>
  </verify>
  <done>
    Three-valued return is documented and tested; lock is not acquired on error; full pytest green; pyright 0/0/0; commit landed.
  </done>
</task>

</tasks>

<verification>
After all four tasks land:

1. **Full suite green:**
   ```
   cd /home/j2h4u/repos/j2h4u/supp-slotter && python -m pytest tests/ -x -q
   ```

2. **Pyright clean:**
   ```
   cd /home/j2h4u/repos/j2h4u/supp-slotter && python -m pyright planner/ --outputjson | python -m json.tool | grep -E '"error"|"warning"' | wc -l
   ```
   Expected output: `0`.

3. **Manual smoke (optional, only if time):** corrupt `data/substances/<one>.yaml` deliberately, run `python -m planner check`, confirm the output names the file and exits non-zero rather than tracebacking. Restore the file.

4. **Grep for raw silent-fail patterns** to confirm none remain in the modified scope:
   ```
   rg -n "except CardLoadError:\s*$" planner/maintenance.py    # expected: no matches in rewrite_substance_refs
   rg -n "except CardLoadError:\s*\n\s*return False" planner/maintenance.py  # expected: no matches in auto_maintenance_needed
   ```
</verification>

<success_criteria>
- `load_yaml` and `load_schema` errors carry the file path; raw `FileNotFoundError`/`yaml.YAMLError`/`JSONDecodeError`/`OSError` no longer surface from these call sites.
- `run_auto_maintenance_unlocked` exits 1 with a clear message on `stacks.yaml` write failure.
- All three `rewrite_substance_refs` skip paths emit a stderr warning with file path and reason.
- `load_global_relations` warns on non-mapping top-level instead of returning `[]` silently.
- `auto_maintenance_needed` returns `None` on `CardLoadError`; `run_auto_maintenance` aborts cleanly without acquiring the lock.
- New regression tests cover each of the above paths.
- `python -m pytest tests/ -x -q` is green; `python -m pyright planner/` reports 0 errors and 0 warnings.
- EH8 (lock-clear TOCTOU) explicitly deferred — note in SUMMARY for the next quick-task pass.
</success_criteria>

<output>
After completion, create `.planning/quick/260510-4rz-fix-maintenance-py-and-io-py-silent-fail/260510-4rz-SUMMARY.md` covering:
- Per-finding status (C1, EH1, EH2, EH7, EH9, EH10) with commit SHA and the test that locks each one.
- Explicit note that EH8 (clear_stale_lock TOCTOU) was deferred per scope; flag it as a candidate for the next pass.
- Any callers of `auto_maintenance_needed` outside `run_auto_maintenance` that needed updating (or confirmation that none did).
- Whether the `cmd_check` "check: skipped (maintenance lock held)" message is still accurate when the underlying failure is a card load error rather than a held lock — flag for follow-up if misleading.
- Final pyright count and total tests added.
</output>
