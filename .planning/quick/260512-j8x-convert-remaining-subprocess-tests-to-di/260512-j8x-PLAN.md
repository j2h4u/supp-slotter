---
phase: quick-260512-j8x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_phase_02.py
  - tests/test_phase_03.py
  - planner/engine/results.py
  - planner/engine/plan.py
autonomous: true
requirements: [j8x-01, j8x-02, j8x-03]

must_haves:
  truths:
    - "test_phase_02.py contains no subprocess (`run_planner`) calls except `test_cli_help_exposes_simple_agent_commands`."
    - "test_phase_03.py contains no subprocess (`run_planner`) calls except the 4 review-substance CLI-contract tests (rejects_missing_file, rejects_path_outside_substances_dir, rejects_non_yaml_suffix, rejects_empty_traits_file)."
    - "`uv run pytest tests/test_phase_02.py tests/test_phase_03.py` passes (all current assertions hold under the new direct calls)."
    - "`uv run pyright` reports 0 errors / 0 warnings / 0 informations (no `type: ignore`, no rule downgrade)."
    - "Tests asserting on the workout-slot rejection text inspect a structured field on `PlanResult`, not redirected stderr."
  artifacts:
    - path: tests/test_phase_02.py
      provides: "Direct cmd_check / cmd_plan calls via rewritten `check_in_temp_dir` and `plan_in_temp_dir` helpers."
    - path: tests/test_phase_03.py
      provides: "Direct cmd_check / cmd_plan calls in 7 listed tests."
    - path: planner/engine/results.py
      provides: "`PlanResult.errors: list[str]` field for non-warning failure messages (workout-slot rejection, etc.)."
    - path: planner/engine/plan.py
      provides: "Populates `PlanResult.errors` from the existing stderr-print paths that return `None` from `_build_active_index`."
  key_links:
    - from: tests/test_phase_02.py
      to: planner.engine.cmd_check / planner.engine.cmd_plan
      via: "direct function call inside `check_in_temp_dir` / `plan_in_temp_dir`"
      pattern: "cmd_(check|plan)\\(data_root=tmp_path\\)"
    - from: tests/test_phase_03.py
      to: planner.engine.cmd_check / planner.engine.cmd_plan
      via: "direct function call replacing `run_planner(\"check\", root=tmp_path)` / `run_planner(root=tmp_path)`"
      pattern: "cmd_(check|plan)\\(data_root=tmp_path\\)"
    - from: tests/test_phase_03.py::test_workout_activity_product_is_not_scheduled_as_daily
      to: PlanResult.errors
      via: "assert against errors list rather than stderr string"
      pattern: "result\\.errors"
---

<objective>
Finish the test-suite subprocess-elimination work started in quick task 260512-ib5. Convert the remaining `run_planner(...)` calls in `tests/test_phase_02.py` (3 direct + 2 helper-internal) and `tests/test_phase_03.py` (7 explicitly enumerated) to direct calls into `planner.engine.cmd_check` / `cmd_plan`. Keep CLI-contract tests (--help, review-substance path/suffix/missing-file/empty-traits) on `run_planner` — those still test the argparse layer and stderr formatting.

Purpose: Bring all non-CLI-contract test runs in-process so they execute faster, give better tracebacks on failure, and assert against typed dataclass fields instead of substring-matching stdout/stderr.

Output: Two test files using `cmd_*` directly for engine assertions; a small extension to `PlanResult` so the workout-slot error path is observable without scraping stderr.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@tests/test_phase_02.py
@tests/test_phase_03.py
@tests/helpers.py
@planner/engine/__init__.py
@planner/engine/results.py
@planner/engine/check.py
@planner/engine/plan.py

# Background — what 260512-ib5 already did
- Added `CheckResult`, `PlanResult`, `DoctorResult`, `FindResult`, `ReviewResult`, `ShowResult`, `AuditResult` dataclasses in `planner/engine/results.py`.
- Made every `cmd_*` return its result dataclass.
- Converted 10 subprocess tests in `tests/test_phase_03.py` to direct `cmd_*` calls.
- Established the rule: `run_planner` is only for CLI-contract tests (argparse, --help, path/suffix validation, stderr formatting).

# What still uses subprocess after ib5

## tests/test_phase_02.py — 5 call sites, all but `--help` to convert
1. `test_cli_help_exposes_simple_agent_commands` — KEEP (tests --help output).
2. `plan_in_temp_dir` helper (called by ~8 tests) → must call `cmd_plan(data_root=tmp_path)` and continue reading `schedule.yaml` from disk (cmd_plan already writes it via `dump_schedule_yaml`).
3. `check_in_temp_dir` helper (called by 3 tests) → must call `cmd_check(data_root=tmp_path)` and return `CheckResult` instead of `RunResult`.
4–6. Three tests using `check_in_temp_dir` need their assertions retargeted from `result.stdout + result.stderr` to `CheckResult.errors` / `exit_code`.

## tests/test_phase_03.py — 7 enumerated tests to convert (KEEP the 4 review-substance CLI tests)
- `test_check_auto_renames_files_when_names_change` → cmd_check; assert exit_code == 0; the file-rename assertions stay (they read disk).
- `test_check_warns_about_products_without_stack_entry` → cmd_check; assert exit_code == 0 and the warning string appears in `result.info` (info, not errors — the test currently asserts `returncode == 0` which means it's a non-fatal report).
- `test_duplicate_stack_item_across_stacks_is_rejected` → cmd_check; assert exit_code != 0 and both `"prd_eb6337a6dc"` and `"multiple stacks"` appear in `"\n".join(result.errors)`.
- `test_auto_maintenance_lock_only_blocks_mutations` → cmd_check twice; assert first exit_code == 0; second exit_code != 0 and `"another planner process is running"` in `"\n".join(result.errors)`.
- `test_workout_activity_product_is_not_scheduled_as_daily` → cmd_plan; assert exit_code != 0 and `"prd_cfce0b36b6"` + `"has no workout pillbox slots"` in `"\n".join(result.errors)` (requires the `PlanResult.errors` field added in Task 1).
- `test_duplicate_slot_ids_across_pillboxes_are_rejected` → cmd_check; assert exit_code != 0 and `"slot id 'morning_food'"` + `"unique across pillboxes"` in `"\n".join(result.errors)`.
- `test_relation_validation_rejects_unknown_substance_name` → cmd_check; assert exit_code != 0 and `"source_name 'Definitely Missing' has no matching substance name"` in `"\n".join(result.errors)`.

# CheckResult is sufficient as-is
`CheckResult` already has `errors: list[str]` and `info: list[str]`. Inspecting `planner/engine/check.py` confirms `report(errors, info)` paths and `CheckResult(exit_code=..., errors=..., info=...)` returns flow through; no extension needed.

# PlanResult needs one new field
`planner/engine/plan.py` lines ~199-207 print the workout-slot rejection message to stderr and then return `None` from `_build_active_index`, which causes `cmd_plan` to return a `PlanResult` with exit_code != 0. The error text is currently lost. Add `errors: list[str]` to `PlanResult` (default empty list) and route the existing stderr-print sites in `_build_active_index` and similar early-bailout sites through a small helper that both prints (preserving CLI behaviour for humans) AND appends to a collected errors list that the surrounding `cmd_plan` returns inside `PlanResult.errors`.

Required pattern (no scope creep — only sites that already print-to-stderr-and-return-None):
- `"plan: no non-inactive stack items."` (line 191)
- `"plan: stack item '{item_id}' has {activity_traits} but stack '...' has no workout pillbox slots."` (lines 202–206)
- Any other print-then-return-None in plan.py uncovered while editing (verify with `rg -n "file=sys.stderr" planner/engine/plan.py`).

# Constraints
- pyright strict must remain 0/0/0. No `type: ignore`, no rule downgrade, no broad `# noqa`.
- Do NOT touch `tests/helpers.py` — the 4 review-substance kept tests + the `--help` test still use `run_planner`/`RunResult`.
- Do NOT change CLI behaviour: stderr prints stay; we're adding a parallel structured capture, not replacing the human-readable path.
- One-liner sanity grep before commit: `rg -n 'run_planner' tests/test_phase_02.py tests/test_phase_03.py` must return only the 5 kept lines (1 in phase_02 = --help, 4 in phase_03 = review-substance CLI tests).
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add PlanResult.errors and convert tests/test_phase_02.py</name>
  <files>planner/engine/results.py, planner/engine/plan.py, tests/test_phase_02.py</files>
  <behavior>
    - `PlanResult` exposes `errors: list[str]` (default `field(default_factory=list)` since the dataclass is frozen — use `dataclasses.field`). Existing callers that pass positional args keep working; if any positional construction exists in plan.py, switch it to keyword args.
    - `cmd_plan` populates `PlanResult.errors` with every line currently printed to stderr from inside the pre-search bailout paths in `_build_active_index` (and any sibling early-return print-to-stderr sites in plan.py). The stderr print stays AS-IS for CLI users; the structured field is additive.
    - In `tests/test_phase_02.py`:
      * Rewrite the `plan_in_temp_dir` helper body: replace `result = run_planner(root=tmp_path); assert result.returncode == 0, result.stdout + result.stderr` with `from planner.engine import cmd_plan` (top-of-file import, alongside the existing imports), `result = cmd_plan(data_root=tmp_path); assert result.exit_code == 0, "\n".join(result.errors)`. Keep the subsequent `yaml.safe_load((tmp_path / "schedule.yaml").read_text())` exactly as-is.
      * Rewrite the `check_in_temp_dir` helper body: replace `return run_planner("check", root=tmp_path)` with `from planner.engine import cmd_check` (top-of-file import) and `return cmd_check(data_root=tmp_path)`. Change its return-type annotation to `CheckResult` and re-import `CheckResult` from `planner.engine`.
      * Update the three callers of `check_in_temp_dir` (`test_product_formula_ref_validator_rejects_missing_substance`, `test_product_schema_accepts_description_urls`, `test_malformed_stack_entry_reports_schema_error`):
        - Replace `result.returncode` with `result.exit_code`.
        - Replace `result.stdout + result.stderr` with `"\n".join(result.errors + result.info)` for the substring checks.
        - Drop the `RunResult` import from `tests.helpers` if it becomes unused (only `ROOT` and `run_planner` should remain — `run_planner` is still used by `test_cli_help_exposes_simple_agent_commands`).
      * Leave `test_cli_help_exposes_simple_agent_commands` untouched.
  </behavior>
  <action>Extend PlanResult with an additive `errors: list[str]` field (use `dataclasses.field(default_factory=list)`; keep `frozen=True`). Audit plan.py for `file=sys.stderr` print-then-return-None sites in `_build_active_index` and adjacent early-bailout helpers, and have them additionally append the formatted message to a local errors list that cmd_plan threads into the returned PlanResult. Then rewrite the two helpers in tests/test_phase_02.py to call cmd_plan / cmd_check directly, retarget the three check_in_temp_dir consumers to PlanResult/CheckResult fields, and keep the --help test on run_planner. Run `uv run pyright` and `uv run pytest tests/test_phase_02.py` until both clean. Do not silence pyright with `type: ignore` or rule downgrades — if pyright complains, fix the call site.</action>
  <verify>
    <automated>cd /home/j2h4u/repos/j2h4u/supp-slotter && uv run pytest tests/test_phase_02.py -x -q && uv run pyright && rg -n 'run_planner' tests/test_phase_02.py | grep -v -E 'test_cli_help_exposes_simple_agent_commands|from tests.helpers import' | grep -c run_planner | grep -qx 0</automated>
  </verify>
  <done>
    - `PlanResult` has an `errors: list[str]` field; `cmd_plan` populates it from the workout-slot rejection and the no-active-items rejection paths.
    - `tests/test_phase_02.py` runs entirely in-process except for the `--help` CLI-contract test.
    - `uv run pytest tests/test_phase_02.py` passes.
    - `uv run pyright` reports 0/0/0.
    - The grep gate above returns 0 (no stray `run_planner` calls outside the --help test).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Convert tests/test_phase_03.py to direct cmd_* calls</name>
  <files>tests/test_phase_03.py</files>
  <behavior>
    - Top-of-file imports gain `cmd_check` and `cmd_plan` from `planner.engine` (note: `cmd_plan` likely already imported; verify). `run_planner` import stays — it's still used by the 4 review-substance CLI-contract tests.
    - Each of the 7 listed tests below uses `cmd_check(data_root=tmp_path)` or `cmd_plan(data_root=tmp_path)` and asserts against the typed result. Concrete per-test assertion edits:
      * `test_check_auto_renames_files_when_names_change` — `result = cmd_check(data_root=tmp_path); assert result.exit_code == 0, "\n".join(result.errors)`. Disk-state assertions (file renamed, stacks.yaml content) stay verbatim.
      * `test_check_warns_about_products_without_stack_entry` — `result = cmd_check(data_root=tmp_path); assert result.exit_code == 0`; replace `"product 'prd_0000000002' has no stack entry" in result.stdout` with `... in "\n".join(result.info)`; replace `"Add it to a stack if it is on the shelf" in result.stdout` with `... in "\n".join(result.info)`; replace `"refresh" not in result.stdout` with `"refresh" not in "\n".join(result.info + result.errors)`.
      * `test_duplicate_stack_item_across_stacks_is_rejected` — `result = cmd_check(data_root=tmp_path); assert result.exit_code != 0`; replace combined_output with `combined_output = "\n".join(result.errors + result.info)` and keep the two substring checks.
      * `test_auto_maintenance_lock_only_blocks_mutations` — two cmd_check calls; first asserts `exit_code == 0`; second asserts `exit_code != 0` and `"another planner process is running" in "\n".join(blocked_result.errors)`.
      * `test_workout_activity_product_is_not_scheduled_as_daily` — `result = cmd_plan(data_root=tmp_path); assert result.exit_code != 0`; replace combined_output with `combined_output = "\n".join(result.errors)` and keep the two substring checks (`"prd_cfce0b36b6"`, `"has no workout pillbox slots"`). This relies on Task 1's `PlanResult.errors` addition.
      * `test_duplicate_slot_ids_across_pillboxes_are_rejected` — `result = cmd_check(data_root=tmp_path); assert result.exit_code != 0`; combined_output via `"\n".join(result.errors + result.info)`; keep both substring checks.
      * `test_relation_validation_rejects_unknown_substance_name` — `result = cmd_check(data_root=tmp_path); assert result.exit_code != 0`; replace `in result.stderr` with `in "\n".join(result.errors)`.
    - The 4 review-substance CLI-contract tests stay UNTOUCHED (rejects_missing_file, rejects_path_outside_substances_dir, rejects_non_yaml_suffix, rejects_empty_traits_file).
  </behavior>
  <action>Edit tests/test_phase_03.py: add `cmd_check` to the existing `from planner.engine import ...` line if not present, then apply the seven per-test rewrites described in `<behavior>` above. Leave the 4 review-substance CLI tests as-is. Run `uv run pyright` and `uv run pytest tests/test_phase_03.py` until both clean. If any test asserts on a string that turns out to live in `result.info` rather than `result.errors` (or vice-versa), pick whichever bucket cmd_check actually populates and update the assertion accordingly — do NOT bypass typing by joining everything blindly when the bucket is determinable from check.py.</action>
  <verify>
    <automated>cd /home/j2h4u/repos/j2h4u/supp-slotter && uv run pytest tests/test_phase_03.py -x -q && uv run pyright && python3 -c "import re,sys; src=open('tests/test_phase_03.py').read(); allowed={'test_review_substance_rejects_missing_file','test_review_substance_rejects_path_outside_substances_dir','test_review_substance_rejects_non_yaml_suffix','test_review_substance_rejects_empty_traits_file'}; bad=[]; cur=None; [bad.append((cur,line.strip())) for line in src.splitlines() for cur in [re.match(r'def (test_[a-z0-9_]+)', line).group(1) if re.match(r'def (test_[a-z0-9_]+)', line) else cur] if 'run_planner(' in line and cur not in allowed]; sys.exit(0 if not bad else (print(bad) or 1))"</automated>
  </verify>
  <done>
    - All 7 enumerated tests in tests/test_phase_03.py call `cmd_check` or `cmd_plan` directly and assert against typed fields.
    - The 4 review-substance CLI-contract tests still use `run_planner` (verified by the python gate above: only those four test bodies may contain `run_planner(`).
    - `uv run pytest tests/test_phase_03.py` passes.
    - `uv run pyright` reports 0/0/0.
  </done>
</task>

</tasks>

<verification>
Run the full suite plus type-check at the end:

```bash
cd /home/j2h4u/repos/j2h4u/supp-slotter
uv run pytest -x -q
uv run pyright
```

Expected: pytest green (all 99-ish tests pass); pyright `0 errors, 0 warnings, 0 informations`.

Final subprocess-usage audit:

```bash
rg -n 'run_planner\(' tests/test_phase_02.py tests/test_phase_03.py
```

Expected output (exactly 5 lines):
- `tests/test_phase_02.py`: 1 line inside `test_cli_help_exposes_simple_agent_commands`.
- `tests/test_phase_03.py`: 4 lines, one inside each of the kept review-substance tests.
</verification>

<success_criteria>
- `tests/test_phase_02.py` and `tests/test_phase_03.py` both run in-process for engine-behaviour assertions; only CLI-contract tests still spawn the CLI layer.
- `PlanResult` carries the workout-slot rejection text as structured `errors`, observable without redirecting stderr.
- `uv run pytest` is green and `uv run pyright` is 0/0/0.
- No `type: ignore`, no rule downgrade, no `# noqa` added.
- `tests/helpers.py` is unchanged; the `RunResult` dataclass and `run_planner` helper are still used by the kept CLI-contract tests.
</success_criteria>

<output>
After completion, create `.planning/quick/260512-j8x-convert-remaining-subprocess-tests-to-di/260512-j8x-SUMMARY.md` covering: files touched, subprocess-call delta (before/after counts), `PlanResult.errors` rationale, and the final `rg` gate output.
</output>
