---
mode: quick-full
status: planned
quick_id: 260508-m3i
must_haves:
  truths:
    - Active unmatched concerns must be visible in generated review output.
    - Review warnings should have a concise grouped surface for human/agent action.
    - Pillbox slot IDs must not silently collide.
    - Goal coverage must remain review-only and must not affect scheduling.
  artifacts:
    - planner.py
    - schedule.yaml
    - tests/
    - docs/domain-model.md
    - SKILL.md
  key_links:
    - docs/domain-model.md
    - data/pillboxes.yaml
    - schema/substance.schema.json
---

# Quick Task 260508-m3i: Improve Review Layer And Pillbox Guardrails

## Task 1: Surface Active Unmatched Concerns

files:
- planner.py
- schedule.yaml
- tests/test_phase_03.py

action:
- Convert active product/substance `unmatched_concerns` into generated warnings.
- Include them in `action_points` when relevant.
- Keep `check` INFO output unchanged.

verify:
- `uv run planner.py plan`
- `uv run pytest -q`

done:
- Active concerns appear in generated `schedule.yaml`.

## Task 2: Add Review Contexts And Better Goal Coverage

files:
- planner.py
- schedule.yaml
- tests/test_phase_03.py

action:
- Add a compact `review_contexts` section grouped by practical concern.
- Split goal coverage into `covered`, `inactive`, and `missing` without changing scheduler logic.

verify:
- Generated schedule remains readable and contains no debug scoring metadata.
- Existing goal coverage tests pass after update.

done:
- Schedule gives high-level review clusters and richer goal review.

## Task 3: Add Pillbox Guardrails And Docs

files:
- planner.py
- docs/domain-model.md
- SKILL.md
- tests/test_phase_01.py

action:
- Reject duplicate slot IDs across pillboxes.
- Document review contexts, unmatched concern surfacing, and the no-goal-scheduler boundary.

verify:
- `uv run planner.py check`
- `uv run planner.py doctor`
- `uv run pytest -q`

done:
- Duplicate slot IDs fail validation and docs match current behavior.
