---
phase: 260510-41b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - planner/engine/plan.py
  - tests/test_phase_02.py
  - planner/cards/dashboards.py
  - planner/cards/substance.py
  - planner/engine/doctor.py
  - planner/engine/_scheduling.py
autonomous: true
requirements: [KQ6]
must_haves:
  truths:
    - All renamed symbols resolve with no NameError at import
    - pyright reports 0 errors after renames
    - pytest passes with no regressions
  artifacts:
    - path: planner/engine/plan.py
      provides: B&B solver with readable local names
    - path: tests/test_phase_02.py
      provides: Test helpers with intent-revealing names
    - path: planner/cards/dashboards.py
      provides: Module-level _member_label_for_substance and _member_label_for_member helpers
    - path: planner/cards/substance.py
      provides: _substance_fallback_name replacing substance_display_name
    - path: planner/engine/doctor.py
      provides: stack_groups alias removed, cast inlined
    - path: planner/engine/_scheduling.py
      provides: build_substance_slot_names param renamed assigned_item_ids
  key_links:
    - from: plan.py build_substance_slot_names call site (line 325)
      to: _scheduling.py build_substance_slot_names param
      via: keyword argument slot_items → assigned_item_ids
    - from: substance_cluster_label (substance.py)
      to: _substance_fallback_name
      via: two internal call sites (lines 127, 136)
---

<objective>
Close the cosmetic naming tail for KQ6: rename B&B solver locals in plan.py,
rename test helpers in test_phase_02.py, lift two member_label closures in
dashboards.py to module level (with distinct names — their signatures differ),
rename substance_display_name to a private helper, inline the stack_groups
alias in doctor.py, and rename the slot_items parameter in _scheduling.py.

Purpose: Eliminate the last cluster of stale or opaque names from the B&B
search, test infrastructure, and production helpers, completing KQ6.
Output: 6 modified files; all names match the intent described in the task spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260510-41b-kq6-close-cosmetic-tail-rename-solver-lo/260510-41b-PLAN.md

<!-- Key interfaces the executor needs — extracted from the codebase. -->

<interfaces>
<!-- plan.py — locals being renamed (all within cmd_plan scope) -->
<!--   active         (dict[str, set[str]])  → item_traits          -->
<!--   active_order   (list[str])            → item_ids_in_order    -->
<!--   candidates     (dict[...])            → scored_slots_by_item -->
<!--   candidate_scores (dict[...])          → slot_score_lookup    -->
<!--   valid          (list, loop-local)     → feasible_slots       -->
<!--   sorted_items   (list[str])            → items_by_scheduling_priority -->
<!--   seed_with_greedy_assignment (def+call)→ initialize_best_with_greedy -->
<!--   assignment_tie_key (def+call)         → slot_order_key       -->
<!--                                                                 -->
<!-- IMPORTANT: `active` is ALSO a parameter name in:               -->
<!--   _build_active_index return value (variable name in caller)    -->
<!--   _build_schedule_output signature (param: active at line 269)  -->
<!--   Both must be renamed consistently.                            -->
<!--   `active_order` is ALSO a param in _build_schedule_output      -->
<!--   (line 257) and used throughout that function body.            -->
<!--                                                                 -->
<!-- dashboards.py — two member_label closures with DIFFERENT sigs: -->
<!--   build_dashboard_review:  member_label(substance_id: str)      -->
<!--   check_dashboards:        member_label(member: dict[str, Any]) -->
<!--   Lift to TWO distinct module-level helpers:                    -->
<!--     _member_label_for_substance(substance_id: str,              -->
<!--                                 substances: dict[str, Substance]) -> str -->
<!--     _member_label_for_member(member: dict[str, Any],            -->
<!--                              substances: dict[str, Substance]) -> str -->
<!--                                                                 -->
<!-- substance.py — substance_display_name is NOT exported in        -->
<!--   __init__.py; only called internally by substance_cluster_label-->
<!--   (lines 127, 136). Safe to rename to _substance_fallback_name. -->
<!--                                                                 -->
<!-- doctor.py — stack_groups alias (line 83):                       -->
<!--   stack_groups = cast(dict[str, Any], stacks_data)              -->
<!--     if isinstance(stacks_data, dict) else {}                    -->
<!--   Used at: line 99 (stack_groups.items()), line 103 (set(stack_groups)),-->
<!--            line 104 (set(stack_groups))                          -->
<!--   Inline each use as cast(dict[str, Any], stacks_data)          -->
<!--   if isinstance(stacks_data, dict) else {} — or just use        -->
<!--   slots_dict pattern: already have a typed local for this.      -->
<!--   Simplest correct approach: replace each reference with the     -->
<!--   already-computed `stack_groups` local by inlining the right   -->
<!--   expression. Since stacks_data cannot change, compute once     -->
<!--   as a typed local with a meaningful name if needed, or simply  -->
<!--   inline the cast at each call site.                            -->
<!--                                                                 -->
<!-- _scheduling.py — build_substance_slot_names kwarg:              -->
<!--   param `slot_items` → `assigned_item_ids`                      -->
<!--   Internal use: `for item_id in slot_items:` → `for item_id in assigned_item_ids:` -->
<!--   Call site in plan.py line 325: slot_items=slot_item_ids → assigned_item_ids=slot_item_ids -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task A: Rename B&B solver locals in plan.py (8 symbols)</name>
  <files>planner/engine/plan.py</files>
  <action>
Apply all 8 renames within planner/engine/plan.py. Work through the file
top-to-bottom, touching every occurrence of each old name. The renames are
purely local — no other file imports these names — except for the two places
noted below where parameter names in helper functions must also change.

Rename map (old → new):

1. `active` → `item_traits`
   - Line 133 in _build_active_index: `active: dict[str, set[str]] = {}`
   - Line 156: `active[item_id] = effective`
   - Line 173: `if not active:`
   - Line 182: `for item_id, traits in active.items():`
   - Line 192 (return tuple element): keep position, variable name in caller
   - In cmd_plan (line 487): unpacking variable `active` → `item_traits`
   - Lines 501, 523, 595, 666, 719: all uses of `active` in cmd_plan
   - In `_build_schedule_output` signature (line 269): param `active` → `item_traits`
   - All uses of `active` inside `_build_schedule_output` body: lines 363, 399, 400

2. `active_order` → `item_ids_in_order`
   - In `_build_schedule_output` signature (line 257): param name
   - All uses inside `_build_schedule_output` body: lines 308, 322, 348, 388, 393
   - In cmd_plan (line 523): `active_order = list(active)`
   - Line 528-529: sort key uses `active_order.index(item)`
   - Line 701/706 call site: keyword arg `active_order=active_order` → `item_ids_in_order=item_ids_in_order`

3. `candidates` → `scored_slots_by_item`
   - Line 500: `candidates: dict[...] = {}`
   - Lines 502, 519: loop over slots building candidates
   - Lines 527-530: sort key references `candidates[item]`
   - Line 533: comprehension uses `candidates.items()`
   - Line 539: `max(candidate_scores[item].values())` (via slot_score_lookup after rename 4)
   - In `seed_with_greedy_assignment` closure (line 598): `candidates[item]`
   - In `search` closure (line 668): `candidates[item]`

4. `candidate_scores` → `slot_score_lookup`
   - Line 533: `candidate_scores = {...}`
   - Line 539: `max(candidate_scores[item].values())`

5. `valid` → `feasible_slots` (loop-local only, lines 502-519)
   - Line 502: `valid: list[...] = []`
   - Line 511: `valid.append(...)`
   - Lines 513-516: `if not valid:` / error message block
   - Line 518: `valid.sort(...)`
   - Line 519: `scored_slots_by_item[sid] = valid`

6. `sorted_items` → `items_by_scheduling_priority`
   - Line 524: `sorted_items = sorted(...)`
   - Line 536: `[0] * (len(sorted_items) + 1)`
   - Line 537: `for index in range(len(sorted_items) - 1, -1, -1):`
   - Line 538: `item = sorted_items[index]`
   - Line 539: `max(slot_score_lookup[item].values())`
   - In `balance_lower_bound` (line 557): `for item in sorted_items[search_index:]:`
   - In `seed_with_greedy_assignment` (line 594): `for item in sorted_items:`
   - In `search` (line 649): `if index == len(sorted_items):`
   - In `search` (line 665): `item = sorted_items[index]`

7. `seed_with_greedy_assignment` → `initialize_best_with_greedy`
   - Line 584: `def seed_with_greedy_assignment() -> None:`
   - Line 688: `seed_with_greedy_assignment()`

8. `assignment_tie_key` → `slot_order_key`
   - Line 551: `def assignment_tie_key(...) -> tuple[int, ...]:`
   - Line 552: body uses slot_order — unchanged
   - Line 634: `best_key = assignment_tie_key(greedy_assignment)`
   - Line 651: `candidate_key = assignment_tie_key(assignment)`
   - Line 662: `best_key = candidate_key` — no rename needed here
  </action>
  <verify>
    <automated>cd /home/j2h4u/repos/j2h4u/supp-slotter && python -m pyright planner/engine/plan.py 2>&1 | tail -5 && python -m pytest tests/test_phase_02.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    pyright: 0 errors on plan.py.
    pytest: all tests pass (no regressions from plan.py renames).
    None of the old names (active, active_order, candidates, candidate_scores,
    valid, sorted_items, seed_with_greedy_assignment, assignment_tie_key) appear
    as standalone identifiers in cmd_plan scope — verify with:
    grep -n "\bactive\b\|\bactive_order\b\|\bcandidates\b\|\bcandidate_scores\b\|\bsorted_items\b\|\bseed_with_greedy\b\|\bassignment_tie_key\b" planner/engine/plan.py
    (only legitimate matches are in string literals or comments, if any).
  </done>
</task>

<task type="auto">
  <name>Task B+D: Test helper renames (test_phase_02.py) and production naming (substance.py, doctor.py, _scheduling.py)</name>
  <files>
    tests/test_phase_02.py,
    planner/cards/substance.py,
    planner/engine/doctor.py,
    planner/engine/_scheduling.py,
    planner/engine/plan.py
  </files>
  <action>
Apply four independent rename sets across five files. None of these symbols
cross file boundaries except where noted.

--- B: tests/test_phase_02.py (5 renames, def + all call sites) ---

B1: `run_temp_plan` → `plan_in_temp_dir`
  - def at line 57
  - calls at lines 513, 579, 640, 694, 744, 787

B2: `run_temp_check` → `check_in_temp_dir`
  - def at line 72
  - calls at lines 375, 396, 409

B3: `run_repo_plan_preserving_schedule` → `plan_against_live_repo`
  - def at line 82
  - call at line 424

B4: `group_stack_items` → `normalize_stack_items`
  - def at line 107
  - call at line 220 (inside write_split_model_fixture / write_minimal_planner_fixture after B5)

B5: `write_split_model_fixture` → `write_minimal_planner_fixture`
  - def at line 167
  - calls at lines 487, 548, 599, 656, 720, 761

--- D1: planner/cards/substance.py ---

Rename `substance_display_name` → `_substance_fallback_name`.
  - def at line 87
  - call site at line 127: `display_names.setdefault(name_key, substance_display_name(substance))`
  - call site at line 136: `return substance_display_name(substances[component[0]])`
  This function is NOT exported from the package __init__ and is not imported
  by any other module. Confirm with: grep -r "substance_display_name" planner/
  before editing; if any external import exists, add it to this task's scope.

--- D2: planner/engine/doctor.py ---

Remove the `stack_groups` alias (line 83). Inline the cast at each use site:
  - Line 83 (remove): `stack_groups = cast(dict[str, Any], stacks_data) if isinstance(stacks_data, dict) else {}`
  - Line 99: `for stack, items in stack_groups.items():` → `for stack, items in (cast(dict[str, Any], stacks_data) if isinstance(stacks_data, dict) else {}).items():`
  - Line 103: `set(stack_groups)` → `set(cast(dict[str, Any], stacks_data) if isinstance(stacks_data, dict) else {})`
  - Line 104: `set(stack_groups)` → same expression

  Since this is verbose, prefer introducing a well-named local ABOVE the
  existing `slots_data = load_yaml(...)` block instead:
    stacks_by_name: dict[str, Any] = cast(dict[str, Any], stacks_data) if isinstance(stacks_data, dict) else {}
  Then replace all three `stack_groups` references with `stacks_by_name`.
  Remove the old `stack_groups = ...` line.

--- D3: planner/engine/_scheduling.py + planner/engine/plan.py ---

In _scheduling.py `build_substance_slot_names`:
  - Signature (line 121): `slot_items: list[str]` → `assigned_item_ids: list[str]`
  - Body (line 127): `for item_id in slot_items:` → `for item_id in assigned_item_ids:`

In plan.py call site (line 325):
  - `slot_items=slot_item_ids` → `assigned_item_ids=slot_item_ids`
  (The local variable `slot_item_ids` keeps its name — only the keyword arg changes.)
  </action>
  <verify>
    <automated>cd /home/j2h4u/repos/j2h4u/supp-slotter && python -m pyright planner/ 2>&1 | tail -5 && python -m pytest tests/test_phase_02.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    pyright: 0 errors across planner/.
    pytest: all tests pass.
    Old names absent from their respective files (spot-check):
      grep -n "\brun_temp_plan\b\|\brun_temp_check\b\|\brun_repo_plan\b\|\bgroup_stack_items\b\|\bwrite_split_model_fixture\b" tests/test_phase_02.py
      grep -n "\bsubstance_display_name\b" planner/cards/substance.py
      grep -n "\bstack_groups\b" planner/engine/doctor.py
      grep -n "\bslot_items\b" planner/engine/_scheduling.py planner/engine/plan.py
    All greps return no matches (or only comments).
  </done>
</task>

<task type="auto">
  <name>Task C: Lift member_label closures in dashboards.py to module level</name>
  <files>planner/cards/dashboards.py</files>
  <action>
The two `member_label` closures have different signatures and cannot be merged.
Lift each to a distinct module-level private function.

--- New module-level functions (place after imports, before load_dashboard) ---

def _member_label_for_substance(
    substance_id: str, substances: dict[str, Substance]
) -> str:
    """Return the formatted display name for a substance_id, falling back to the raw id."""
    substance = substances.get(substance_id)
    if substance is not None:
        return format_substance_name(substance)
    return substance_id


def _member_label_for_member(
    member: dict[str, Any], substances: dict[str, Substance]
) -> str:
    """Return display label for a dashboard member dict (substance ref or name fallback)."""
    ref = member.get("substance")
    if isinstance(ref, str):
        substance = substances.get(ref)
        if substance is not None:
            return format_substance_name(substance)
        return ref
    name = member.get("name")
    return str(name or "")

--- In build_dashboard_review ---

Remove the local `def member_label(substance_id: str) -> str:` closure entirely.
Replace every call `member_label(...)` with `_member_label_for_substance(..., substances)`.

Call sites inside build_dashboard_review:
  - Line 141: `label = member_label(member.substance)` → `label = _member_label_for_substance(member.substance, substances)`
  - Line 181: `key=lambda sid: member_label(sid).casefold()` → `key=lambda sid: _member_label_for_substance(sid, substances).casefold()`

--- In check_dashboards ---

Remove the local `def member_label(member: dict[str, Any]) -> str:` closure entirely.
Replace every call `member_label(...)` with `_member_label_for_member(..., substances)`.

Call sites inside check_dashboards:
  - Line 228: `labels = [member_label(m) for m in members]` → `labels = [_member_label_for_member(m, substances) for m in members]`
  </action>
  <verify>
    <automated>cd /home/j2h4u/repos/j2h4u/supp-slotter && python -m pyright planner/cards/dashboards.py 2>&1 | tail -5 && python -m pytest tests/ -x -q -k "dashboard" 2>&1 | tail -10</automated>
  </verify>
  <done>
    pyright: 0 errors on dashboards.py.
    No local `def member_label` remains in the file:
      grep -n "def member_label" planner/cards/dashboards.py  # → no matches
    Both module-level helpers present:
      grep -n "def _member_label_for" planner/cards/dashboards.py  # → 2 matches
    All test_phase_02.py tests that exercise plan/check still pass (run full suite
    to confirm no regression from the dashboard output path change):
      python -m pytest tests/ -x -q 2>&1 | tail -5
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Pure internal refactor — no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41b-01 | Tampering | Rename correctness | accept | All renames are within-file or keyword-arg only; pyright + pytest gate catches any missed site |
</threat_model>

<verification>
Full suite after all three tasks:

```bash
cd /home/j2h4u/repos/j2h4u/supp-slotter
python -m pyright planner/ 2>&1 | tail -5
python -m pytest tests/ -x -q 2>&1 | tail -10
```

Both must exit 0. Any pyright error or test failure means a rename site was missed.

Spot-check for stale names after all tasks complete:
```bash
grep -rn "\bactive\b" planner/engine/plan.py | grep -v "inactive\|active_substances\|active_count\|active_substance\|active_stack\|active_components\|item_products\|#"
grep -n "\bsubstance_display_name\b" planner/cards/substance.py
grep -n "\bstack_groups\b" planner/engine/doctor.py
grep -n "\bslot_items\b" planner/engine/_scheduling.py planner/engine/plan.py
grep -n "def member_label" planner/cards/dashboards.py
```
All should return no matches (or only comment lines).
</verification>

<success_criteria>
- pyright reports 0 errors on planner/ (strict mode, same baseline as pre-task)
- pytest tests/ exits 0 with no regressions
- 8 B&B solver names replaced in plan.py (item_traits, item_ids_in_order, scored_slots_by_item, slot_score_lookup, feasible_slots, items_by_scheduling_priority, initialize_best_with_greedy, slot_order_key)
- 5 test helper names replaced in test_phase_02.py
- 2 module-level helpers in dashboards.py replace both member_label closures
- substance_display_name private-ized to _substance_fallback_name
- stack_groups alias removed from doctor.py
- build_substance_slot_names kwarg renamed to assigned_item_ids
</success_criteria>

<output>
After completion, create `.planning/quick/260510-41b-kq6-close-cosmetic-tail-rename-solver-lo/260510-41b-SUMMARY.md`
</output>
