---
phase: 260510-is4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_scheduling_units.py
autonomous: true
requirements:
  - C4
  - C5
must_haves:
  truths:
    - "compute_slot_score food-axis branch is exercised: same-food matches, different-food does not, food-axis block is observed"
    - "compute_slot_score block path also asserts score == 0"
    - "collect_missing_support_relations warning-direction (target active, source absent) is asserted to emit one structurally-correct warning"
  artifacts:
    - path: "tests/test_scheduling_units.py"
      provides: "Food-axis coverage for compute_slot_score and warning-direction coverage for collect_missing_support_relations"
      contains: "food-axis tests + warning-direction support test"
  key_links:
    - from: "tests/test_scheduling_units.py"
      to: "planner.engine._scheduling.compute_slot_score"
      via: "TraitEffectMatch(food=...) fixtures"
      pattern: "TraitEffectMatch.*food="
    - from: "tests/test_scheduling_units.py"
      to: "planner.cards.relations.collect_missing_support_relations"
      via: "active_substances containing target only"
      pattern: "missing_support_substance"
---

<objective>
Close two unit-test coverage gaps surfaced by code-quality review:

- **C4** — `compute_slot_score` is never tested on the `food` discriminant; the food
  branch in `slot_matches` is silently uncovered. A polarity inversion would not
  fail the suite.
- **C5** — `collect_missing_support_relations` only has a negative-direction test
  (source active, target absent → empty). The warning-emitting direction
  (target active, source absent → one warning) is unverified.

Purpose: prevent silent regressions on the food axis and the supports-direction
warning structure, both of which are load-bearing for slot scoring and the
relations-backed "missing support" warning UI.

Output: amended `tests/test_scheduling_units.py` with three new food-axis tests
under SI-04 plus one warning-direction test under SI-08, plus a tightened
assertion on the existing block test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@tests/test_scheduling_units.py
@planner/engine/_scheduling.py
@planner/cards/relations.py

<interfaces>
<!-- Confirmed signatures from current source — no codebase exploration needed. -->

From planner/engine/_scheduling.py:
```python
def compute_slot_score(
    trait_ids: set[str],
    slot: Slot,
    trait_defs: dict[str, TraitDef],
    trait_sources: dict[str, list[str]],
) -> tuple[int, bool, list[str]]:
    """Returns (score, blocked, reasons)."""

def slot_matches(slot: Slot, match: TraitEffectMatch) -> bool:
    # near and food are checked independently; both default to None (wildcard).
```

From planner/cards/relations.py:
```python
def collect_missing_support_relations(
    substances: dict[str, Substance],
    active_substances: set[str],
    global_relations: list[Relation] | None = None,
) -> list[dict[str, Any]]:
    # Emits warning when relation.type == "supports", target endpoint is active,
    # and source endpoint is absent. Warning dict shape:
    #   {"type": "missing_support_substance",
    #    "source_substance", "source_name",
    #    "target_substance", "target_name",
    #    "reason", "action"}
```

From planner/contracts (already imported in the test file):
- `TraitEffectMatch(near: str | None = None, food: bool | None = None)`
- `TraitEffect(match, level=None, block=False)`
- `Relation(type, reason, source_substance, target_substance, source_name=None, target_name=None, action=None)`

Existing test conventions in this file (SI-04 section, lines 32–130):
- `_slot(near=..., food=...)` factory builds a `Slot`
- `_trait_def(trait_id, *, effects=(), separate_from=())` builds a `TraitDef`
- `_NO_SOURCES: dict[str, list[str]] = {}` is the shared empty trait_sources sentinel
- Tests use `compute_slot_score(traits_set, slot, trait_defs, _NO_SOURCES)` and unpack `(score, blocked, _)`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add food-axis coverage for compute_slot_score (C4)</name>
  <files>tests/test_scheduling_units.py</files>
  <behavior>
    Three new tests in the SI-04 section, plus a tightened assertion on the
    existing block test:

    - `test_compute_slot_score_food_axis_match`: `TraitEffectMatch(near=None, food=False)`
      with a `prefer_strong` effect against `_slot(near="breakfast", food=False)`
      → score == LEVEL_SCORES["prefer_strong"], blocked is False.
      (Confirms food-axis match works when near is wildcard.)

    - `test_compute_slot_score_food_axis_mismatch`: same effect (food=False)
      against `_slot(near="breakfast", food=True)` → score == 0, blocked is False.
      (Confirms food-axis discriminant blocks accumulation when food differs.)

    - `test_compute_slot_score_food_axis_block`: `TraitEffectMatch(near=None, food=False)`
      with `block=True` against `_slot(near="breakfast", food=False)` → blocked is True.
      (Confirms block path fires on food-only match.)

    - Tighten existing `test_compute_slot_score_block_on_matching_slot`: also
      assert `score == 0` (a blocked slot accumulates no score).
  </behavior>
  <action>
    Edit `tests/test_scheduling_units.py`. Insert the three new food-axis tests
    immediately after `test_compute_slot_score_no_matching_effects` (around line
    131), before the `# SI-05: must_separate` divider.

    Follow existing conventions exactly:
    - Use the `_slot`, `_trait_def`, `_substance`, `_NO_SOURCES` fixtures already
      defined in the file. Do not introduce new helpers.
    - Use `TraitEffectMatch(near=None, food=False)` — explicitly pass `near=None`
      so the food-only intent is clear at the call site.
    - Trait IDs follow the existing `intake:` / `effect:` namespace style.
    - Unpack as `score, blocked, _ = compute_slot_score(...)` matching existing
      tests (the third tuple element is the reasons list, ignored here).

    Then modify `test_compute_slot_score_block_on_matching_slot` (currently
    around lines 99–108): change the unpack from `_, blocked, _` to
    `score, blocked, _` and add `assert score == 0` after the `assert blocked
    is True`. Do not weaken or remove any existing assertion.

    Do not touch any other test or any production code.
  </action>
  <verify>
    <automated>cd /home/j2h4u/repos/j2h4u/supp-slotter &amp;&amp; python -m pytest tests/test_scheduling_units.py -x -q</automated>
  </verify>
  <done>
    Four new/changed assertions pass: three new food-axis tests + tightened
    block test asserting score == 0. Full `tests/test_scheduling_units.py` suite
    green. Pyright reports zero errors/warnings on `planner/ tests/`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add warning-direction test for collect_missing_support_relations (C5)</name>
  <files>tests/test_scheduling_units.py</files>
  <behavior>
    One new test in the SI-08 section:

    - `test_collect_missing_support_relations_target_active_source_absent_emits_warning`:
      Build a `Relation(type="supports", source_substance="sub_src",
      target_substance="sub_tgt", reason="supports pair")`. Build `substances`
      containing both `sub_src` and `sub_tgt`. Set `active_substances = {"sub_tgt"}`
      (target active, source absent). Call `collect_missing_support_relations`.
      Assert:
        - returned list has exactly one element
        - element["type"] == "missing_support_substance"
        - element["source_substance"] == "sub_src"
        - element["source_name"] matches the source Substance.name
        - element["target_substance"] == "sub_tgt"
        - element["target_name"] matches the target Substance.name
        - element["reason"] == "supports pair"

    This complements the existing
    `test_collect_missing_support_relations_source_active_target_absent_returns_empty`
    by covering the symmetric, warning-emitting direction.
  </behavior>
  <action>
    Edit `tests/test_scheduling_units.py`. Append the new test at the end of the
    SI-08 section (after the existing
    `test_collect_missing_support_relations_source_active_target_absent_returns_empty`,
    currently the last test in the file).

    Conventions:
    - Use the `_substance(sub_id, name)` fixture for both substances.
    - Build the `Relation` inline using the same constructor pattern as the
      existing SI-08 test.
    - Verify the dict structure field-by-field rather than asserting on the
      whole dict object — so that future fields added to the warning don't
      silently break the test, and so the key fields under test stay obvious.
    - Use `len(result) == 1` then index `result[0]` for clarity.

    Do not modify the existing SI-08 test or any production code.
  </action>
  <verify>
    <automated>cd /home/j2h4u/repos/j2h4u/supp-slotter &amp;&amp; python -m pytest tests/test_scheduling_units.py -x -q</automated>
  </verify>
  <done>
    New test passes. Full `tests/test_scheduling_units.py` suite green.
    Pyright reports zero errors/warnings on `planner/ tests/`. Two atomic
    commits exist (one for C4, one for C5) with `test(260510-is4): ...`
    style messages.
  </done>
</task>

</tasks>

<verification>
- `cd /home/j2h4u/repos/j2h4u/supp-slotter && python -m pytest tests/test_scheduling_units.py -x -q` → all tests pass
- `cd /home/j2h4u/repos/j2h4u/supp-slotter && python -m pyright planner/ tests/ --outputjson 2>/dev/null | python -m json.tool 2>/dev/null | grep -cE '"error"|"warning"' || true` → 0
- `git log --oneline -3` shows two new commits, one per task, scoped to `tests/test_scheduling_units.py`
</verification>

<success_criteria>
- All three food-axis behaviours of `compute_slot_score` are asserted (match, mismatch, block)
- Existing block test additionally asserts `score == 0`
- `collect_missing_support_relations` warning-emitting direction is asserted with full dict structure
- No production code modified
- Full test suite green, pyright clean
- Two atomic commits, one per gap closure
</success_criteria>

<output>
After completion, create `.planning/quick/260510-is4-close-c4-and-c5-add-food-axis-coverage-t/260510-is4-01-SUMMARY.md`
</output>
